---
phase: 08-visual-redesign-accessibility-pwa-safe-area
plan: 04
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/pay-setup-forms.tsx
  - src/app/(app)/settings/salary/page.tsx
  - src/app/(app)/onboarding/page.tsx
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-05, UI-07]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "UI-03 (verbatim ROADMAP Phase 8 success criterion 2, confirmation-dialog half): Overwriting a salary shows a confirmation dialog with the before/after value. [Spec-less probe fallback note: UI-03 came back `unclassified`/`unresolved` from the deterministic edge-coverage engine — this truth is authored from ROADMAP.md's Phase 8 Success Criteria text plus 08-UI-SPEC.md's approved Confirmation Panel spec.] `pay-setup-forms.tsx`'s `SalaryForm`'s existing `pendingConfirmation` inline panel (08-CONTEXT.md confirms this already functionally satisfies UI-03 — restyle only, no new modal component) renders the old value struck-through in `--color-text-secondary` and the new value in `--color-text-primary` font-semibold, switching to `--color-accent` specifically when the new amount is greater than the old (per 08-UI-SPEC.md § Confirmation Panel), both using identical `tabular-nums` money formatting; the confirm button (\"Подтвердить и заменить\") uses the accent-filled primary-button style; no cancel button is added (editing the form again remains the implicit cancel, unchanged from today)."
    - "UI-01 (settings/salary + onboarding screens): Both screens keep their existing `isSubmitting`-driven disabled+label-swap loading pattern (\"Сохраняем…\"/\"Заменяем…\"/\"…\") restyled onto tokens — no new loading mechanism is introduced, per 08-UI-SPEC.md § UI Considerations row \"loading | ...salary-setup-form (form) | covered\". Every form's existing generic-Russian catch-block error message (\"Не удалось сохранить оклад/график. Попробуйте ещё раз.\") is restyled to `--color-destructive`, never replaced with raw exception text."
    - "UI-02 (salary/schedule/YTD forms + salary history list money/date formatting): Every `formatKopecks()` value in `settings/salary/page.tsx`'s salary-history list and every rendered rubles amount inside `pay-setup-forms.tsx`'s confirmation panel carries the `.tabular-nums` utility class."
    - "UI-05/UI-07 (form accessibility): Every input across `SalaryForm`/`ScheduleForm`/`YtdForm` (in `pay-setup-forms.tsx`) and the onboarding page keeps its existing real `<label htmlFor>`/`id` pair, gains a 2px accent-color `focus-visible` outline with visible offset, and 8px-radius tertiary-surface-bordered primary-surface backgrounds — matching 08-UI-SPEC.md § Form Fields exactly."
  artifacts:
    - src/components/pay-setup-forms.tsx
    - src/app/(app)/settings/salary/page.tsx
    - src/app/(app)/onboarding/page.tsx
  key_links:
    - "pay-setup-forms.tsx's SalaryForm/ScheduleForm/YtdForm exports → both settings/salary/page.tsx and onboarding/page.tsx import and render the identical components — this plan restyles the shared component exactly once, and both consuming pages inherit the restyle automatically; this is why these three files must be one plan (a parallel plan touching only one consumer page would conflict on pay-setup-forms.tsx)"
    - "SalaryForm's pendingConfirmation state → the accent-vs-neutral color branch (`submittedAmountRubles > existingAmountRubles`) → the only place in this phase where a color choice depends on a runtime data comparison rather than a fixed role — must not regress to a static color"
  prohibitions:
    - requirement_id: UI-03
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT replace pay-setup-forms.tsx's existing inline pendingConfirmation panel with a new modal/dialog component — 08-CONTEXT.md explicitly locks this as 'restyled, not rebuilt' since it already functionally satisfies UI-03."
    - requirement_id: UI-05
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT change any value, formula, or branch in src/domain/**, src/lib/db/**, or src/app/actions/salary.ts while restyling — presentation-only pass, guarded by a git status --porcelain check in every task's acceptance criteria. In particular, saveSalaryAction's confirmation-token/needsConfirmation logic (SalaryActionResult shape) must not change."
---

<objective>
Restyle the shared `pay-setup-forms.tsx` component (`SalaryForm`, `ScheduleForm`, `YtdForm` — used by both the first-run onboarding flow and the settings page) onto Plan 08-01's token system, with special attention to the salary-overwrite confirmation panel: 08-UI-SPEC.md's § Confirmation Panel spec requires the old value struck-through, the new value emphasized (and emerald-accented specifically when it's an increase), both in identical `tabular-nums` formatting — this is the one place in the phase where color depends on a runtime comparison, not a fixed role. Then restyle both consuming pages (`settings/salary/page.tsx`'s salary-history list, `onboarding/page.tsx`'s intro copy) around the now-restyled shared component.

Purpose: UI-03's salary-overwrite confirmation requirement is already functionally satisfied by the existing `pendingConfirmation` panel (08-CONTEXT.md's locked decision) — this plan's job is purely to bring its visual presentation up to the new system without touching `saveSalaryAction`'s confirmation-token protocol.
Output: A restyled `pay-setup-forms.tsx` with the before/after confirmation panel matching 08-UI-SPEC.md exactly, and both `settings/salary/page.tsx` and `onboarding/page.tsx` restyled around it.
</objective>

<execution_context>
@/home/zaiden/.claude/gsd-core/workflows/execute-plan.md
@/home/zaiden/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-CONTEXT.md
@.planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-UI-SPEC.md
@.planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle pay-setup-forms.tsx (SalaryForm's confirmation panel, ScheduleForm, YtdForm)</name>
  <read_first>
    src/components/pay-setup-forms.tsx (file being modified — 403-line file: `SalaryForm`'s `pendingConfirmation` panel at lines 158-174, `ScheduleForm`, `YtdForm` — all three share the same `zinc-*`/`bg-black`/`amber-*`/`red-600`/`green-700` styling idiom; do not touch `submit`/`onConfirmReplace`/`onSkip` logic, `toFormData`/`joinFieldErrors` helpers, or any `SalaryActionResult`/`ScheduleActionResult`/`YtdBaselineActionResult` handling)
    .planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-PATTERNS.md § "Salary Confirmation Panel" (full revised pattern: struck-through old value, conditional-accent new value, accent-filled confirm button), § "Form Fields"
    .planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-UI-SPEC.md § Confirmation Panel (Salary Overwrite), § Copywriting Contract (Destructive confirmation row — locked exact copy: "На {effectiveFrom} уже сохранён оклад {existing} ₽. Он будет безвозвратно заменён на {submitted} ₽." + "Подтвердить и заменить"), § Form Fields
    e2e/auth.spec.ts (confirms `getByLabel("Оклад «грязными», ₽")`, `getByLabel("Дата вступления в силу")`, `getByRole("button", { name: "Сохранить оклад" })`, `getByLabel("День аванса (число месяца)")`, `getByLabel("День зарплаты (число месяца)")`, `getByRole("button", { name: "Сохранить график" })`, `getByRole("button", { name: "Пропустить" })`, `getByText("Оклад сохранён.")`, `getByText("График сохранён.")`, `getByText("Пропущено — доход с начала года считается нулевым до заполнения.")` — every label/button/message text is load-bearing and must not change)
  </read_first>
  <files>src/components/pay-setup-forms.tsx</files>
  <action>
    In `SalaryForm`'s `pendingConfirmation` block: replace the `border-amber-400 bg-amber-50` container with `rounded-[8px] border border-[color:var(--color-tertiary-surface)] bg-[color:var(--color-secondary)] p-4 text-sm`. Restructure the paragraph's interpolated values (keeping the exact locked sentence structure from 08-UI-SPEC.md's Copywriting Contract) so `pendingConfirmation.effectiveFrom` gets `tabular-nums`, `pendingConfirmation.existingAmountRubles` gets wrapped in a `<span className="tabular-nums line-through text-[color:var(--color-text-secondary)]">`, and `pendingConfirmation.submittedAmountRubles` gets wrapped in a `<span>` whose className is computed conditionally: `font-semibold tabular-nums ${submittedAmountRubles > existingAmountRubles ? "text-[color:var(--color-accent)]" : "text-[color:var(--color-text-primary)]"}` (an increase gets the emerald accent per 08-UI-SPEC.md; a decrease or equal amount stays neutral text-primary — do not accent a decrease). The confirm button (`bg-black`) becomes `rounded-[8px] bg-[color:var(--color-accent)] px-4 py-2 text-white font-semibold disabled:opacity-50 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[color:var(--color-accent)]`. Do not add a cancel button — none exists today and 08-UI-SPEC.md confirms this is intentional (editing the form again is the implicit cancel).

    Apply the Form Fields pattern (label `text-[length:var(--font-size-label)] font-[number:var(--font-weight-label)] text-[color:var(--color-text-primary)]`; input `rounded-[8px] border border-[color:var(--color-tertiary-surface)] bg-[color:var(--color-dominant)] px-3 py-2 text-[color:var(--color-text-primary)] focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[color:var(--color-accent)]`; error text `text-[color:var(--color-destructive)]`; helper text `text-[length:var(--font-size-caption)] text-[color:var(--color-text-secondary)]`) identically to every input in `SalaryForm` (`grossRubles`, `effectiveFrom`), `ScheduleForm` (`avansDay`, `salaryDay`), and `YtdForm` (`amountRubles`, `asOfDate`) — do not change any `id`/`htmlFor` value. Every `<h2>` section heading becomes `text-[length:var(--font-size-heading)] font-[number:var(--font-weight-heading)] text-[color:var(--color-text-primary)]`. Success messages (`text-green-700`) become `text-[color:var(--color-accent)]`; `ScheduleForm`'s non-blocking `warning` (`text-amber-700`) becomes `text-[color:var(--color-text-secondary)]` (a warning, not an error — no destructive color, no accent, per 08-UI-SPEC's accent-reservation rule); `YtdForm`'s `isEstimated` notice (`text-amber-700`) becomes `text-[color:var(--color-text-secondary)]` for the same reason. Every submit/skip button follows the same accent-fill (submit) / neutral-border (skip/secondary) treatment established in Plan 08-01/08-02's button patterns, all with focus-visible outlines.
  </action>
  <verify>
    <automated>npx vitest run "src/components/pay-setup-forms.test.ts" && npx tsc --noEmit && npm run test:e2e -- e2e/auth.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run "src/components/pay-setup-forms.test.ts"` passes unmodified
    - `npm run test:e2e -- e2e/auth.spec.ts` passes (exercises SalaryForm/ScheduleForm/YtdForm submission through the full register → onboarding → home flow)
    - `grep -c "zinc-\|amber-\|bg-black\|red-6\|green-7" src/components/pay-setup-forms.tsx` reports 0
    - `grep -n "line-through" src/components/pay-setup-forms.tsx` matches (old value struck-through)
    - `grep -n "submittedAmountRubles > .*existingAmountRubles" src/components/pay-setup-forms.tsx` or an equivalent grep confirms the conditional-accent comparison is present
    - `git status --porcelain -- src/domain/ src/lib/db/ src/app/actions/salary.ts` produces no output
    - `npx tsc --noEmit` reports no new errors
  </acceptance_criteria>
  <done>pay-setup-forms.tsx's three forms are fully token-driven; the salary-overwrite confirmation panel matches 08-UI-SPEC.md's struck-through-old/conditional-accent-new pattern exactly; all input ids, the confirmation-token protocol, and every label/button/message text e2e/auth.spec.ts depends on are unchanged; existing unit test and e2e/auth.spec.ts both pass.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle settings/salary/page.tsx (salary history list) and onboarding/page.tsx</name>
  <read_first>
    src/app/(app)/settings/salary/page.tsx (file being modified — current `zinc-*` composition, salary-history `<ul>` with `border-zinc-100` rows, "Пока нет сохранённых значений оклада." empty-state text for the history list only)
    src/app/(app)/onboarding/page.tsx (file being modified — current `zinc-*` composition, `hasSalaryAndSchedule`-gated "Перейти на главный экран" link)
    .planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-UI-SPEC.md § Typography, § Color, § Copywriting Contract
    e2e/auth.spec.ts (confirms `getByRole("heading", { name: "Настройка выплат" })` on onboarding — do not change this exact heading text)
  </read_first>
  <files>src/app/(app)/settings/salary/page.tsx, src/app/(app)/onboarding/page.tsx</files>
  <action>
    In `settings/salary/page.tsx`: restyle the page `<h1>` to `text-[length:var(--font-size-display)] font-[number:var(--font-weight-display)] text-[color:var(--color-text-primary)]`, the "История окладов" `<h2>` to the heading-role token treatment. Restyle the salary-history list: replace `border-zinc-100` row dividers with `border-[color:var(--color-tertiary-surface)]`, wrap each row's `formatKopecks(row.grossAmountKopecks)` and `row.effectiveFrom` in `tabular-nums`, and restyle the empty-state text ("Пока нет сохранённых значений оклада." — already matches the UI-SPEC's "Пока нет {N}" pattern, no copy change needed) to `text-[color:var(--color-text-secondary)]`. No structural change to the `Promise.all([...])` data-fetch (this page is not part of Plan 08-01's Suspense-loading-state scope — it stays a straightforward server-rendered page, since it is not one of the ROADMAP's five explicitly-named UI-01 screens and 08-UI-SPEC.md's UI Considerations table does not list it separately from `salary-setup-form`, whose loading state is the existing isSubmitting pattern restyled in Task 1).

    In `onboarding/page.tsx`: restyle the `<h1>`/intro `<p>` the same way as every other page's heading treatment in this phase, and the "Перейти на главный экран" link to `text-[color:var(--color-accent)] underline focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[color:var(--color-accent)]`. Do not change the `"Настройка выплат"` heading text (load-bearing for `e2e/auth.spec.ts`) or the `hasSalaryAndSchedule` conditional-render logic.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build && npm run test:e2e -- e2e/auth.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `npm run test:e2e -- e2e/auth.spec.ts` passes
    - `grep -c "zinc-" src/app/(app)/settings/salary/page.tsx src/app/(app)/onboarding/page.tsx` reports 0 across both files
    - `grep -n "Настройка выплат" src/app/(app)/onboarding/page.tsx` still matches unchanged
    - `git status --porcelain -- src/domain/ src/lib/db/` produces no output
  </acceptance_criteria>
  <done>Salary settings page's history list and onboarding page are fully token-driven with tabular-nums money/date formatting; the onboarding heading text and post-setup home link are unchanged in behavior; e2e/auth.spec.ts passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `SalaryForm`'s `pendingConfirmation` panel → user's overwrite decision | The one UI-03 confirmation surface in this phase whose color depends on a runtime data comparison — a mistake here could mislead a user about whether their salary is increasing or decreasing |
| `saveSalaryAction`'s confirmation-token protocol → `pay-setup-forms.tsx`'s `submit`/`onConfirmReplace` | Untouched server-trust boundary; restyle must not alter how the confirmation claim is constructed or sent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-08-11 | Tampering | `SalaryForm`'s conditional accent-color branch | medium | mitigate | Task 1's acceptance criteria grep for the exact `submittedAmountRubles > existingAmountRubles` comparison; a decrease must never render the accent color, which would visually imply a false increase |
| T-08-12 | Tampering | `saveSalaryAction`'s confirmation-token protocol (`src/app/actions/salary.ts`) | critical | mitigate | Task 1's files list never includes `src/app/actions/salary.ts`; `git status --porcelain` structural guard confirms it |
| T-08-13 | Repudiation | Salary overwrite without the confirmation panel rendering correctly | high | mitigate | `e2e/auth.spec.ts` exercises the full salary-save path; the confirmation panel's presence/copy is unchanged from the already-shipped v1.0 behavior, only its styling changes |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new package installs — pure Tailwind arbitrary-value restyle |
</threat_model>

<verification>
- `npx vitest run "src/components/pay-setup-forms.test.ts"` — existing unit test passes unmodified
- `npm run test:e2e -- e2e/auth.spec.ts` — full register→onboarding→salary/schedule/YTD-save→home path passes after both tasks
- `npm run build` — production build succeeds
- `git status --porcelain -- src/domain/ src/lib/db/ src/app/actions/salary.ts` — empty after both tasks
</verification>

<success_criteria>
- SalaryForm/ScheduleForm/YtdForm (shared by onboarding and settings) fully reflect the new token-driven visual system
- The salary-overwrite confirmation panel matches 08-UI-SPEC.md's before/after (struck-through/conditional-accent) spec exactly, satisfying UI-03 without introducing a new modal component
- Zero change to `saveSalaryAction`'s confirmation-token protocol or any other domain/action logic
</success_criteria>

## Artifacts this phase produces

- `src/components/pay-setup-forms.tsx` — modified: token-driven restyle of SalaryForm/ScheduleForm/YtdForm; the confirmation panel now matches 08-UI-SPEC.md's struck-through-old/conditional-accent-new pattern; zero logic change
- `src/app/(app)/settings/salary/page.tsx` — modified: token-driven restyle including tabular-nums salary-history list
- `src/app/(app)/onboarding/page.tsx` — modified: token-driven restyle; heading text and conditional home-link logic unchanged

<output>
Create `.planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-04-SUMMARY.md` when done
</output>
</output>
