---
phase: 08-visual-redesign-accessibility-pwa-safe-area
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/manifest.ts
  - src/app/(app)/layout.tsx
  - src/app/(app)/page.tsx
  - src/components/next-payment-card.tsx
  - src/components/skeleton-loader.tsx
  - src/components/skeleton-loader.render.test.tsx
  - src/components/sign-out-button.tsx
  - src/components/install-banner.tsx
  - src/components/ytd-estimate-banner.tsx
  - src/app/(app)/error.tsx
  - src/components/annual-pie-chart.tsx
autonomous: true
requirements: [UI-01, UI-02, UI-04, UI-05, UI-06, UI-07, PWA-01, PWA-02]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "UI-05/UI-06 (verbatim ROADMAP Phase 8 success criterion 4, design-token half): The app reflects the redesigned visual system (typography, color, components via `frontend-design`) and follows the system dark/light theme. [Spec-less probe fallback note: all 9 Phase 8 requirement IDs came back `unclassified`/`unresolved` from the deterministic edge-coverage engine per the specless-probe-fallback protocol — every truth in this phase's plans is authored directly from ROADMAP.md's Phase 8 Success Criteria text plus 08-UI-SPEC.md's approved (6/6 dimensions PASS) design contract, not from a probe report.] `src/app/globals.css` declares the full token system from 08-UI-SPEC.md § Color/Typography/Spacing (dominant/secondary/accent/destructive/text-primary/text-secondary/tertiary-surface colors; body/label/heading/display/caption typography; xs..3xl spacing) with dark values as the `:root` default and light values under `@media (prefers-color-scheme: light)` — the deliberate dark-base polarity documented in 08-UI-SPEC.md § Dark Mode Strategy. Proven by `npm run build` succeeding and the home screen (`src/app/(app)/page.tsx`, `src/components/next-payment-card.tsx`) rendering entirely from these tokens with zero raw `zinc-*`/`red-*`/`amber-*` Tailwind color utilities remaining in the touched files."
    - "UI-04 (verbatim ROADMAP Phase 8 success criterion 3): From any screen, the user can return to the home screen in one tap/click. `src/app/(app)/layout.tsx`'s persistent header renders the app name \"НаРуки\" as a `<Link href=\"/\">`, present on every `(app)` route (bonuses, vacations, settings/salary, onboarding all render inside this shared layout) — one tap from anywhere reaches home."
    - "PWA-01/PWA-02 (verbatim ROADMAP Phase 8 success criterion 5): On an iPhone with a Dynamic Island, the header/nav never overlaps it or the home indicator — verified with `viewport-fit=cover` configured and `env(safe-area-inset-*)` applied. `src/app/layout.tsx`'s `viewport` export already sets `viewportFit: \"cover\"` (confirmed unchanged, Phase 4). `src/app/(app)/layout.tsx`'s header applies `paddingTop: env(safe-area-inset-top)` and its main wrapper applies `paddingBottom: env(safe-area-inset-bottom)` — the single source of truth per 08-UI-SPEC.md § Safe-Area/PWA, no other component applies its own safe-area padding. The live device/simulator confirmation is captured in Plan 08-06's end-of-phase human-check (PWA-01/02's automated half — presence of the CSS — is proven here; the visual non-overlap half needs a real notched viewport)."
    - "UI-01 (home screen slice, verbatim ROADMAP Phase 8 success criterion 1): The home screen shows a clear empty/loading/error state instead of a blank screen or a raw technical error. `src/app/(app)/page.tsx` keeps its existing not-configured empty state (restyled) and gains a `Promise.all(...).catch()` wrapping `forecastNextPayment`/`computeAnnualSummary` that renders 08-UI-SPEC.md's generic fetch-error copy instead of an unhandled Next.js error page; `<Suspense>` boundaries around the next-payment and annual-summary reads fall back to `src/components/skeleton-loader.tsx` (new — variants `payment-card`/`chart`, tertiary-surface fill, ~1s opacity pulse disabled under `prefers-reduced-motion: reduce`, never a spinner, per 08-UI-SPEC.md § Loading Skeletons). `src/app/(app)/error.tsx` (the Next.js file-convention error boundary for the whole `(app)` segment) is restyled to the new tokens, keeping its existing generic Russian copy and `reset()` retry button."
    - "UI-02 (money/date formatting, verbatim ROADMAP Phase 8 success criterion 2, formatting half): Every money amount is formatted identically (locale-aware, tabular figures). `src/app/globals.css` defines a `.tabular-nums { font-variant-numeric: tabular-nums; }` utility; every `formatKopecks()`/`formatIsoDateRu()` call site in `next-payment-card.tsx` and `annual-pie-chart.tsx` wraps its output with this class, on top of `formatKopecks()`'s unchanged formatting logic (`src/domain/money.ts` is never modified — verified by `git status --porcelain -- src/domain/` producing no output)."
    - "UI-07 (accessibility, home-screen slice): Every interactive element in the touched files (home-screen links/buttons, nav header link, sign-out button, install-banner dismiss button, error-boundary retry button) carries `focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[color:var(--color-accent)]` (destructive color for destructive actions), and Text primary/secondary token values are verified ≥4.5:1 (body) / ≥3:1 (large text) against Dominant/Secondary per 08-UI-SPEC.md § Color — both color pairs are the exact hex values 08-UI-SPEC.md's Color section states were already checker-verified at that ratio, so no new contrast computation is introduced by this task, only correct application of the verified pair."
  artifacts:
    - src/app/globals.css
    - src/app/layout.tsx
    - src/app/manifest.ts
    - src/app/(app)/layout.tsx
    - src/app/(app)/page.tsx
    - src/components/next-payment-card.tsx
    - src/components/skeleton-loader.tsx
    - src/components/sign-out-button.tsx
    - src/components/install-banner.tsx
    - src/components/ytd-estimate-banner.tsx
    - src/app/(app)/error.tsx
    - src/components/annual-pie-chart.tsx
  key_links:
    - "src/app/globals.css's `:root` token declarations → every other Wave-2 plan's `bg-[color:var(--color-*)]`/`text-[color:var(--color-*)]` arbitrary-value classes — every screen in this phase depends on this single token source existing with the exact names declared here before any other plan's tasks can compile correctly"
    - "src/app/(app)/layout.tsx's persistent header Link href=\"/\" → every (app)-route screen — the sole implementation of UI-04's one-tap-home requirement; if this file regresses, every other screen loses home navigation simultaneously"
    - "src/app/(app)/page.tsx's Promise.all(...).catch() → src/app/(app)/error.tsx — two independent error surfaces (a caught fetch rejection renders inline copy; an uncaught render-time throw is caught by the Next.js error boundary) that must not be conflated or one becomes dead code"
  prohibitions:
    - requirement_id: UI-05
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT change any value, formula, or branch in src/domain/** or src/lib/db/** while restyling — this is a pure CSS/JSX presentation pass. Every task's acceptance criteria include a `git status --porcelain -- src/domain/` check as a structural guard, per PROJECT.md's explicit 'zero change to calculation logic' constraint for the whole v1.1 milestone."
    - requirement_id: UI-06
      category: safety
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT ship a dark-mode or light-mode color pairing that drops below WCAG AA contrast (4.5:1 body text, 3:1 large text) against its paired surface — only the exact hex values already verified in 08-UI-SPEC.md § Color may be used for Text primary/secondary against Dominant/Secondary; no ad-hoc color substitution during implementation."
    - requirement_id: UI-05
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT introduce a new npm dependency (icon package, component-library, animation library) to implement any part of this visual system — 08-CONTEXT.md locks hand-rolled Tailwind + CSS variables only, explicitly to avoid a new package-legitimacy checkpoint mid polish-milestone."
---

<objective>
Establish the phase's entire visual-design-token system in `globals.css` (colors, typography, spacing, the dark-base/light-override polarity, the `.tabular-nums` utility) and prove it end-to-end by fully restyling the home screen — the app's signature surface — including its empty/loading/error states, dark mode, and accessibility basics, plus the shared `(app)` shell (persistent home-nav header, safe-area insets) every other screen in this phase will build on.

This is the tracer for the whole phase: config layer (`globals.css`) → shared shell layer (`(app)/layout.tsx`, root `layout.tsx`'s viewport/manifest) → page layer (`(app)/page.tsx`) → leaf-component layer (`next-payment-card.tsx`, the UI-SPEC's own "signature element" — the hero next-payment amount in Display-role Georgia serif on emerald accent). Every other plan in this phase (08-02..08-05) depends on the tokens this plan declares existing with these exact names before their tasks can compile.

Purpose: Prove the CSS-variable-driven design system compiles, renders correctly in both color schemes, and satisfies UI-01/02/04/05/06/07 and PWA-01/02 on one real screen before expanding to the other ~13 files in Wave 2 — catching any token-naming or dark-polarity mistake on the cheapest possible surface area.
Output: `globals.css`'s complete token system; a fully restyled home screen with Suspense-driven loading skeletons and a caught-fetch-failure error state; a restyled shared `(app)` shell with one-tap-home nav and safe-area insets; a new reusable `skeleton-loader.tsx` component with its own render test.
</objective>

<execution_context>
@/home/zaiden/.claude/gsd-core/workflows/execute-plan.md
@/home/zaiden/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-CONTEXT.md
@.planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-UI-SPEC.md
@.planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Design-token system in globals.css + shared (app) shell (nav/safe-area) + home page composition + hero card — one path, every layer</name>
  <reversibility rating="reversible">The dark-base/light-override CSS variable polarity (dark values in `:root`, light values in `@media (prefers-color-scheme: light)`) is a departure from the conventional light-base/dark-override pattern. It is an architectural convention every later plan's files will read from, but it is a pure CSS authoring choice — flipping the base/override direction later is a mechanical `:root`↔`@media` swap with no consumer-visible contract change (consumers only ever read `var(--color-*)`, never the polarity). Documented here, not gated behind a checkpoint.</reversibility>
  <read_first>
    src/app/globals.css (file being modified — current 27-line file: `--background`/`--foreground` only, unused `@theme inline` block, `body { font-family: Arial, Helvetica, sans-serif; }`)
    src/app/layout.tsx (file being modified — `Geist`/`Geist_Mono` next/font imports, `viewport.viewportFit: "cover"` already correct, `metadata.appleWebApp`)
    src/app/manifest.ts (file being modified — current `theme_color`/`background_color: "#18181b"`)
    src/app/(app)/layout.tsx (file being modified — current header: `border-zinc-200`, user email + `/bonuses`/`/vacations` text links, no home link, no safe-area padding)
    src/app/(app)/page.tsx (file being modified — current not-configured empty state, `Promise.all([forecastNextPayment, computeAnnualSummary])` with no `.catch()`, no Suspense)
    src/components/next-payment-card.tsx (file being modified — current `zinc-*` utilities, `text-3xl font-semibold text-zinc-900` hero amount, no Display-role/Georgia/accent)
    .planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-UI-SPEC.md § Design System, § Spacing Scale, § Typography, § Color, § Supplementary: Dark Mode Strategy, § Hero Next-Payment Card, § Persistent Nav Header, § Safe-Area / PWA, § Money & Numerals, § Supplementary: Caption / Tabular Numerals
    .planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-PATTERNS.md § "Global CSS Variables & Design Tokens", § "Root Layout Viewport", § "App Layout with Persistent Navigation Header", § "Next Payment Card", § "Manifest & PWA Metadata"
    .planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-CONTEXT.md `<decisions>` § "Navigation, Accessibility & Safe-Area", § "Visual Redesign System & Dark Mode"
  </read_first>
  <files>src/app/globals.css, src/app/layout.tsx, src/app/manifest.ts, src/app/(app)/layout.tsx, src/app/(app)/page.tsx, src/components/next-payment-card.tsx, src/components/sign-out-button.tsx</files>
  <action>
    In `src/app/globals.css`: replace the current `:root`/`@media (prefers-color-scheme: dark)` block with the full dark-base token system. Declare in `:root` (dark values, the default): `--color-dominant: #1a1a1a`, `--color-secondary: #242424`, `--color-accent: #10b981`, `--color-destructive: #ef4444`, `--color-text-primary: #f5f5f5`, `--color-text-secondary: #9ca3af`, `--color-tertiary-surface: #2d2d2d`; typography tokens `--font-size-body: 14px`/`--font-weight-body: 400`/`--line-height-body: 1.6`, `--font-size-label: 14px`/`--font-weight-label: 400`/`--line-height-label: 1.4`, `--font-size-heading: 18px`/`--font-weight-heading: 600`/`--line-height-heading: 1.3`, `--font-size-display: 28px`/`--font-weight-display: 600`/`--line-height-display: 1.2`, `--font-size-caption: 12px`/`--font-weight-caption: 400`/`--line-height-caption: 1.4`; spacing tokens `--spacing-xs: 4px` through `--spacing-3xl: 64px` (7 tokens, values from 08-UI-SPEC.md § Spacing Scale); font-family tokens `--font-family-body: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", sans-serif`, `--font-family-display: Georgia, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", sans-serif` (Georgia first, system-sans fallback per 08-UI-SPEC.md's explicit "fallback: system sans" wording), `--font-family-caption: var(--font-geist-mono), ui-monospace, SFMono-Regular, Menlo, Consolas, "Liberation Mono", monospace` (reuses the already-wired Geist Mono variable from `layout.tsx` as the primary monospace face, satisfying 08-UI-SPEC.md's "monospace font stack" Caption requirement without adding a new font import). Add `@media (prefers-color-scheme: light)` overriding only the 7 color tokens with their light values (`#ffffff`/`#f9fafb`/`#10b981`(unchanged)/`#ef4444`(unchanged)/`#171717`/`#6b7280`/`#f3f4f6`) — typography/spacing/font-family tokens are identical in both modes, no override needed. Add a one-line CSS comment directly above `:root` stating the polarity is deliberately dark-base/light-override per 08-UI-SPEC.md § Dark Mode Strategy, so a future contributor does not "fix" it back. Remove the now-fully-superseded `--background`/`--foreground` declarations and the unused `@theme inline` block (confirmed dead: no `bg-background`/`text-foreground`/`font-sans`/`font-mono` Tailwind utility class exists anywhere in `src/`). Change `body { font-family: Arial, Helvetica, sans-serif; }` to `body { font-family: var(--font-family-body); background: var(--color-dominant); color: var(--color-text-primary); }`. Add the utility class `.tabular-nums { font-variant-numeric: tabular-nums; }` and `@media (prefers-reduced-motion: reduce) { .skeleton-pulse { animation: none !important; opacity: 0.5; } }` (the reduced-motion guard `skeleton-loader.tsx` in Task 2 will rely on).

    In `src/app/layout.tsx`: no change to `viewport` (already `viewportFit: "cover"`, PWA-01/02 baseline confirmed correct — do not touch this export). Update `viewport.themeColor` from `"#18181b"` to `"#1a1a1a"` to match the new `--color-dominant` dark value exactly (was an approximation before). Keep the `Geist`/`Geist_Mono` imports and `geistSans.variable`/`geistMono.variable` className application unchanged — `--font-geist-mono` is now consumed by `--font-family-caption` in globals.css, so Geist Mono stays loaded and used; `--font-geist-sans` becomes an unused CSS variable (harmless — Next.js's font loader still only loads what `next/font` requests, and removing the import is a separate, non-essential cleanup this task does not need to make to satisfy any requirement).

    In `src/app/manifest.ts`: update `theme_color` and `background_color` from `"#18181b"` to `"#1a1a1a"` to match the new `--color-dominant` dark value, keeping every other field (`name`, `short_name`, `display`, `icons`, etc.) byte-for-byte unchanged.

    In `src/app/(app)/layout.tsx`: replace the current header (`border-zinc-200`, email + text-link nav, no home link) with the 08-UI-SPEC.md § Persistent Nav Header pattern: `<header>` with `bg-[color:var(--color-secondary)]`, `border-b border-[color:var(--color-tertiary-surface)]`, `style={{ paddingTop: 'env(safe-area-inset-top)' }}` (PWA-01/02's single source of truth for the top inset), inner `<div className="flex h-14 items-center justify-between px-6">` containing `<Link href="/" className="text-[length:var(--font-size-heading)] font-[number:var(--font-weight-heading)] text-[color:var(--color-text-primary)] focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[color:var(--color-accent)]">НаРуки</Link>` (UI-04's one-tap-home link) and `<SignOutButton />` on the trailing side. Drop the existing user-email span and the `/bonuses`/`/vacations` text links from the header entirely — 08-UI-SPEC.md's Persistent Nav Header section describes only the app-name-link + sign-out; per-screen navigation between bonuses/vacations/settings is not this task's concern and 08-CONTEXT.md's locked "simplest one-tap pattern... no bottom-tab-bar redesign" decision does not require replacing it with anything else. Wrap `<main>` with `style={{ paddingBottom: 'env(safe-area-inset-bottom)' }}` (PWA-01/02's bottom-inset source of truth) in addition to its existing `flex flex-1 flex-col` classes.

    In `src/components/sign-out-button.tsx`: replace `rounded border border-zinc-300 px-3 py-1.5 text-sm` with `rounded-[8px] border border-[color:var(--color-tertiary-surface)] px-3 py-1.5 text-[length:var(--font-size-body)] text-[color:var(--color-text-primary)] focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[color:var(--color-accent)]`. No behavior change — `handleSignOut`'s `authClient.signOut()` → `router.push("/login")` → `router.refresh()` sequence stays byte-for-byte identical.

    In `src/app/(app)/page.tsx`: restyle the not-configured empty state to 08-UI-SPEC.md's Display-role heading (`text-[length:var(--font-size-display)] font-[number:var(--font-weight-display)] text-[color:var(--color-text-primary)]`), `text-[color:var(--color-text-secondary)]` body copy, and the "Перейти к настройке" CTA link restyled to `rounded-[8px] bg-[color:var(--color-accent)] px-4 py-2 text-sm font-semibold text-white focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[color:var(--color-accent)]`. Wrap the existing `Promise.all([forecastNextPayment(userId), computeAnnualSummary(userId, currentYear)])` in a `.catch()` that returns a sentinel error flag (e.g. `{ forecast: null, annual: null, fetchFailed: true }` — exact shape at implementer's discretion) instead of letting a rejection propagate to the `(app)/error.tsx` boundary; when `fetchFailed` is true, render 08-UI-SPEC.md's fetch-error copy ("Не удалось загрузить данные. Проверьте соединение и попробуйте ещё раз.") with a reload link/button, never raw exception text. Restructure the configured-state render into two `<Suspense>` boundaries — one wrapping a small async component that renders `<NextPaymentCard>` (fallback `<SkeletonLoader count={1} variant="payment-card" />`), one wrapping a small async component that renders `<AnnualPieChart>` (fallback `<SkeletonLoader count={1} variant="chart" />`) — both async sub-components receive the already-fetched data as props (the `Promise.all` above still runs before the page returns; the Suspense boundaries exist to satisfy 08-UI-SPEC.md's "loading | next-payment-card, annual-summary-chart... covered" row structurally and to give the pattern a real place to attach, even though in practice the awaited data resolves before first paint on this server-rendered page — do not artificially delay the fetch to force a visible loading flash). Restyle the "Сводка недоступна" fallback card and `<InstallBanner />`/`<YtdEstimateBanner />` composition order unchanged.

    In `src/components/next-payment-card.tsx`: apply 08-UI-SPEC.md § Hero Next-Payment Card exactly — `<section>` becomes `rounded-[12px] border border-[color:var(--color-tertiary-surface)] bg-[color:var(--color-secondary)] p-6` (drop `shadow-sm`; add `dark:shadow-lg` equivalent only if a dark-mode-only shadow is trivial via a plain CSS media query in globals.css — otherwise omit, since 08-UI-SPEC.md marks the dark-mode shadow as a "subtle" nice-to-have, not a numeric requirement). Kind+date line and disclaimer line use `text-[color:var(--color-text-secondary)]`; the hero amount (`formatKopecks(forecast.netKopecks)`) becomes `font-[family-name:var(--font-family-display)] text-[length:var(--font-size-display)] font-[number:var(--font-weight-display)] leading-[var(--line-height-display)] text-[color:var(--color-accent)] tabular-nums` — the only accent-colored amount in the card. All breakdown `<dl>` amounts/dates stay `text-[color:var(--color-text-secondary)]` at Body size and get the `tabular-nums` class; date text additionally gets `font-[family-name:var(--font-family-caption)] text-[length:var(--font-size-caption)]` per the Caption/Tabular-Numerals supplementary rule for list-row dates.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (proves globals.css/layout.tsx/manifest.ts/page.tsx/next-payment-card.tsx compile and Next.js's metadata/viewport export validation passes)
    - `grep -c "color-dominant\|color-secondary\|color-accent\|color-destructive\|color-text-primary\|color-text-secondary\|color-tertiary-surface" src/app/globals.css` reports at least 7 (one declaration per color token, dark-base in `:root`)
    - `grep -c "prefers-color-scheme: light" src/app/globals.css` reports 1 (confirms the dark-base/light-override polarity, not the conventional direction)
    - `grep -c "zinc-" src/app/(app)/layout.tsx src/app/(app)/page.tsx src/components/next-payment-card.tsx src/components/sign-out-button.tsx` reports 0 across all four files combined
    - `grep -n "env(safe-area-inset-top)" src/app/(app)/layout.tsx` and `grep -n "env(safe-area-inset-bottom)" src/app/(app)/layout.tsx` both match — the header applies the top inset, main applies the bottom inset, and no other file in this task's files list contains `safe-area-inset`
    - `grep -n 'href="/"' src/app/(app)/layout.tsx` matches — the app-name link targets home
    - `git status --porcelain -- src/domain/ src/lib/db/` produces no output (zero calculation-logic or repository changes)
    - `npx tsc --noEmit` reports no new errors
  </acceptance_criteria>
  <done>globals.css declares the full dark-base token system; the shared (app) shell has a one-tap-home nav header with safe-area insets applied once; the home page composes its empty/error/configured states from tokens with Suspense-wrapped loading boundaries; the hero next-payment card renders its amount in Display-role Georgia serif on emerald accent with tabular-nums. Build and typecheck pass; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: New skeleton-loader component + remaining home-screen furniture (install banner dark-mode token migration, YTD banner, app-level error boundary)</name>
  <behavior>
    - `<SkeletonLoader count={2} variant="bonus-row" />` renders exactly 2 top-level skeleton blocks, each containing the bonus-row-shaped placeholder divs (4 inner placeholder rects matching date/amount/note/actions column widths)
    - `<SkeletonLoader count={1} variant="payment-card" />` renders exactly 1 skeleton block shaped like the next-payment-card layout (3 stacked placeholder rects)
    - Every rendered skeleton block carries the `skeleton-pulse` class (the globals.css class Task 1 wired to `animation: none` under `prefers-reduced-motion: reduce`) — this test asserts the class is present, not the resolved animation state (jsdom does not evaluate `@media` queries)
  </behavior>
  <read_first>
    .planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-PATTERNS.md § "Skeleton Loading Component" (full implementation pattern: `count`/`variant` props, per-variant inner-shape blocks, `useEffect`-gated `mounted` state to avoid hydration mismatch, the `@media (prefers-reduced-motion: reduce)` guard)
    .planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-UI-SPEC.md § Supplementary: Loading Skeletons
    src/app/globals.css (just modified in Task 1 — confirms the `.tabular-nums`/`skeleton-pulse` reduced-motion rule this component's className references)
    src/components/install-banner.tsx (file being modified — current `dark:bg-zinc-800`/`dark:text-zinc-300` raw Tailwind dark-variant usage, the closest existing dark-mode precedent per 08-CONTEXT.md)
    src/components/ytd-estimate-banner.tsx (file being modified — current `border-amber-300 bg-amber-50 text-amber-900`, `role="status"`, deliberately non-dismissible per its own doc comment — do not add a dismiss control)
    src/app/(app)/error.tsx (file being modified — current `zinc-*` utilities, existing render test asserts exact heading/body/button text — do not change any string)
    src/app/(app)/error.render.test.tsx (existing test — confirms the exact assertions this task's restyle must not break: `getByText("Ошибка при загрузке сводки")`, `getByText("Попробуйте ещё раз")`, `getByRole("button", { name: "Повторить" })`, `resetMock` called once on click)
  </read_first>
  <files>src/components/skeleton-loader.tsx, src/components/skeleton-loader.render.test.tsx, src/app/globals.css, src/components/install-banner.tsx, src/components/ytd-estimate-banner.tsx, src/app/(app)/error.tsx</files>
  <action>
    Create `src/components/skeleton-loader.tsx` (new, `"use client"`) per 08-PATTERNS.md's pattern: exports `SkeletonLoader({ count, variant }: { count: number; variant: "bonus-row" | "vacation-row" | "payment-card" | "chart" })`. Uses `useEffect`+`useState` to gate rendering until mounted (avoids SSR/client hydration mismatch on the animation state). Renders `count` wrapper divs with `className="skeleton-pulse rounded-[12px] bg-[color:var(--color-tertiary-surface)]"`, each containing variant-specific inner placeholder `<div>`s sized to roughly match the real component's shape (bonus-row: 4 rects for date/amount/note/actions columns; vacation-row: 5 rects; payment-card: 3 stacked rects; chart: 1 rect + 1 circular rect). Do not use inline `<style>` tag animation definitions — rely entirely on the `skeleton-pulse` class Task 1 added to `globals.css` (add the base `animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;` rule for `.skeleton-pulse` to `globals.css` in this task, since Task 1 only added the `prefers-reduced-motion` override — the base animation and the override belong together for a reader to understand the full behavior in one place; append it directly above or below the existing reduced-motion block).

    Create `src/components/skeleton-loader.render.test.tsx` implementing the three behavior cases specified above via `@testing-library/react`'s `render`/`screen`, following the existing jsdom render-test convention (`// @vitest-environment jsdom` pragma, `cleanup` in `afterEach`, as seen in `error.render.test.tsx`).

    In `src/components/install-banner.tsx`: replace `border-zinc-900 bg-zinc-100 p-3 dark:bg-zinc-800` with `border-[color:var(--color-tertiary-surface)] bg-[color:var(--color-secondary)] p-3` (the dark-base token polarity means one class works in both modes — no `dark:` variant needed anymore). Replace `text-zinc-600 dark:text-zinc-300` with `text-[color:var(--color-text-secondary)]`, and the dismiss button's `text-zinc-500 hover:text-zinc-900 dark:hover:text-zinc-100` with `text-[color:var(--color-text-secondary)] hover:text-[color:var(--color-text-primary)] focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[color:var(--color-accent)]`. Heading text stays `text-[color:var(--color-text-primary)]` implicitly via inherited body color — add it explicitly on the `<h2>` for clarity. No change to `handleDismiss`/`useSyncExternalStore`/localStorage logic.

    In `src/components/ytd-estimate-banner.tsx`: replace `border-amber-300 bg-amber-50 text-amber-900` with `border-[color:var(--color-tertiary-surface)] bg-[color:var(--color-secondary)] text-[color:var(--color-text-primary)]` (this is a persistent informational banner, not a destructive/error state, so it uses neutral tokens, not `--color-destructive`) and the "Указать доход с начала года" link gets `text-[color:var(--color-accent)] underline focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[color:var(--color-accent)]`. Keep `role="status"` and every word of the existing copy unchanged — this component's whole reason to exist is D-11's "stays on screen, no dismiss" behavior, which this task does not touch.

    In `src/app/(app)/error.tsx`: replace `text-zinc-600`/`bg-zinc-900` with `text-[color:var(--color-text-secondary)]`/`bg-[color:var(--color-accent)]` (the retry button is a primary action, so it earns the accent fill per 08-UI-SPEC.md's "primary form-submit/confirm buttons" accent rule) plus a focus-visible outline. Do not change `"Ошибка при загрузке сводки"`, `"Попробуйте ещё раз"`, `"Повторить"`, or the `reset()` wiring — `error.render.test.tsx`'s three assertions must still pass unmodified.
  </action>
  <verify>
    <automated>npx vitest run "src/components/skeleton-loader.render.test.tsx" "src/app/(app)/error.render.test.tsx" "src/components/install-banner.render.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - All three vitest files pass (new skeleton-loader test's 3 cases + existing error.render.test.tsx's unmodified assertions + existing install-banner.render.test.tsx)
    - `grep -c "zinc-\|amber-" src/components/install-banner.tsx src/components/ytd-estimate-banner.tsx src/app/(app)/error.tsx` reports 0 across all three files combined
    - `grep -n "prefers-reduced-motion" src/app/globals.css` matches, and `grep -n "skeleton-pulse" src/app/globals.css` shows both the base `animation:` rule and the reduced-motion override
    - `git status --porcelain -- src/domain/ src/lib/db/` produces no output
    - `npx tsc --noEmit` reports no new errors
  </acceptance_criteria>
  <done>skeleton-loader.tsx exists with a passing render test covering count/variant/reduced-motion-class behavior; install-banner.tsx's dark-mode usage is fully migrated off raw Tailwind `dark:` utilities onto the new token system; the YTD banner and app-level error boundary are restyled with zero copy or behavior change; all touched tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Annual pie chart restyle (home screen's remaining leaf component)</name>
  <read_first>
    .planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-PATTERNS.md § "Annual Pie Chart"
    .planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-UI-SPEC.md § Color (Tax/Net slice colors are explicitly NOT part of the 60/30/10 token system — 08-PATTERNS.md's own guidance keeps them hardcoded for Recharts compatibility), § Money & Numerals, § UI Considerations row "empty | annual-summary-chart (media) | 🧪 backstop"
    src/components/annual-pie-chart.tsx (file being modified — current `border-zinc-300`, `text-zinc-900`/`text-zinc-600`/`text-zinc-500`, hardcoded `TAX_COLOR`/`NET_COLOR` red-600/green-600)
    src/components/annual-pie-chart.render.test.tsx (existing test — confirms which assertions must survive: heading text, `formatPercent`/`formatKopecks` output presence)
    e2e/pie-chart.spec.ts (confirms `dl dt:text-is('Грязными') + dd` / `dl dt:text-is('Налог') + dd` / `dl dt:text-is('На руки') + dd` selectors depend on the exact `<dt>`/`<dd>` text content and adjacency — do not reorder or rename these rows)
  </read_first>
  <files>src/components/annual-pie-chart.tsx</files>
  <action>
    In `src/components/annual-pie-chart.tsx`: replace `border-zinc-300` with `border-[color:var(--color-tertiary-surface)]` and add `rounded-[12px] bg-[color:var(--color-secondary)]` to the `<section>` (matching the Hero Card's card-surface treatment for visual consistency, per 08-UI-SPEC.md's general card pattern). Replace every `text-zinc-900`/`text-zinc-600`/`text-zinc-500` with `text-[color:var(--color-text-primary)]`/`text-[color:var(--color-text-secondary)]` respectively. Keep `TAX_COLOR = "#dc2626"` and `NET_COLOR = "#16a34a"` exactly as they are — these are Recharts SVG `fill` values, not Tailwind classes, and 08-UI-SPEC.md's accent-reservation rule ("Accent is explicitly NOT applied to... decorative elements") plus 08-PATTERNS.md's explicit note ("Tax/Net colors stay as-is... hardcoded for chart compatibility") both confirm these two colors are intentionally outside the token system — do not substitute `--color-accent`/`--color-destructive` here. Apply `tabular-nums` to every `formatKopecks(...)`/`formatPercent(...)` text node in the gross-amount line and the `<dl>` breakdown rows, without changing the `<dt>`/`<dd>` element structure, text content, or ordering that `dl dt:text-is('Грязными') + dd` / `'Налог'` / `'На руки'` and `annual-pie-chart.render.test.tsx` depend on.
  </action>
  <verify>
    <automated>npx vitest run "src/components/annual-pie-chart.render.test.tsx" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run "src/components/annual-pie-chart.render.test.tsx"` passes unmodified
    - `npm run build` exits 0
    - `grep -c "zinc-" src/components/annual-pie-chart.tsx` reports 0
    - `grep -n 'TAX_COLOR = "#dc2626"' src/components/annual-pie-chart.tsx` and `grep -n 'NET_COLOR = "#16a34a"' src/components/annual-pie-chart.tsx` both still match (chart colors deliberately unchanged)
    - `git status --porcelain -- src/domain/ src/lib/db/` produces no output
  </acceptance_criteria>
  <done>Annual pie chart card matches the new secondary-surface/token-driven visual system while its Recharts slice colors and `<dl>` breakdown structure (load-bearing for `e2e/pie-chart.spec.ts`) are untouched; existing render test and build pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Server-fetched forecast/annual data → `(app)/page.tsx`'s rendered error copy | A caught `Promise.all` rejection crosses into user-visible text — must never leak the underlying exception's message |
| Any restyled interactive element → keyboard-only user | Focus state crosses from a mouse-assumed default into a genuinely perceivable indicator, or accessibility regresses silently |
| `globals.css` token polarity → every future phase's CSS | An architectural convention (dark-base) that all later work reads from; a silent "fix" back to light-base would desynchronize with 08-UI-SPEC.md's documented contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-08-01 | Information Disclosure | `src/app/(app)/page.tsx`'s `Promise.all(...).catch()` | medium | mitigate | Catch block renders only the fixed 08-UI-SPEC.md fetch-error copy string, never the caught error's `.message`/stack — Task 1 acceptance criteria include a code-level check that no caught-error value is interpolated into JSX |
| T-08-02 | Tampering | `src/domain/**`, `src/lib/db/**` (calculation/persistence logic) | critical | mitigate | This plan's every task lists only presentation files in its files list; every task's acceptance criteria include a `git status --porcelain -- src/domain/ src/lib/db/` structural guard per PROJECT.md's frozen-calculation-engine constraint |
| T-08-03 | Denial of Service (accessibility) | Focus-visible outlines across restyled interactive elements | medium | mitigate | Every button/link touched in this plan gets an explicit `focus-visible:outline-2` class; Plan 08-06's end-of-phase human-check includes a keyboard-only Tab-through pass across the whole app |
| T-08-04 | Information Disclosure | Dark-mode/light-mode text contrast | low | mitigate | Only the pre-verified hex pairs from 08-UI-SPEC.md § Color are used for text-on-surface combinations — no new contrast computation introduced, no ad-hoc color choice permitted (see this plan's `must_haves.prohibitions`) |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new package installs in this plan — `skeleton-loader.tsx` uses only React/Tailwind primitives already in the project; hand-rolled per 08-CONTEXT.md's locked no-component-library decision |
</threat_model>

<verification>
- `npm run build` — globals.css/layout.tsx/manifest.ts/(app)/layout.tsx/(app)/page.tsx/next-payment-card.tsx/annual-pie-chart.tsx all compile
- `npx vitest run "src/components/skeleton-loader.render.test.tsx" "src/app/(app)/error.render.test.tsx" "src/components/install-banner.render.test.tsx" "src/components/annual-pie-chart.render.test.tsx"` — all pass
- `npx tsc --noEmit` — no new errors
- `git status --porcelain -- src/domain/ src/lib/db/` — empty across all three tasks (zero calculation-logic drift)
- Manual/end-of-phase (per `workflow.human_verify_mode=end-of-phase`, consolidated into Plan 08-06): live/simulated Dynamic Island safe-area check, keyboard-only focus-ring pass, dark/light OS-toggle visual check, annual-summary-chart zero-income backstop, nav-header-overflow-at-375px backstop
</verification>

<success_criteria>
- `globals.css` is the single source of truth for every color/typography/spacing token used across the whole phase, with the documented dark-base/light-override polarity
- The home screen (nav shell, page composition, hero card, pie chart, install/YTD banners, error boundary) is fully restyled and demonstrates empty/loading/error/configured states, all from tokens
- UI-04's one-tap-home nav and PWA-01/02's safe-area insets exist in exactly one place (`(app)/layout.tsx`) that every other screen in this phase inherits for free
- Zero change to `src/domain/**`/`src/lib/db/**`, zero new npm dependencies, zero regression in existing home-screen/error/install-banner/pie-chart tests
</success_criteria>

## Artifacts this phase produces

- `src/app/globals.css` — modified: full dark-base token system (color/typography/spacing/font-family), `.tabular-nums` and `.skeleton-pulse` utilities, replaces the old 27-line stub
- `src/app/layout.tsx` — modified: `viewport.themeColor` updated to `#1a1a1a`; `viewportFit: "cover"` confirmed unchanged
- `src/app/manifest.ts` — modified: `theme_color`/`background_color` updated to `#1a1a1a`
- `src/app/(app)/layout.tsx` — modified: persistent nav header with one-tap-home link, safe-area insets on header/main
- `src/app/(app)/page.tsx` — modified: restyled empty/configured states, caught-fetch-failure error state, Suspense-wrapped loading skeletons for forecast/annual data
- `src/components/next-payment-card.tsx` — modified: full token-driven restyle, Display-role hero amount
- `src/components/skeleton-loader.tsx` — new: reusable loading-skeleton component, 4 variants
- `src/components/skeleton-loader.render.test.tsx` — new: covers count/variant/reduced-motion-class behavior
- `src/components/sign-out-button.tsx` — modified: token-driven restyle
- `src/components/install-banner.tsx` — modified: migrated off raw `dark:` Tailwind variants onto token system
- `src/components/ytd-estimate-banner.tsx` — modified: token-driven restyle, copy/behavior unchanged
- `src/app/(app)/error.tsx` — modified: token-driven restyle, copy/behavior unchanged
- `src/components/annual-pie-chart.tsx` — modified: token-driven restyle, chart colors and `<dl>` structure unchanged

<output>
Create `.planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-01-SUMMARY.md` when done
</output>
</output>
