---
phase: 01-core-payroll-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - .gitignore
  - .env.local
  - .env.example
  - README.md
  - src/env.ts
  - drizzle.config.ts
  - vitest.config.ts
  - src/lib/db/index.ts
  - src/lib/db/schema.ts
  - src/lib/auth.ts
  - src/lib/db/auth-schema.ts
autonomous: false
requirements: [AUTH-01, AUTH-02, SAL-01, SAL-02, SAL-03]
user_setup:
  - service: neon
    why: "Postgres database — the single source of truth that makes AUTH-02 cross-device sync work. Nothing DB-backed can run without it."
    env_vars:
      - name: DATABASE_URL
        source: "Neon Console -> your project -> Connection Details -> pooled connection string (includes sslmode=require)"
    dashboard_config:
      - task: "Create a Neon project and database for НаРуки"
        location: "https://console.neon.tech -> New Project"

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`npm run build` completes successfully from the repo root"
    - "`npx tsc --noEmit` reports zero type errors"
    - "Salary and all money amounts are stored as `bigint` kopecks (Drizzle `bigint({mode:'number'})`), not `integer`, to avoid 32-bit overflow at ~21.47M rub — well within this app's own top NDFL bracket (20M-50M+ rub)."
    - "Every app-owned table carries a `user_id` column referencing the Better Auth `user` table with ON DELETE CASCADE, so ownership is enforceable at the schema level"
    - "`salary_history` has a unique constraint on `(user_id, effective_from)`, so an exact-date backdated collision is a single-row replace rather than a silent duplicate (D-14)"
    - "The app refuses to boot when DATABASE_URL or BETTER_AUTH_SECRET is missing, rather than starting and failing at first query"
    - "The Better Auth config enables email+password with email verification disabled (D-05, D-06) and a 30-day rolling session (D-07)"
  artifacts:
    - path: "src/env.ts"
      provides: "Boot-time validated environment variables"
      exports: ["env"]
      contains: "DATABASE_URL"
    - path: "src/lib/db/index.ts"
      provides: "Drizzle client bound to the Neon HTTP driver"
      exports: ["db"]
    - path: "src/lib/db/schema.ts"
      provides: "App-owned tables: salary_history, payment_schedule, ytd_baseline"
      exports: ["salaryHistory", "paymentSchedule", "ytdBaseline"]
      contains: "bigint"
      min_lines: 40
    - path: "src/lib/auth.ts"
      provides: "Better Auth server config (email+password, 30-day session)"
      exports: ["auth"]
    - path: "src/lib/db/auth-schema.ts"
      provides: "Better Auth Drizzle tables generated by the auth CLI"
      exports: ["user", "session", "account", "verification"]
    - path: "drizzle.config.ts"
      provides: "drizzle-kit config pointing at both schema files"
      contains: "auth-schema"
    - path: "vitest.config.ts"
      provides: "Vitest runner config with the @/* path alias"
  key_links:
    - from: "src/lib/auth.ts"
      to: "src/lib/db/index.ts"
      via: "drizzleAdapter(db, { provider: 'pg' }) — Better Auth persists into our own Postgres"
      pattern: "drizzleAdapter\\(\\s*db"
    - from: "src/lib/db/index.ts"
      to: "src/env.ts"
      via: "neon(env.DATABASE_URL) — connection string comes from the validated env module, never process.env directly"
      pattern: "env\\.DATABASE_URL"
    - from: "src/lib/db/schema.ts"
      to: "src/lib/db/auth-schema.ts"
      via: "user_id foreign keys reference the generated Better Auth user table"
      pattern: "user\\.id"
---

<objective>
Scaffold the НаРуки application shell and its Neon-backed Drizzle data model: a Next.js 16 App Router project with pinned TypeScript 6.0.3, a boot-validated environment module, the three app-owned tables (`salary_history`, `payment_schedule`, `ytd_baseline`), and the Better Auth server config plus its generated table schema (D-01, D-05, D-06, D-07, D-14).

Purpose: this is the foundation of the Walking Skeleton. Nothing in Phase 1 — auth (AUTH-01/AUTH-02), salary entry (SAL-01/SAL-02/SAL-03), or the forecast (HOME-01) — can exist until the project, the database client, and the schema shape are correct. Getting the money-column type and the ownership foreign keys right here is what prevents a silent 32-bit overflow and an IDOR-shaped data model later.

Output: a buildable Next.js project with `src/env.ts`, `src/lib/db/index.ts`, `src/lib/db/schema.ts`, `src/lib/auth.ts`, `src/lib/db/auth-schema.ts`, `drizzle.config.ts`, and `vitest.config.ts`.
</objective>

<execution_context>
@/home/zaiden/.claude/gsd-core/workflows/execute-plan.md
@/home/zaiden/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-payroll-loop/01-CONTEXT.md
@.planning/phases/01-core-payroll-loop/01-RESEARCH.md
@.planning/phases/01-core-payroll-loop/SKELETON.md
@.claude/CLAUDE.md
</context>

## Phase Goal

Sourced verbatim from `.planning/ROADMAP.md` § Phase 1:

> A registered user can enter their gross salary and avans/salary payment schedule and see an accurate amount and date for their next take-home payment, computed via the progressive 2025 НДФЛ scale applied cumulatively from the start of the calendar year — with data synced across their devices.

**Note (MVP mode):** the ROADMAP `**Goal:**` line is not in canonical `As a … I want to … so that …` user-story form. It is reproduced verbatim above rather than invented into story form. Run `/gsd-mvp-phase 1` if canonical story framing is wanted in ROADMAP.md.

## Artifacts this phase produces (Plan 01 scope)

New symbols and files created by this plan — none exist in the repo before it runs:

| Symbol / path | Kind |
|---|---|
| `src/env.ts` → `env` | module + export |
| `src/lib/db/index.ts` → `db` | module + export |
| `src/lib/db/schema.ts` → `salaryHistory`, `paymentSchedule`, `ytdBaseline` | Drizzle tables |
| `src/lib/db/auth-schema.ts` → `user`, `session`, `account`, `verification` | generated Drizzle tables |
| `src/lib/auth.ts` → `auth` | Better Auth server instance |
| `drizzle.config.ts`, `vitest.config.ts` | tool configs |
| Columns: `salary_history.gross_amount_kopecks`, `salary_history.effective_from`, `payment_schedule.avans_day`, `payment_schedule.salary_day`, `ytd_baseline.amount_kopecks`, `ytd_baseline.as_of_date`, `ytd_baseline.is_estimated` | schema surface |
| Index `salary_history_user_effective_from_uq` | unique constraint |
| npm scripts `db:push`, `db:studio`, `test` | package.json scripts |

## Flagged assumptions (spec-less probe — unresolved rows)

The deterministic edge probe left these rows `unclassified` for this phase. Surfaced here rather than silently dropped; neither is resolved by this plan.

- **AUTH-02 (unclassified):** no edge category was assigned to "data syncs across devices via cloud". The working assumption this schema encodes: sync is achieved solely by a single per-user row set in Postgres, with no client-side cache and no conflict-resolution layer — last write wins, because exactly one human owns the account. If a genuine multi-writer conflict requirement exists, it is unplanned.
- **SAL-01 (unclassified):** no edge category assigned. Repeated in Plan 01-04, which owns SAL-01's input surface.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify package legitimacy before the first install</name>
  <what-built>Nothing yet — this gate runs BEFORE any package-manager install. `01-RESEARCH.md` § Package Legitimacy Audit flagged four packages SUS on publish-recency alone: `better-auth` (1.7.2), `@better-auth/drizzle-adapter` (1.7.2), `auth` (the Better Auth CLI), and `date-holidays` (3.36.0). The researcher cross-checked download counts and repo provenance and approved all four, but protocol requires a human to confirm before install.</what-built>
  <how-to-verify>
    Open each registry page and confirm the package is the real, maintained project — not a typosquat or a hallucinated name:
    1. https://www.npmjs.com/package/better-auth — repository should be github.com/better-auth/better-auth, weekly downloads in the millions.
    2. https://www.npmjs.com/package/@better-auth/drizzle-adapter — same org/repo. Recent split-out; `drizzleAdapter` is no longer exported from the `better-auth` root package.
    3. https://www.npmjs.com/package/auth — same org/repo (github.com/better-auth/better-auth). Replacement for the older, registry-deprecated Better Auth CLI package.
    4. https://www.npmjs.com/package/date-holidays — repository should be github.com/commenthol/date-holidays, package created 2016.
    Confirm none of the four shows a "deprecated" banner.
  </how-to-verify>
  <resume-signal>Type "approved" to allow the installs, or name any package you want swapped or removed</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold Next.js 16, pin the toolchain, validate env, wire the Neon Drizzle client</name>
  <precondition>A Neon project exists and its pooled connection string is available to write into .env.local as DATABASE_URL (user_setup step 1 complete)</precondition>
  <files>package.json, tsconfig.json, .gitignore, .env.local, .env.example, README.md, src/env.ts, drizzle.config.ts, vitest.config.ts, src/lib/db/index.ts</files>
  <read_first>
    - `.planning/phases/01-core-payroll-loop/01-RESEARCH.md` § "Walking Skeleton Scaffolding Order" steps 1-4 and 10, and § "Neon + Drizzle client (Node.js runtime, HTTP driver)" — the exact driver pairing to use
    - `.planning/phases/01-core-payroll-loop/SKELETON.md` § "Architectural Decisions" — framework, language pin, directory layout
    - `.claude/CLAUDE.md` § "Technology Stack" — the locked version table and the "What NOT to Use" rows
    - The repo root listing (`ls -a`) — it already contains `.planning/`, `.claude/`, `.git/`; there is no `package.json` yet
  </read_first>
  <action>
    The repo root is non-empty, and `create-next-app` refuses to scaffold into a directory containing unrecognised entries such as `.planning/`. Scaffold into a temp subdirectory and move the result up.

    1. Run `npx create-next-app@16 .gsd-scaffold --typescript --eslint --app --src-dir --tailwind --turbopack --import-alias "@/*" --no-git --use-npm --yes`.
    2. Move every entry of `.gsd-scaffold/` into the repo root — including dotfiles, so `.gitignore` is not silently skipped — then remove `.gsd-scaffold/`.
    3. Confirm the moved `.gitignore` covers `node_modules`, `.next`, and the pattern that ignores local env files; append any missing line.

    Install the dependency set from RESEARCH § Walking Skeleton step 2. Runtime: `drizzle-orm@0.45.2`, `@neondatabase/serverless`, `drizzle-zod`, `zod`, `date-fns`, `date-holidays`, `better-auth@1.7.2`, `@better-auth/drizzle-adapter@1.7.2`, `react-hook-form`, `@hookform/resolvers`, `@t3-oss/env-nextjs`. Dev: `drizzle-kit@0.31.10`, `typescript@6.0.3`, `vitest`. Pin TypeScript to the exact string `6.0.3` — a caret range drifts to 7.x, which breaks `typescript-eslint`. Do not install the older, registry-deprecated Better Auth CLI package; schema generation runs through `npx auth generate` in Task 3.

    Create `src/env.ts` using `createEnv` from `@t3-oss/env-nextjs`, declaring server vars `DATABASE_URL` (`z.string().url()`), `BETTER_AUTH_SECRET` (`z.string().min(32)`), `BETTER_AUTH_URL` (`z.string().url()`), with `runtimeEnv` wired to the matching `process.env` keys. Export `env`.

    Create `.env.local` holding `DATABASE_URL` (the Neon pooled string), `BETTER_AUTH_SECRET` (generate with `openssl rand -base64 32`), and `BETTER_AUTH_URL=http://localhost:3000`. Create `.env.example` with the same three keys and placeholder values; commit only `.env.example`.

    Create `src/lib/db/index.ts`: import `neon` from `@neondatabase/serverless`, `drizzle` from `drizzle-orm/neon-http`, and `env` from `@/env`; export `const db = drizzle({ client: neon(env.DATABASE_URL) })`. Resolve the connection string only through `env` — this module must not read the raw environment object directly.

    Create `drizzle.config.ts` with `dialect: "postgresql"`, `schema: ["./src/lib/db/schema.ts", "./src/lib/db/auth-schema.ts"]`, `out: "./drizzle"`, and `dbCredentials` reading the connection string from the raw environment (drizzle-kit runs outside Next.js and cannot import `@/env`).

    Create `vitest.config.ts` with `test.environment: "node"`, `test.include: ["src/**/*.test.ts"]`, and a `resolve.alias` mapping `@` to `./src` so domain tests share the app's import alias.

    Add npm scripts to `package.json`: `db:push` running `drizzle-kit push`, `db:studio` running `drizzle-kit studio`, `test` running `vitest run`.

    Add a `## Running locally` section to `README.md`: copy `.env.example` to `.env.local`, fill the three vars, `npm install`, `npm run db:push`, `npm run dev`. This is the Walking Skeleton's documented full-stack run command.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npm run build &amp;&amp; node -e "const d={...require('./package.json').dependencies,...require('./package.json').devDependencies};for(const k of ['drizzle-orm','@neondatabase/serverless','better-auth','@better-auth/drizzle-adapter','date-holidays','zod','date-fns','drizzle-kit','vitest','@t3-oss/env-nextjs'])if(!d[k])throw new Error('missing dep '+k);if(d.typescript!=='6.0.3')throw new Error('typescript must be pinned to 6.0.3, got '+d.typescript);if(Object.keys(d).some(k=>k.startsWith('@better-auth/cl')))throw new Error('deprecated Better Auth CLI package present');console.log('deps ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` exists at the repo root and `.gsd-scaffold/` no longer exists
    - `package.json` `devDependencies.typescript` is the exact string `6.0.3` (no caret, no tilde)
    - `package.json` contains scripts `db:push`, `db:studio`, `test`
    - No dependency key in `package.json` names the deprecated Better Auth CLI package (asserted by the dep-check command above)
    - `src/env.ts` exports `env`; `grep -c DATABASE_URL src/env.ts`, `grep -c BETTER_AUTH_SECRET src/env.ts`, and `grep -c BETTER_AUTH_URL src/env.ts` each return at least 1
    - `src/lib/db/index.ts` exports `db` and references `env.DATABASE_URL`; `grep -v '^\s*//' src/lib/db/index.ts | grep -c 'process\.env'` returns 0
    - `drizzle.config.ts` lists both `./src/lib/db/schema.ts` and `./src/lib/db/auth-schema.ts` in its `schema` array
    - `vitest.config.ts` maps the `@` alias to `./src`
    - `git check-ignore -q .env.local` exits 0 (the local env file is ignored, not staged)
    - `npx tsc --noEmit` exits 0 and `npm run build` exits 0
  </acceptance_criteria>
  <done>A Next.js 16 App Router project builds cleanly at the repo root with TypeScript pinned to 6.0.3, all Phase 1 dependencies installed, environment variables validated at boot, and a Drizzle client wired to Neon.</done>
</task>

<task type="auto">
  <name>Task 3: Define the app-owned Drizzle schema and Better Auth config, then generate the auth tables</name>
  <reversibility rating="costly">D-01 fixes the payment schedule to day-of-month integers; moving to an exact-recurring-date model later means altering `payment_schedule` and reworking every candidate-date generator that reads it.</reversibility>
  <files>src/lib/db/schema.ts, src/lib/auth.ts, src/lib/db/auth-schema.ts</files>
  <read_first>
    - `src/lib/db/index.ts` and `src/env.ts` (created in Task 2) — the `db` export and env keys this task builds on
    - `.planning/phases/01-core-payroll-loop/01-RESEARCH.md` § "Walking Skeleton Scaffolding Order" steps 5-7, § "YTD Baseline Pattern (SAL-03)", § "Anti-Patterns to Avoid" (the 32-bit money-column pitfall), § "Better Auth config (email+password, 30-day session)", and § "Common Pitfalls" (the deprecated-CLI pitfall)
    - `.planning/phases/01-core-payroll-loop/01-CONTEXT.md` § Implementation Decisions — D-01, D-05, D-06, D-07, D-08, D-11, D-14
  </read_first>
  <action>
    Create `src/lib/db/schema.ts` with three tables, importing `user` from `./auth-schema` for the foreign keys (the import resolves once this task's generation step runs; write it now).

    `salaryHistory` maps table `salary_history`: `id` uuid primary key with `defaultRandom()`; `userId` text column `user_id`, not null, referencing `user.id` with `onDelete: "cascade"`; `grossAmountKopecks` as `bigint({ mode: "number" })` column `gross_amount_kopecks`, not null; `effectiveFrom` as `date({ mode: "string" })` column `effective_from`, not null; `createdAt` timestamp `created_at` with `defaultNow()`. Add a unique index named `salary_history_user_effective_from_uq` over `(userId, effectiveFrom)` — this is what makes D-14's exact-date collision a single-row replace instead of a duplicate. Money columns are `bigint({ mode: "number" })`: kopecks for a 50M rub cumulative income exceed the 32-bit ceiling of about 21.47M rub, which sits inside this app's own top tax bracket.

    `paymentSchedule` maps table `payment_schedule`: `userId` text `user_id` as primary key, referencing `user.id` with `onDelete: "cascade"`; `avansDay` integer `avans_day`, not null; `salaryDay` integer `salary_day`, not null; `updatedAt` timestamp `updated_at` with `defaultNow()`. One current row per user — D-01 stores day-of-month numbers and no schedule history is retained, since REQUIREMENTS.md asks for salary history (SAL-02) only. Add Postgres check constraints keeping `avans_day` and `salary_day` within the inclusive range 1 to 31.

    `ytdBaseline` maps table `ytd_baseline`: `userId` text `user_id` as primary key, referencing `user.id` with `onDelete: "cascade"`; `amountKopecks` as `bigint({ mode: "number" })` column `amount_kopecks`, not null, default 0; `asOfDate` as `date({ mode: "string" })` column `as_of_date`, not null; `isEstimated` boolean `is_estimated`, not null, default true; `updatedAt` timestamp `updated_at` with `defaultNow()`. The `is_estimated` flag is what drives D-11's persistent "forecast assumes zero income since Jan 1" banner built in Plan 01-05.

    Create `src/lib/auth.ts` exporting `auth = betterAuth({ ... })`: `database` set to `drizzleAdapter(db, { provider: "pg" })`, importing `drizzleAdapter` from `@better-auth/drizzle-adapter` (it is not exported from the `better-auth` root package); `emailAndPassword` with `enabled: true` and `requireEmailVerification: false` per D-05 and D-06; `session` with `expiresIn` of 60*60*24*30 seconds and `updateAge` of 60*60*24*7 seconds per D-07's 30-day refresh-on-use sessions; `secret` from `env.BETTER_AUTH_SECRET`; `baseURL` from `env.BETTER_AUTH_URL`. Configure no social/OAuth provider (D-05 defers those) and no password-reset flow (D-08 defers it).

    Then run `npx auth generate --output src/lib/db/auth-schema.ts` — the `auth` package approved in Task 1 — to emit the `user`, `session`, `account`, and `verification` Drizzle tables from that config. If the CLI writes to a different default path, move the file to `src/lib/db/auth-schema.ts` and fix the import in `schema.ts`. Do not hand-write those four tables; they must stay generated so a Better Auth upgrade can regenerate them.

    Run no migration or push command in this task — the live-database push is Plan 01-02's dedicated blocking task.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; node -e "const fs=require('fs');const s=fs.readFileSync('src/lib/db/schema.ts','utf8').split('\n').filter(l=>!/^\s*(\/\/|\*|\/\*)/.test(l)).join('\n');const a=fs.readFileSync('src/lib/db/auth-schema.ts','utf8');['salary_history','payment_schedule','ytd_baseline'].forEach(t=>{if(!s.includes(t))throw new Error('missing table '+t)});const big=(s.match(/bigint\(/g)||[]).length;if(big<2)throw new Error('expected at least 2 bigint money columns, got '+big);if(!s.includes('salary_history_user_effective_from_uq'))throw new Error('missing unique index');const casc=(s.match(/cascade/g)||[]).length;if(casc<3)throw new Error('expected 3 cascade FKs, got '+casc);['user','session','account','verification'].forEach(t=>{if(!new RegExp('\\\\b'+t+'\\\\b').test(a))throw new Error('auth-schema missing '+t)});const au=fs.readFileSync('src/lib/auth.ts','utf8');if(!/requireEmailVerification:\s*false/.test(au))throw new Error('D-06 requireEmailVerification must be false');if(!/60\s*\*\s*60\s*\*\s*24\s*\*\s*30/.test(au))throw new Error('D-07 30-day expiresIn not found');console.log('schema+auth ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/db/schema.ts` exports `salaryHistory`, `paymentSchedule`, and `ytdBaseline`
    - `src/lib/db/schema.ts` declares at least two `bigint(` money columns (`gross_amount_kopecks`, `amount_kopecks`) and zero money columns typed as a 32-bit int column
    - `src/lib/db/schema.ts` declares the unique index `salary_history_user_effective_from_uq` over `(user_id, effective_from)`
    - All three app-owned tables carry a `user_id` foreign key to `user.id` with cascade delete (3 cascade occurrences)
    - `payment_schedule` carries check constraints restricting `avans_day` and `salary_day` to 1..31
    - `src/lib/db/auth-schema.ts` exists and defines `user`, `session`, `account`, `verification`; it was produced by `npx auth generate` (not hand-authored)
    - `src/lib/auth.ts` exports `auth`, imports `drizzleAdapter` from `@better-auth/drizzle-adapter`, sets `requireEmailVerification: false`, and sets `expiresIn` to 60*60*24*30
    - `grep -c "socialProviders\|forgetPassword\|resetPassword" src/lib/auth.ts` returns 0 (D-05 and D-08 deferrals honoured)
    - `npx tsc --noEmit` exits 0
    - No `drizzle-kit push` or `drizzle-kit migrate` was executed by this task
  </acceptance_criteria>
  <done>`src/lib/db/schema.ts`, `src/lib/db/auth-schema.ts`, and `src/lib/auth.ts` exist and type-check; the data model encodes D-01's day-of-month schedule, D-14's exact-date collision constraint, D-11's `is_estimated` flag, and D-05/D-06/D-07's auth configuration — with no schema yet applied to the live database.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → developer machine | Third-party package code executes at install and build time |
| application process → Neon Postgres | Financial personal data crosses the network and is persisted |
| environment/secret store → application | `DATABASE_URL` and `BETTER_AUTH_SECRET` enter the process |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs in Task 2 | high | mitigate | Package-legitimacy gate (Task 1) is a `gate="blocking-human"` checkpoint covering the four SUS-flagged packages; the Task 2 verify command additionally fails the build if the deprecated Better Auth CLI package is present |
| T-01-01 | Elevation of Privilege | `salary_history`, `payment_schedule`, `ytd_baseline` | high | mitigate | Schema-level ownership anchor: every app table has a not-null `user_id` FK to `user.id` with cascade delete, so no row can exist without an owner. Query-level enforcement lands in Plan 01-04's repository layer |
| T-01-03 | Spoofing | Better Auth session config (`src/lib/auth.ts`) | medium | mitigate | Session lifetime is set explicitly (30-day `expiresIn`, 7-day `updateAge`) per D-07 rather than left implicit; Better Auth's default `httpOnly`/`secure`/`sameSite` cookie flags are not weakened. Secret is a 32-byte random value validated by `src/env.ts` at boot |
| T-01-05 | Information Disclosure | `.env.local` containing the Neon credential and auth secret | high | mitigate | `.env.local` is gitignored (asserted by `git check-ignore`); only `.env.example` with placeholder values is committed |
| T-01-06 | Denial of Service | Missing or malformed env vars at boot | low | accept | `@t3-oss/env-nextjs` fails the process loudly at boot. A loud boot failure is the intended behaviour for a solo-operated app; no runtime degradation path is warranted |
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0.
2. `npm run build` exits 0.
3. `git check-ignore -q .env.local` exits 0 and `.env.example` is tracked.
4. The dependency assertion in Task 2's verify passes (all Phase 1 packages present, TypeScript exactly 6.0.3, deprecated auth CLI absent).
5. The schema assertion in Task 3's verify passes (three app tables, two bigint money columns, the unique index, three cascade FKs, four generated auth tables, D-06 and D-07 settings present).
</verification>

<success_criteria>
- A Next.js 16 App Router project exists at the repo root and builds without errors.
- TypeScript is pinned to exactly `6.0.3`.
- `src/env.ts` validates `DATABASE_URL`, `BETTER_AUTH_SECRET`, and `BETTER_AUTH_URL` at boot.
- `src/lib/db/schema.ts` defines `salary_history`, `payment_schedule`, and `ytd_baseline` with `bigint` kopeck money columns, cascade-delete ownership FKs, the `(user_id, effective_from)` unique index, and 1..31 day-of-month check constraints.
- `src/lib/db/auth-schema.ts` was generated by `npx auth generate` and defines `user`, `session`, `account`, `verification`.
- `src/lib/auth.ts` configures email+password auth with verification disabled and a 30-day rolling session, and configures no OAuth provider and no password-reset flow.
</success_criteria>

<output>
Create `.planning/phases/01-core-payroll-loop/01-01-SUMMARY.md` when done
</output>
