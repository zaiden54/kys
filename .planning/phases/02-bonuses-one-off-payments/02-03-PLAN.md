---
phase: 02-bonuses-one-off-payments
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - src/app/actions/forecast.ts
  - src/app/actions/forecast.test.ts
  - src/app/(app)/bonuses/bonus-form.tsx
  - src/app/(app)/bonuses/bonus-form.test.ts
  - src/app/(app)/bonuses/bonus-row.tsx
  - src/app/(app)/bonuses/bonus-row.test.ts
  - src/lib/validation/bonus.ts
  - src/lib/validation/bonus.test.ts
autonomous: true
requirements: [BON-01, BON-02]
gap_closure: true
gap_ids: [G-02-roadmap-userstory]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The Phase 2 ROADMAP.md Goal line validates as a well-formed user story (non-empty role, capability, and outcome slots), so gsd-verifier's mandatory MVP pre-flight guard stops refusing to evaluate BON-01/BON-02."
    - "A confirmed YTD baseline whose asOfDate falls outside the year/date boundary getCumulativeIncomeBeforeDate actually honors is never reported to the UI as baselineIsEstimated: false."
    - "A rejected or thrown saveBonusAction call from either the create form or the inline edit form always surfaces the same fixed generic Russian retry message the delete flow already shows, never a silent/unhandled rejection."
    - "A bonus amount carrying more than two decimal places of ruble precision is rejected by bonusInputSchema before it can silently pass through Math.round(value * 100)."
  artifacts:
    - ".planning/ROADMAP.md (Phase 2 Goal line, committed in valid user-story format)"
    - "src/app/actions/forecast.ts (baselineIsEstimated gated on the same boundary getCumulativeIncomeBeforeDate uses)"
    - "src/app/actions/forecast.test.ts (regression case for the boundary fix)"
    - "src/app/(app)/bonuses/bonus-form.tsx (guarded onSubmit + rendered serverError)"
    - "src/app/(app)/bonuses/bonus-form.test.ts (new AST regression test)"
    - "src/app/(app)/bonuses/bonus-row.tsx (guarded onEdit + rendered error in editing mode)"
    - "src/app/(app)/bonuses/bonus-row.test.ts (new AST regression test)"
    - "src/lib/validation/bonus.ts (sub-kopeck precision refine on amountRubles)"
    - "src/lib/validation/bonus.test.ts (regression cases for precision rejection)"
  key_links:
    - "ROADMAP.md Phase 2 Goal -> gsd-tools query user-story.validate -> gsd-verifier's MVP pre-flight guard — the exact chain 02-VERIFICATION.md's single gap describes."
    - "forecastNextPayment's baselineIsEstimated flag -> the same asOfDate/year boundary getCumulativeIncomeBeforeDate already applies to the cumulative-income figure itself, so the confidence flag and the number it describes can never disagree."
    - "bonus-form.tsx/bonus-row.tsx catch clauses -> the fixed generic retry string already established by SalaryForm's guarded submit in pay-setup-forms.tsx, keeping every save-path failure mode uniform across the app."
---

<objective>
Close the single gap recorded in `02-VERIFICATION.md`: Phase 2's ROADMAP goal failed the canonical
`user-story.validate` guard, so the MVP verifier refused to evaluate BON-01/BON-02 at all — it is not
evidence that the bonus implementation is broken. This plan (1) locks in the already-corrected,
now-valid user-story goal so `/gsd-execute-phase 02 --gaps-only` can re-invoke `gsd-verifier` against a
valid roadmap contract, and (2) closes the three real, evidence-backed code-review warnings
(`WR-01`, `WR-02`, `WR-03`) still open in `02-REVIEW.md` against the current `src/app/actions/forecast.ts`,
`src/app/(app)/bonuses/bonus-form.tsx`, `src/app/(app)/bonuses/bonus-row.tsx`, and
`src/lib/validation/bonus.ts` — so re-verification does not immediately surface them as fresh gaps.

This plan makes NO other changes to BON-01/BON-02 behavior. Bonus creation, editing, deletion, and
forecast integration were built and tested in 02-01/02-02 and are not touched here beyond the three
named warning fixes.

Purpose: unblock re-verification of Phase 2 by correcting the roadmap-contract format defect, and
retire the three outstanding code-review warnings before the verifier looks at them again.
Output: a corrected, committed ROADMAP.md goal; a forecast confidence flag that can never
misrepresent an ignored baseline as confirmed; save/edit forms that always surface a failure to the
user; and a validation schema that rejects sub-kopeck ruble precision.
</objective>

<execution_context>
@/home/zaiden/.claude/gsd-core/workflows/execute-plan.md
@/home/zaiden/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-bonuses-one-off-payments/02-VERIFICATION.md
@.planning/phases/02-bonuses-one-off-payments/02-REVIEW.md
@.planning/phases/02-bonuses-one-off-payments/02-01-SUMMARY.md
@.planning/phases/02-bonuses-one-off-payments/02-02-SUMMARY.md
@src/app/actions/forecast.ts
@src/lib/db/salary-repository.ts
@src/app/(app)/bonuses/bonus-form.tsx
@src/app/(app)/bonuses/bonus-row.tsx
@src/lib/validation/bonus.ts
@src/components/pay-setup-forms.tsx
@src/components/pay-setup-forms.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock in the corrected Phase 2 user-story goal (closes G-02-roadmap-userstory)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    .planning/ROADMAP.md (Phase 2 section — the `**Goal:**` line already reads, uncommitted in the
    working tree, "As a signed-in user, I want to attach a one-off bonus or compensation (ex. sports)
    to a payment date, so that I can see how it affects my cumulative НДФЛ and future take-home
    payments."); .planning/phases/02-bonuses-one-off-payments/02-VERIFICATION.md (the sole gap: the
    old goal — "A user can attach a one-off premium or compensation to a specific payment date and
    see it flow through the same cumulative НДФЛ engine as regular salary, correctly affecting that
    and subsequent payments." — failed `user-story.validate` because it has no "As a [role], I want
    to [capability], so that [outcome]." shape).
  </read_first>
  <action>
    Confirm the Phase 2 `**Goal:**` line in `.planning/ROADMAP.md` already reads as a valid
    "As a [role], I want to [capability], so that [outcome]." user story — it does, per the edit
    already present in the working tree before this plan runs. Do not reword it further; this task
    only verifies the existing text is correct and lets it be committed as part of this plan's
    execution. Run the verification command below and confirm it reports `valid: true` with
    non-empty `role`, `capability`, and `outcome` slots. If, and only if, the goal line has
    regressed back to non-user-story prose, restore it to exactly: "As a signed-in user, I want to
    attach a one-off bonus or compensation (ex. sports) to a payment date, so that I can see how it
    affects my cumulative НДФЛ and future take-home payments." No other line in `.planning/ROADMAP.md`
    changes — the `**Requirements**`, `**Mode**`, `**Depends on**`, `**Success Criteria**`, and
    `**Plans**` lines for Phase 2 stay exactly as they are.
  </action>
  <verify>
    <automated>node /home/zaiden/.claude/gsd-core/bin/gsd-tools.cjs query user-story.validate --story "As a signed-in user, I want to attach a one-off bonus or compensation (ex. sports) to a payment date, so that I can see how it affects my cumulative НДФЛ and future take-home payments." | grep -q '"valid": true'</automated>
  </verify>
  <acceptance_criteria>
    `gsd-tools query user-story.validate` run against the committed Phase 2 Goal line in
    `.planning/ROADMAP.md` reports `valid: true` with non-empty `role`, `capability`, and `outcome`
    slots in its `slots` object, and no other content in `.planning/ROADMAP.md`'s Phase 2 section
    changed.
  </acceptance_criteria>
  <done>The Phase 2 Goal line is a valid, committed user story; `G-02-roadmap-userstory` is closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate baselineIsEstimated on the same boundary the cumulative-income query honors (closes WR-01)</name>
  <files>src/app/actions/forecast.ts, src/app/actions/forecast.test.ts</files>
  <read_first>
    src/app/actions/forecast.ts (`forecastNextPayment`, the `baselineIsEstimated: ytdBaseline.isEstimated`
    line near the end of the function); src/lib/db/salary-repository.ts (`getCumulativeIncomeBeforeDate`'s
    `baselineApplies` check: `baseline.asOfDate.slice(0, 4) === paymentYear && baseline.asOfDate <=
    isoDate`); .planning/phases/02-bonuses-one-off-payments/02-REVIEW.md WR-01 (exact reproduction:
    a confirmed baseline dated in a different year than the payment, or dated after the payment,
    contributes zero to the cumulative figure but is still reported as `baselineIsEstimated: false`).
  </read_first>
  <behavior>
    - Test: a confirmed (`isEstimated: false`) YTD baseline whose `asOfDate` year does not match the
      resolved payment's year still yields `result.forecast.baselineIsEstimated === true` (the
      baseline was silently ignored, so the flag must say so).
    - Existing test (4) ("a user who skipped the year-to-date question...") and test (1)/(5)'s
      same-year, in-boundary confirmed-baseline behavior are unchanged: a baseline that DOES apply
      still reports its own `isEstimated` value untouched.
  </behavior>
  <action>
    In `forecastNextPayment`, after `paymentDateIso` and `ytdBaseline` are both known and before the
    returned `forecast` object is built, compute `const baselineApplies = ytdBaseline.asOfDate.slice(0, 4)
    === paymentDateIso.slice(0, 4) && ytdBaseline.asOfDate <= paymentDateIso;` — the identical
    two-part boundary condition `getCumulativeIncomeBeforeDate` already uses in
    `src/lib/db/salary-repository.ts` to decide whether the baseline contributes to the cumulative
    figure. Replace the returned `baselineIsEstimated: ytdBaseline.isEstimated` field with
    `baselineIsEstimated: !baselineApplies || ytdBaseline.isEstimated`. This closes WR-01: a
    confirmed baseline the cumulative-income query silently ignored (wrong year, or dated after the
    payment) must never be reported to the UI as confirmed.
  </action>
  <verify>
    <automated>npx vitest run src/app/actions/forecast.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    A new regression test in `forecast.test.ts` proves a confirmed baseline dated in a year other
    than the resolved payment's year produces `baselineIsEstimated: true`; all five pre-existing
    numbered tests in `forecast.test.ts` still pass unchanged; `npx tsc --noEmit` reports no errors.
  </acceptance_criteria>
  <done>forecastNextPayment never reports a silently-ignored baseline as confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Guard bonus save/edit against unhandled rejections and reject sub-kopeck precision (closes WR-02, WR-03)</name>
  <files>src/app/(app)/bonuses/bonus-form.tsx, src/app/(app)/bonuses/bonus-form.test.ts, src/app/(app)/bonuses/bonus-row.tsx, src/app/(app)/bonuses/bonus-row.test.ts, src/lib/validation/bonus.ts, src/lib/validation/bonus.test.ts</files>
  <read_first>
    src/app/(app)/bonuses/bonus-form.tsx (`onSubmit`, currently un-guarded `await saveBonusAction(...)`);
    src/app/(app)/bonuses/bonus-row.tsx (`onEdit`, currently un-guarded — contrast with `onDelete`'s
    existing try/catch/finally at the bottom of the same file); src/components/pay-setup-forms.tsx
    (`SalaryForm`'s `submit` function, the `try { ... } catch { setServerError("Не удалось сохранить
    оклад. Попробуйте ещё раз."); }` guard) and src/components/pay-setup-forms.test.ts (the
    `ts.createSourceFile`-based AST test that asserts this guard's shape — the pattern to replicate
    for both new bonus test files); src/lib/validation/bonus.ts (`amountRubles`'s existing
    `.refine((value) => Math.round(value * 100) > 0, ...)` chain); .planning/phases/02-bonuses-one-off-payments/02-REVIEW.md
    WR-02 and WR-03 (exact reproductions and suggested fixes).
  </read_first>
  <behavior>
    - bonus-form.tsx: a `saveBonusAction` call that rejects (not just one that resolves with
      `success: false`) sets a generic Russian retry message that renders in the form, and never
      reads or interpolates the caught value.
    - bonus-row.tsx: an `onEdit` call to `saveBonusAction` that rejects sets the same generic retry
      message, rendered while still in `mode === "editing"` (not only in the `"display"` return).
    - bonus.ts: `amountRubles` values of `1.001` and `1.005` fail validation; `1.01` still passes.
  </behavior>
  <action>
    WR-02 (bonus-form.tsx): add `const [serverError, setServerError] = useState<string | null>(null);`
    alongside the existing `message` state. In `onSubmit`, clear it (`setServerError(null)`) next to
    the existing `setMessage(null)`, wrap the remaining body of `onSubmit` (the `saveBonusAction`
    call and its result handling) in a `try` block, and add `catch { setServerError("Не удалось
    сохранить бонус. Попробуйте ещё раз."); }` — the identical message and shape as `SalaryForm`'s
    guarded `submit` in `pay-setup-forms.tsx`. Render `{serverError && <p className="text-sm
    text-red-600">{serverError}</p>}` next to the existing `{message && ...}` line.

    WR-02 (bonus-row.tsx): in `onEdit`, clear the existing `error` state (`setErrorMessage(null)`) at
    the top, wrap the remaining body in `try`, and on `catch` call `setErrorMessage("Не удалось
    сохранить бонус. Попробуйте ещё раз.")` — reusing the same state and message `onDelete` already
    uses. Add `{error && <p className="text-sm text-red-600">{error}</p>}` inside the `mode ===
    "editing"` `<li>` block's form (it currently only renders in the `"display"` return, so an
    edit-time failure is invisible while still in edit mode).

    WR-03 (bonus.ts): add a second `.refine(...)` on `amountRubles`, chained after the existing
    "at least one kopeck" refine: `.refine((value) => Number.isInteger(Math.round(value *
    100_000_000) / 1_000_000), { message: "Укажите сумму с точностью не более двух знаков после
    запятой" })` — rejects any ruble amount carrying more than two decimal places before it reaches
    `rublesToKopecks`'s `Math.round(value * 100)`.

    New tests: add `bonus-form.test.ts` and `bonus-row.test.ts` using the same
    `ts.createSourceFile`/AST-inspection technique as `pay-setup-forms.test.ts` — assert a
    try/catch wraps the awaited `saveBonusAction` call in each component's submit handler, that the
    catch clause calls the corresponding error setter with a Russian, non-templated string literal
    (no `console.`, no `.message`, no `${`), and that a conditional JSX expression referencing that
    same state variable exists in the component's returned JSX. Extend `bonus.test.ts` with cases
    rejecting `1.001` and `1.005`, and accepting `1.01`.
  </action>
  <verify>
    <automated>npx vitest run src/app/(app)/bonuses/bonus-form.test.ts src/app/(app)/bonuses/bonus-row.test.ts src/lib/validation/bonus.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    Both new AST tests pass, confirming a try/catch guards the awaited `saveBonusAction` call in
    `onSubmit` (bonus-form.tsx) and `onEdit` (bonus-row.tsx), each catch sets a fixed Russian retry
    string without reading the caught error's message, and each component renders that state
    conditionally; `bonus.test.ts` rejects `1.001` and `1.005` and accepts `1.01`; `npx tsc --noEmit`
    reports no errors.
  </acceptance_criteria>
  <done>Save/edit failures on the bonus form and row are never silent; sub-kopeck ruble precision is rejected before conversion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → `saveBonusAction` (create + edit paths) | Untrusted amount/date/note (and, for edits, a client-supplied bonus id) crosses here; Zod (`bonusInputSchema`) re-validates server-side regardless of client-side `step="0.01"` hints |
| Documentation contract → automated verifier tooling | `.planning/ROADMAP.md`'s Phase 2 `**Goal:**` line is read by `gsd-verifier`'s mandatory MVP pre-flight guard; a malformed goal blocks all downstream requirement verification for the entire phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Tampering | `src/lib/validation/bonus.ts` `bonusInputSchema.amountRubles` | low | mitigate | Add a second `refine` rejecting ruble amounts with more than two decimal places (e.g. `1.005`) before `rublesToKopecks`'s `Math.round(value * 100)` can silently round them — closes WR-03. |
| T-02-11 | Denial of Service | `src/app/(app)/bonuses/bonus-form.tsx` `onSubmit`, `src/app/(app)/bonuses/bonus-row.tsx` `onEdit` | low | mitigate | Wrap the awaited `saveBonusAction` call in `try/catch` in both handlers; the catch sets only the fixed generic Russian retry string, never the caught error's own message/detail (matches the T-01-04 no-money-in-logs convention) — closes WR-02. |
| T-02-12 | Tampering (data-integrity of a trust signal) | `src/app/actions/forecast.ts` `forecastNextPayment`'s `baselineIsEstimated` field | medium | mitigate | Gate the flag on the identical `asOfDate`/year boundary `getCumulativeIncomeBeforeDate` already enforces on the cumulative figure itself, so a baseline the tax calculation silently ignored can never be reported to the UI as confirmed — closes WR-01. |
| T-02-13 | Denial of Service (verification pipeline) | `.planning/ROADMAP.md` Phase 2 `**Goal:**` line | low | mitigate | Lock the goal into `gsd-tools user-story.validate`-conformant "As a / I want to / so that" format so `gsd-verifier`'s mandatory MVP pre-flight guard stops refusing to evaluate BON-01/BON-02 — closes `G-02-roadmap-userstory`. |
</threat_model>

<verification>
1. `node /home/zaiden/.claude/gsd-core/bin/gsd-tools.cjs query user-story.validate --story "As a signed-in user, I want to attach a one-off bonus or compensation (ex. sports) to a payment date, so that I can see how it affects my cumulative НДФЛ and future take-home payments."` reports `valid: true`.
2. `npx vitest run src/app/actions/forecast.test.ts src/app/(app)/bonuses/bonus-form.test.ts src/app/(app)/bonuses/bonus-row.test.ts src/lib/validation/bonus.test.ts` passes.
3. `npm test -- --run` (full suite) passes.
4. `npx tsc --noEmit` and `npm run build` both exit 0.
</verification>

<success_criteria>
- `.planning/ROADMAP.md`'s Phase 2 Goal line is committed and validates as a well-formed user story.
- `02-REVIEW.md`'s three open warnings (WR-01, WR-02, WR-03) are closed with passing regression tests.
- No BON-01/BON-02 behavior implemented in 02-01/02-02 was altered beyond these three named fixes.
- Full test suite and production build are green.
</success_criteria>

<output>
Create `.planning/phases/02-bonuses-one-off-payments/02-03-SUMMARY.md` when done.
</output>

## Artifacts this phase produces

No new production modules, exported types, or user-facing routes are created by this plan. It:
- Corrects and commits `.planning/ROADMAP.md`'s Phase 2 Goal line (documentation only — closes the
  sole `02-VERIFICATION.md` gap, `G-02-roadmap-userstory`).
- Modifies three existing production files (`src/app/actions/forecast.ts`,
  `src/app/(app)/bonuses/bonus-form.tsx`, `src/app/(app)/bonuses/bonus-row.tsx`,
  `src/lib/validation/bonus.ts`) to close three pre-existing, evidence-backed code-review warnings
  (WR-01, WR-02, WR-03) from `02-REVIEW.md`.
- Adds two new regression test files (`src/app/(app)/bonuses/bonus-form.test.ts`,
  `src/app/(app)/bonuses/bonus-row.test.ts`) using the AST-inspection technique already established
  by `src/components/pay-setup-forms.test.ts`, plus new cases in two existing test files
  (`src/app/actions/forecast.test.ts`, `src/lib/validation/bonus.test.ts`).
