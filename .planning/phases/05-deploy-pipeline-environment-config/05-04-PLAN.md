---
phase: 05-deploy-pipeline-environment-config
plan: 04
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - .planning/phases/05-deploy-pipeline-environment-config/DEPLOYMENT.md
autonomous: false
requirements: [DEPLOY-01, DEPLOY-02, DEPLOY-04]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "DEPLOY-01 (verbatim ROADMAP Phase 5 success criterion 1): A persistent staging URL (its own Vercel domain + its own Neon branch) exists, is reachable independent of production, and shows its own data. [Spec-less probe fallback note: unclassified/unresolved by the deterministic edge-coverage engine; authored from ROADMAP.md's Phase 5 Success Criteria text.] Delivered via a real, pushed, persistent `staging` git branch (Vercel's existing native git integration auto-assigns it a stable branch-alias domain — no Vercel Pro \"Custom Environments\" feature required, sidestepping 05-RESEARCH.md's Assumption A1 plan-tier uncertainty) paired with a real, dedicated Neon branch created off the project backing `on-hands` (host `ep-calm-silence-b13zw5kq-pooler.c-5.eu-central-1.aws.neon.tech`)."
    - "DEPLOY-02 (verbatim ROADMAP Phase 5 success criterion 5, env-var half): Environment variables (BETTER_AUTH_URL, DATABASE_URL, etc.) are correctly scoped per environment (Preview/Staging/Prod). Delivered via `vercel env` commands that audit current scoping first (`vercel env ls`) rather than blindly overwriting, then add/replace only what is missing or mis-scoped — Production is left untouched unless demonstrably wrong; Preview (generic, ad-hoc PR branches) and the new Staging (the `staging` git branch specifically) each get their own dedicated Neon branch's DATABASE_URL and a secret distinct from Production's."
    - "DEPLOY-04 (verbatim ROADMAP Phase 5 success criterion 5, process half): A documented feature-branch → staging (manual check) → production release procedure exists, has been followed for a real deploy, and environment variables are confirmed correctly scoped per environment. Delivered via DEPLOYMENT.md (this phase's own artifact) plus a real, once-only execution of the feature-branch → staging leg using this very phase's branch as the payload — staging → production promotion itself is intentionally left for a human-approved `/gsd-ship` later, not auto-merged to main by this autonomous plan."
  artifacts:
    - .planning/phases/05-deploy-pipeline-environment-config/DEPLOYMENT.md
  key_links:
    - "The pushed `staging` git branch is created from `origin/main` and then has this phase's own branch merged in — so the very first real payload through the new pipeline is Phase 5's own deploy-pipeline work, giving DEPLOY-04's 'exercised once for real' requirement a genuine, self-referential proof rather than a synthetic no-op commit"
    - "vercel env add DATABASE_URL preview staging scopes the staging Neon branch's connection string specifically to the staging git branch — distinct from the generic Preview scope used by ad-hoc PR branches"
  prohibitions:
    - requirement_id: DEPLOY-01
      category: safety
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT present or link the staging environment to end users as if it were the production app — no navigation link, shared bookmark, or shared credential set should let a real user mistake staging for production."
    - requirement_id: DEPLOY-02
      category: privacy
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT scope a Production-tier secret (BETTER_AUTH_SECRET, the production DATABASE_URL) to Preview or Development environments for testing convenience — each environment's secrets stay isolated to that environment, and staging gets its own freshly-generated BETTER_AUTH_SECRET, never a copy of production's."
    - requirement_id: DEPLOY-04
      category: transparency
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT treat the staging manual-verification checklist as a rubber stamp — each DEPLOYMENT.md checklist item must be genuinely executed (real curl/login attempt, real response inspected) before being checked off, not marked complete without being done."
---

<objective>
Stand up the persistent staging environment DEPLOY-01 requires (a dedicated Neon branch + a dedicated Vercel git-branch-alias domain), correctly scope DEPLOY-02's environment variables across Production/Preview/Staging using `vercel env`, and write + genuinely exercise DEPLOYMENT.md's feature-branch → staging leg for real — completing the phase.

Purpose: this is the expansion that finally makes SEC-04's dynamic host allowlist (Plan 05-01) and the CI gate (Plan 05-03) meaningful against real, separate, live environments instead of only localhost.
Output: a live `staging` branch/domain/Neon-branch triad, correctly scoped Vercel environment variables, and `DEPLOYMENT.md` — the phase's canonical, exercised-once release procedure.
</objective>

<execution_context>
@/home/zaiden/.claude/gsd-core/workflows/execute-plan.md
@/home/zaiden/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-deploy-pipeline-environment-config/05-RESEARCH.md
@src/lib/auth-allowed-hosts.ts
@scripts/verify-auth-flow.mjs
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package-legitimacy check for the `vercel` CLI before install</name>
  <what-built>Nothing yet in this plan — this checkpoint gates the FIRST external package install this plan needs (the `vercel` CLI), per the mandatory Package Legitimacy Gate. 05-RESEARCH.md's own Package Legitimacy Audit table states "No new packages required for Phase 5" and does not cover `vercel`, so it is treated as `[ASSUMED]` and must be confirmed before install, even though it is the official first-party CLI for the platform this entire project already deploys to (confirmed live via `gh api repos/zaiden54/kys/deployments` during planning — `vercel[bot]` already deploys this repo through GitHub's native Vercel integration).</what-built>
  <how-to-verify>
    Confirm `vercel` is the genuine, official Vercel CLI package before Task 2 installs it:
    1. Visit https://www.npmjs.com/package/vercel
    2. Confirm the publisher is Vercel Inc. (the same company operating the Vercel platform this project already deploys to) and the package has a very high, long-standing weekly download count consistent with a foundational platform CLI (tens of millions/week)
    3. Confirm the version Task 2 will install (`npm i -g vercel` with no pin, i.e. latest) is a recent, non-yanked release
  </how-to-verify>
  <resume-signal>Type "approved" to let Task 2 install and use the `vercel` CLI, or name a concern to investigate first</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create the persistent staging Neon branch and push the persistent staging git branch</name>
  <reversibility rating="reversible">Neon's copy-on-write branching model makes a wrongly-parented or wrongly-named branch cheap to delete and recreate — no data migration, and only one Vercel env var (DATABASE_URL for the `staging` Preview scope, set in Task 3) needs repointing if this branch is ever redone.</reversibility>
  <precondition>The Package Legitimacy checkpoint above was approved.</precondition>
  <files>N/A — this task provisions external infrastructure (Neon branches, a git branch push) and installs the `vercel` CLI; no repository source files are modified</files>
  <read_first>
    .env.local (read-only — contains the current single DATABASE_URL: host `ep-calm-silence-b13zw5kq-pooler.c-5.eu-central-1.aws.neon.tech`, database `on-hands`, role `on-hands_owner` — use this host string to locate the correct Neon project; do not guess a project name)
    .planning/phases/05-deploy-pipeline-environment-config/05-RESEARCH.md ("Pitfall 3: Preview Database Branch Conflicts" — why a dedicated branch per environment tier matters)
  </read_first>
  <action>
    Locate the Neon project backing the connection currently in `.env.local` (host `ep-calm-silence-b13zw5kq-pooler.c-5.eu-central-1.aws.neon.tech`, database `on-hands`) — use the Neon MCP tools available in this session if present (list projects/branches, match by host or database name), falling back to the Neon Console (https://console.neon.tech/app/projects) if no Neon MCP tool is available in this execution context. Create two new branches from that project's default/production branch: one named `staging` (persistent — pairs with the `staging` git branch below) and one named `preview` (persistent — pairs with Vercel's generic Preview scope used by every ad-hoc PR branch other than `staging`, replacing whatever DATABASE_URL Preview currently has, which — absent evidence of a dedicated Vercel-Neon Marketplace integration on this project — may currently be pointing at production; this is the DEPLOY-02 gap this task closes for the Preview tier). Capture each new branch's own pooled connection string — do not reuse the production connection string for either.

    Create and push the persistent staging git branch, using this phase's own work as its first real payload (this is the concrete DEPLOY-04 "exercised once for real" proof): `git fetch origin main && git checkout -b staging origin/main && git merge --no-edit <this-phase's-branch-name> && git push -u origin staging`, then return to this phase's own branch (`git checkout <this-phase's-branch-name>`) to continue work — do not leave `staging` as the checked-out branch. Confirm Vercel's existing native git integration picked up the push automatically (no vercel CLI needed for this step — it already auto-deploys every pushed branch on this repo): poll `gh api repos/zaiden54/kys/deployments --jq '.[] | select(.ref | test("staging")) | .id'` until a deployment for `staging` appears, then fetch its status the same way Task 1 of Plan 05-03 did (`gh api repos/zaiden54/kys/deployments/{id}/statuses --jq '.[0].target_url'`) to discover the real staging deployment URL. Separately, install and use the `vercel` CLI (`npm i -g vercel`) to discover the STABLE branch-alias domain (not the per-deployment hash URL) via `vercel ls staging` or `vercel inspect <the-per-deployment-url-from-target_url>` — expect a pattern like `on-hands-git-staging-careeremit-9861s-projects.vercel.app` (team slug confirmed during planning via `gh api`); if `vercel` prompts for authentication, this is the expected auth gate — request a Vercel token from the human (Vercel Dashboard → Account Settings → Tokens) via a dynamically-created `checkpoint:human-action`, run `vercel login --token <token>` or export `VERCEL_TOKEN`, then retry.
  </action>
  <verify>
    <automated>gh api repos/zaiden54/kys/deployments --jq '[.[] | select(.ref | test("staging"))] | length' 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - Two new Neon branches exist (`staging`, `preview`), each with its own connection string distinct from production's and distinct from each other's
    - `git ls-remote origin staging` shows the branch exists on the shared remote
    - `gh api repos/zaiden54/kys/deployments --jq '...'` (query above) returns a count ≥ 1 for the `staging` ref — Vercel genuinely deployed it, not merely accepted the push
    - The stable staging branch-alias domain (not a per-deployment hash URL) is captured for use in Task 3's DEPLOYMENT.md
  </acceptance_criteria>
  <done>A dedicated Neon staging branch and a dedicated Neon preview branch exist; the persistent staging git branch is pushed and has a confirmed real Vercel deployment with a discovered stable domain.</done>
</task>

<task type="auto">
  <name>Task 3: Scope environment variables, write DEPLOYMENT.md, exercise the staging leg for real</name>
  <precondition>Task 2's Neon staging/preview branches and the pushed `staging` git branch with a confirmed Vercel deployment both exist.</precondition>
  <files>.planning/phases/05-deploy-pipeline-environment-config/DEPLOYMENT.md</files>
  <read_first>
    scripts/verify-auth-flow.mjs (existing script from Phase 1 — reads `BASE_URL`/`DATABASE_URL` from the environment and exercises register → sign-in → protected-route → duplicate-registration-rejected against a running deployment; reuse this exact script pointed at the staging URL/Neon branch rather than writing a new one)
    src/lib/auth-allowed-hosts.ts (confirms `*.vercel.app` already covers whatever domain Task 2 discovered — no code change needed here, only verification that the real discovered domain matches the pattern)
  </read_first>
  <action>
    Run `vercel link` (non-interactively if possible; if it prompts, answer to link the existing `on-hands` project under team `careeremit-9861s-projects`) then `vercel env ls` for Production, Preview, and Development to see current scoping before changing anything — do not blindly overwrite. For the `staging`-branch-specific Preview scope: `vercel env add DATABASE_URL preview staging` (paste the staging Neon branch's connection string from Task 2) and `vercel env add BETTER_AUTH_SECRET preview staging` (paste a freshly generated `openssl rand -base64 32` value — distinct from Production's secret, per this plan's DEPLOY-02 prohibition against secret reuse across environments). For the generic Preview scope (every other, ad-hoc PR branch): if `DATABASE_URL`/`BETTER_AUTH_SECRET` are already set there and correctly point away from production, leave them; if they are missing or currently point at the production database (the likely pre-existing gap, since no Vercel-Neon Marketplace integration was found on this project during planning), replace them (`vercel env rm <name> preview --yes` then `vercel env add <name> preview`) with the `preview` Neon branch's connection string from Task 2 and a freshly generated secret. Leave Production's existing `DATABASE_URL`/`BETTER_AUTH_SECRET` untouched unless `vercel env ls production` shows one is missing entirely (the app is already live in production per the existing deployments, so this should already be correctly set).

    Write `.planning/phases/05-deploy-pipeline-environment-config/DEPLOYMENT.md` documenting, with the real values discovered in this plan (not RESEARCH.md's `example.com` placeholders): the three environments (Local/PR-Preview/Staging/Production) and their real or real-pattern URLs, the environment-variable scoping matrix this task just configured, the feature-branch → staging (manual check) → production procedure (staging leg: merge feature branch into `staging`, confirm Vercel auto-deploys, run the manual checklist below; production leg: explicitly deferred to a human-approved PR/`/gsd-ship` — this plan does not auto-merge to `main`), and a manual verification checklist for the staging leg covering: staging URL responds 200, register/login succeed with no cross-environment redirect (this is SEC-04's live proof, completing what Plan 05-01 could only prove with a unit test), and staging shows its own, isolated data (not production's).

    Genuinely execute the staging-leg checklist for real (not a rubber stamp, per this plan's DEPLOY-04 prohibition): `curl -sI https://<the-real-staging-domain-from-task-2>/` and confirm a 200/redirect-to-login response; then run `scripts/verify-auth-flow.mjs` with `BASE_URL=https://<the-real-staging-domain>` and `DATABASE_URL=<the-staging-Neon-branch-connection-string>` to prove register → sign-in → protected-route works end-to-end against the real deployed staging environment with its own real database — this is the same script Phase 1 wrote for local verification, reused unmodified against a live deployment. Record the actual pass/fail output in the SUMMARY.
  </action>
  <verify>
    <automated>BASE_URL="https://<the-real-staging-domain>" DATABASE_URL="<the-staging-neon-connection-string>" node scripts/verify-auth-flow.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `vercel env ls preview` (with the `staging` git-branch filter, if the CLI supports scoping the list) shows `DATABASE_URL` and `BETTER_AUTH_SECRET` pointing at values distinct from production's
    - `DEPLOYMENT.md` exists, contains the real discovered staging domain (not a placeholder), and its environment-variable matrix reflects what `vercel env ls` actually returns after this task
    - `scripts/verify-auth-flow.mjs` run against the real staging URL/database exits 0 — a genuine register/login round-trip against the live staging deployment, not a local-only run
  </acceptance_criteria>
  <done>Environment variables are confirmed (not assumed) correctly scoped per environment; DEPLOYMENT.md exists with real values; the feature-branch → staging leg has been exercised once for real with a passing end-to-end auth proof against the live staging deployment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Executor/human ↔ Vercel API (via CLI token) | Secret-bearing boundary — a Vercel access token must never be echoed into a log, commit, or DEPLOYMENT.md |
| Neon staging/preview branch credentials ↔ production credentials | Each Neon branch gets unique, independently-revocable credentials by construction of Neon's branching model |
| Staging deployment ↔ real users | Staging must never be mistaken for production by an end user |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-05-02 | Information Disclosure | Vercel/Neon secrets during CLI provisioning | high | mitigate | Secrets are piped directly into `vercel env add`/generated via `openssl rand` and never written to a tracked file; DEPLOYMENT.md records the environment-variable *matrix* (which var is set where) but never a secret *value*; any Vercel token obtained via the dynamic auth-gate checkpoint is used only in-session, never committed |
| T-05-06 | Information Disclosure | Staging/production data isolation | high | mitigate | Dedicated Neon `staging` branch (copy-on-write, unique credentials) with its own `DATABASE_URL` scoped specifically to the `staging` git branch via `vercel env add DATABASE_URL preview staging`; verified end-to-end by `verify-auth-flow.mjs` creating and reading back a user against the staging database specifically, not production |
| T-05-07 | Information Disclosure / Tampering | Generic ad-hoc PR-preview isolation | low | accept | Ad-hoc PR previews (any branch other than `staging`) share ONE dedicated `preview` Neon branch rather than a fully-isolated branch per PR (which would require installing the Vercel-Neon Marketplace integration — an OAuth-consent, browser-only action with no CLI/API path, and explicitly Phase 7's job per E2E-06's isolated-branch-per-CI-run scope). No production or real financial data is at risk; only synthetic PR-preview test data could theoretically cross between concurrent ad-hoc previews. |
| T-05-SC | Tampering | `vercel` npm package install | high | mitigate | Blocking-human package-legitimacy checkpoint (Task 1) before install, per the mandatory gate — never auto-approved even in auto-mode |
</threat_model>

<verification>
- Two dedicated Neon branches (`staging`, `preview`) exist with unique credentials
- `staging` git branch is pushed, has a confirmed real Vercel deployment, and a discovered stable domain
- `vercel env ls` confirms Production/Preview/Staging scoping is correct (not assumed)
- `scripts/verify-auth-flow.mjs` passes against the real staging URL and its real staging database
- `DEPLOYMENT.md` exists with real values, not placeholders
</verification>

<success_criteria>
- A persistent staging URL backed by its own Neon branch is live, reachable, and shows its own isolated data (DEPLOY-01)
- Environment variables are confirmed — not assumed — correctly scoped across Production/Preview/Staging (DEPLOY-02)
- The feature-branch → staging leg of the release procedure has been genuinely exercised once, end to end, with a passing automated auth proof (DEPLOY-04)
</success_criteria>

<output>
Create `.planning/phases/05-deploy-pipeline-environment-config/05-04-SUMMARY.md` when done
</output>
