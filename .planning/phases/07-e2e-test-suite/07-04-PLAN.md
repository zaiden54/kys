---
phase: 07-e2e-test-suite
plan: 04
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - e2e/pie-chart.spec.ts
  - e2e/pwa.spec.ts
autonomous: true
requirements: [E2E-04]
user_setup: []

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A Playwright test navigates to the home screen after the authenticated fixture user has salary/bonus/vacation data and confirms the annual pie chart renders exactly two Recharts slices (Налог, На руки) with visible ruble/percent figures (E2E-04)"
    - "A Playwright test fetches /manifest.webmanifest directly and confirms its JSON contains name/short_name, start_url '/', display 'standalone', and at least a 192x192 and a 512x512 icon entry (E2E-04)"
    - "A Playwright test confirms the InstallBanner is visible on first load (the test browser is never in standalone display mode) and that dismissing it hides it and the dismissal persists across a reload (E2E-04)"
  artifacts:
    - e2e/pie-chart.spec.ts
    - e2e/pwa.spec.ts
  key_links:
    - "e2e/pie-chart.spec.ts's `authenticated` project session depends on playwright.config.ts's `webServer.command` running the production build (Plan 07-01 Task 2's `npm run build --webpack && npm run start` choice) so Serwist's service worker is actually registered for this plan's PWA assertions — this plan does not re-decide that, it relies on it"

flagged_assumptions:
  - "Nyquist spec-less fallback: the deterministic edge-probe classified E2E-04 as 'unclassified/unresolved' (Russian-language E2E-testing requirements don't fit its shape rules). E2E-04's real acceptance criteria are captured in must_haves.truths above, sourced directly from ROADMAP.md's Phase 7 Success Criterion 3 and 07-CONTEXT.md, not from the probe."
  - "This plan's PWA coverage is scoped to the manifest content and the InstallBanner's own UI logic (visible-when-not-standalone, dismiss-persists) — it does NOT attempt to simulate an actual 'Add to Home Screen' OS-level action (no browser automation API exists for that on iOS Safari, and CONTEXT.md's own scope note confirms this app never calls the Chrome-style `beforeinstallprompt` API since InstallBanner is static instructional text, not a native install-prompt capture). Recorded here so this scoping reads as a deliberate, explained boundary rather than a silently narrower test than E2E-04's wording might suggest."
---

<objective>
Cover E2E-04 end to end: the annual pie-chart summary renders the correct two-slice gross/tax/net breakdown for the authenticated fixture user's actual data, and the PWA installability surface (manifest content + InstallBanner UI behavior) is verified.

Purpose: the annual summary is the other calculation-heavy screen in this app (alongside the next-payment forecast) and shares the same `computeAnnualSummary`/`getCumulativeIncomeBeforeDate` chokepoint that E2E-01/02/03 already exercise piecemeal — this plan is the one place that proves the FULL-YEAR aggregate is also correct end to end. The PWA checks protect the one non-calculation requirement this app has (installability on iOS, per CLAUDE.md's core PWA constraint).
Output: `e2e/pie-chart.spec.ts`, `e2e/pwa.spec.ts`.
</objective>

<execution_context>
@/home/zaiden/.claude/gsd-core/workflows/execute-plan.md
@/home/zaiden/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-e2e-test-suite/07-CONTEXT.md
@.planning/phases/07-e2e-test-suite/07-PATTERNS.md
@src/app/(app)/page.tsx
@src/components/annual-pie-chart.tsx
@src/components/install-banner.tsx
@src/lib/use-standalone.ts
@src/app/manifest.ts
@next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annual pie-chart data verification</name>
  <files>e2e/pie-chart.spec.ts</files>
  <read_first>
    src/components/annual-pie-chart.tsx (renders exactly 2 `<Cell>` slices inside one `<PieChart>` — "Налог" and "На руки", never a third gross wedge; below the chart, a `<dl>` shows "Грязными"/"Налог"/"На руки" rows each as `{formatKopecks(...)} · {percent}%`; the top-level gross figure also appears as its own `<p>` above the chart)
    src/app/(app)/page.tsx (the pie chart only renders when `annualResult.configured` is true, i.e. the authenticated user already has salary+schedule configured — true for the `auth.setup.ts` fixture user since onboarding fills SalaryForm/ScheduleForm before skipping YtdForm)
    Recharts renders SVG — do not attempt to assert on individual `<Cell>` fill colors or pixel geometry; assert on the surrounding DOM text (the `<dl>` figures) and on the presence of exactly 2 `path`/`Cell`-equivalent SVG sector elements within the chart's `<svg>` if a structural check is wanted (e.g. `page.locator("svg path.recharts-sector")` — verify the actual class Recharts 3.10.1 emits by inspecting the rendered DOM once locally rather than guessing, since Recharts internal class names have changed across major versions)
  </read_first>
  <action>
    Create `e2e/pie-chart.spec.ts`. Picked up by the `authenticated` project (Plan 07-01), no storageState override needed.
    Before asserting on the chart, first add at least one bonus and one vacation for the authenticated fixture user via the real UI (reuse the same field-filling steps documented in Plans 07-02/03's `<read_first>` sections — a bonus dated this year, a vacation dated this year) so the annual summary has non-trivial, non-zero gross/tax/net figures to check, rather than asserting only against whatever bare salary the `auth.setup.ts` fixture already configured.
    Test "renders the annual pie chart with matching gross/tax/net figures": `page.goto("/")` -> assert the "Годовая сводка" heading is visible -> assert the `<dl>`'s "Грязными"/"Налог"/"На руки" rows each contain a RUB-currency-formatted, non-zero value -> assert `grossKopecks` (the "Грязными" figure, parsed back from its formatted string via a small helper that strips non-breaking spaces/₽/thousand separators) is greater than or equal to both the "Налог" and "На руки" figures, and that Налог + На руки sum to Грязными within a 1-ruble rounding tolerance (rounding tolerance, not exact equality, since ст.52 rounding can leave a 1-kopeck-class residual across three independently-formatted whole-ruble figures) — this is the one place in the suite that structurally proves the "exactly two slices, no double-counting" invariant `04-01-PLAN.md`'s own code comment calls out, rather than trusting the component's comment.
  </action>
  <verify>
    <automated>npm run test:e2e -- e2e/pie-chart.spec.ts</automated>
  </verify>
  <done>The pie-chart test passes; it independently re-derives from the rendered DOM text that gross = tax + net (within rounding tolerance), proving the two-slice invariant rather than trusting it.</done>
</task>

<task type="auto">
  <name>Task 2: PWA manifest and install-banner behavior</name>
  <files>e2e/pwa.spec.ts</files>
  <read_first>
    src/app/manifest.ts (served at the Next.js-generated `/manifest.webmanifest` route, NOT `/manifest.json` — confirm this exact path by checking the response of a direct `page.request.get("/manifest.webmanifest")` locally before hardcoding it, since Next.js's metadata-route naming convention is version-specific and this project is on Next 16)
    src/components/install-banner.tsx (dismissal persisted via `window.localStorage` key `__pwa_install_banner_dismissed`; dismiss button `aria-label="Скрыть"`; the banner is unconditionally visible whenever `useIsStandalone()` is false AND the dismissed flag is false — a fresh Playwright browser context is never in standalone display mode, so the banner should be visible on first load for a freshly-created page/context)
    src/lib/use-standalone.ts (standalone detection via `navigator.standalone` (iOS-only) or `matchMedia("(display-mode: standalone)")` — Playwright's default Chromium context matches neither, so `isStandalone` is reliably `false` in this suite; no special viewport/emulation setup is needed to get InstallBanner into its "visible" state)
  </read_first>
  <action>
    Create `e2e/pwa.spec.ts`. Uses the `authenticated` project's storageState so the InstallBanner test runs against the real home screen (`/`) where it's actually rendered (`InstallBanner` appears on both the configured and not-configured home-screen branches per `src/app/(app)/page.tsx`).
    Test "manifest.webmanifest is served with the correct installability metadata": `const res = await page.request.get("/manifest.webmanifest"); const manifest = await res.json();` -> assert `res.ok()` -> assert `manifest.name` and `manifest.short_name` are non-empty strings -> assert `manifest.start_url === "/"` -> assert `manifest.display === "standalone"` -> assert `manifest.icons` contains at least one entry with `sizes: "192x192"` and one with `sizes: "512x512"`.
    Test "install banner is visible and dismissal persists across reload": `page.goto("/")` -> assert the "Установить приложение" heading is visible -> click the button with `aria-label="Скрыть"` -> assert the banner is no longer visible -> `page.reload()` -> assert the banner is STILL not visible (proves the localStorage-backed persistence, not just the in-memory React state) -> as cleanup for this shared-session file, clear the `__pwa_install_banner_dismissed` localStorage key at the end of the test (via `page.evaluate(() => localStorage.removeItem("__pwa_install_banner_dismissed"))`) so a later local re-run of this same authenticated session doesn't start with the banner already dismissed.
  </action>
  <verify>
    <automated>npm run test:e2e -- e2e/pwa.spec.ts</automated>
  </verify>
  <done>Both tests in e2e/pwa.spec.ts pass; the manifest test proves the served JSON (not just the source file) has the fields iOS Safari's "Add to Home Screen" flow needs, and the banner test proves dismissal survives a real page reload via localStorage, not just React state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary is introduced by this plan — it reads an already-public, unauthenticated-safe route (`/manifest.webmanifest` carries no user data) and exercises client-only localStorage state. Recorded per-plan for traceability.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-07 | Information Disclosure | `/manifest.webmanifest` fetched directly via `page.request.get` | low | accept | The manifest is a public, unauthenticated Next.js metadata route by design (needed for iOS's "Add to Home Screen" to work at all) — it carries no user-specific data, only static app metadata |

</threat_model>

<verification>
`npm run test:e2e -- e2e/pie-chart.spec.ts e2e/pwa.spec.ts` passes. `npx playwright test --list` shows both files' tests under the `authenticated` project.
</verification>

<success_criteria>
E2E-04 is fully automated: the annual pie chart's rendered figures are independently checked for the gross=tax+net invariant, the manifest is verified by fetching the real served endpoint (not just reading the source file), and the install-banner's localStorage-backed dismissal is proven to survive a reload.
</success_criteria>

<output>
Create `.planning/phases/07-e2e-test-suite/07-04-SUMMARY.md` when done
</output>
