---
phase: 07-e2e-test-suite
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - e2e/vacation.spec.ts
autonomous: true
requirements: [E2E-03]
user_setup: []

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A Playwright test creates a vacation through the real /vacations form and confirms the calculated отпускные (vacation pay) amount is displayed in the history row (E2E-03)"
    - "A Playwright test attempts to create a second, date-overlapping vacation and confirms the exact server-validated error message 'Даты пересекаются с существующим отпуском' is shown inline, and that no new row is created (E2E-03)"
    - "A Playwright test edits a vacation's dates through the real inline edit form and confirms the day count and отпускные amount both update (E2E-03)"
    - "A Playwright test deletes a future vacation (accepting the native window.confirm dialog) and confirms it disappears from the history list (E2E-03)"
  artifacts:
    - e2e/vacation.spec.ts
  key_links:
    - "e2e/vacation.spec.ts's `authenticated` project session (playwright/.auth/user.json from Plan 07-01) is the only auth path this file uses — it never re-implements login"

flagged_assumptions:
  - "Nyquist spec-less fallback: the deterministic edge-probe classified E2E-03 as 'unclassified/unresolved' (Russian-language E2E-testing requirements don't fit its shape rules). E2E-03's real acceptance criteria are captured in must_haves.truths above, sourced directly from ROADMAP.md's Phase 7 Success Criterion 2 and 07-CONTEXT.md, not from the probe."
---

<objective>
Cover E2E-03 end to end: add, edit, and delete a vacation through the real UI, including the overlap-date rejection path, and prove the отпускные (average-earnings vacation pay) calculation shown in the UI reflects the real domain engine's output for each mutation.

Purpose: `calculateVacationPayGross` is one of this app's two highest-risk pure calculations (per CLAUDE.md's stack rationale); a UI-level regression here would silently show a wrong отпускные figure to a real user without any test catching it. The overlap-validation path is also the app's only cross-record business-rule rejection surface currently exercised end to end.
Output: `e2e/vacation.spec.ts`.
</objective>

<execution_context>
@/home/zaiden/.claude/gsd-core/workflows/execute-plan.md
@/home/zaiden/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-e2e-test-suite/07-CONTEXT.md
@.planning/phases/07-e2e-test-suite/07-PATTERNS.md
@src/app/(app)/vacations/page.tsx
@src/app/(app)/vacations/vacation-form.tsx
@src/app/(app)/vacations/vacation-row.tsx
@src/app/actions/vacation.test.ts
@src/domain/money.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create-vacation flow and overlap validation</name>
  <files>e2e/vacation.spec.ts</files>
  <read_first>
    src/app/(app)/vacations/vacation-form.tsx (field ids `startDate`/`endDate`; live day-count preview text `"N день/дня/дней отпуска"` via `pluralizeRu`, only rendered when `endDate >= startDate` and both are valid ISO dates; success text "Отпуск записан."; overlap error surfaces via `setError("endDate", ...)`, rendered as `errors.endDate.message` directly under the endDate field)
    src/app/(app)/vacations/page.tsx (rows render inside a `<ul>` under "История отпусков"; each row shows отпускные as `formatKopecks(grossKopecks)` in the 4th grid column, or the literal fallback text "Укажите оклад, чтобы увидеть сумму" when `grossKopecks` is null — this fallback should NOT appear once a salary exists, which the `authenticated` fixture user has from `auth.setup.ts`'s onboarding step)
    src/app/actions/vacation.test.ts (exact overlap error string "Даты пересекаются с существующим отпуском", exact blocked-delete string "Нельзя удалять отпуска из прошлого. Вы можете изменить даты." — this spec asserts these verbatim in the DOM, it does not invent new wording)
  </read_first>
  <action>
    Create `e2e/vacation.spec.ts` importing `test`/`expect` from `@playwright/test`. No storageState override needed (picked up by the `authenticated` project from Plan 07-01).
    Test "creates a vacation and shows the calculated payout": `page.goto("/vacations")` -> pick a `startDate`/`endDate` pair at least ~14 days in the future and spanning multiple days (e.g. start +30 days, end +34 days from today, computed in the test) so the day-count preview and отпускные amount are both non-trivial and never collide with dates other tests in this file use -> fill and submit -> assert success text "Отпуск записан." -> assert a new row appears showing the entered start/end dates (formatted via `formatIsoDateRu` — confirm its exact output shape, likely `DD.MM.YYYY`, by reading `src/domain/time.ts` if the exact format string is needed for an assertion, otherwise assert on visible day-count text and a non-empty RUB-currency-formatted отпускные figure rather than a hardcoded date string) and a отпускные amount matching the RUB-currency regex (not the "Укажите оклад..." fallback text).
    Test "rejects an overlapping vacation": using the SAME start/end dates just created above (guaranteed overlap), submit the form again -> assert the exact inline error text "Даты пересекаются с существующим отпуском" appears near the `endDate` field -> assert the history list still shows exactly one row for that date range (the second submission created nothing).
  </action>
  <verify>
    <automated>npm run test:e2e -- e2e/vacation.spec.ts -g "creates a vacation|rejects an overlapping"</automated>
  </verify>
  <done>Both tests pass; the overlap test proves the exact server-validated error string renders in the UI and no duplicate row is created.</done>
</task>

<task type="auto">
  <name>Task 2: Edit and delete flows (window.confirm handling)</name>
  <files>e2e/vacation.spec.ts</files>
  <read_first>
    src/app/(app)/vacations/vacation-row.tsx ("Изменить отпуск"/"Удалить отпуск" buttons; edit mode exposes only `startDate`/`endDate` date inputs, no visible label text change needed since both retain their `type="date"` inputs without ids duplicated from the create form — use `page.locator("li").filter(...)`-scoped locators or Playwright's row-scoped `getByRole("listitem")` to avoid ambiguity between the create form's `startDate` input and an edit-mode row's `startDate` input, both present on the page simultaneously once a row is in edit mode; delete triggers `window.confirm(`Удалить отпуск с ${...} по ${...}?`)` — same dialog-handling requirement as bonus-row.tsx, register `page.on("dialog", (dialog) => dialog.accept())` before the delete click)
  </read_first>
  <action>
    Add a test "edits a vacation's dates and updates the payout": create a fresh vacation (its own distinct future date range, e.g. +60/+63 days, to avoid colliding with Task 1's rows), click that row's "Изменить отпуск", change `endDate` to extend the range by a few days (scope the locator to the specific `<li>` containing this vacation's original dates so the create-form's identically-named `startDate`/`endDate` inputs are never accidentally targeted), submit, assert the row now shows the new day count and an updated (larger) отпускные amount than before.
    Add a test "deletes a future vacation": create a fresh vacation dated far enough in the future to be unambiguously deletable (per vacation.test.ts's future-only delete rule), register the dialog-accept handler, click "Удалить отпуск", assert the row is gone from the history list (or the "Нет отпусков" empty state appears if it was the only row).
    As in Plan 07-02, all tests in this file use distinct non-overlapping date ranges and are not individually cleaned up (the shared `authenticated` session's user persists for the run) — an explicit, accepted trade-off, not a bug.
  </action>
  <verify>
    <automated>npm run test:e2e -- e2e/vacation.spec.ts</automated>
  </verify>
  <done>All four tests in e2e/vacation.spec.ts (create, overlap-reject, edit, delete) pass in a single `npm run test:e2e -- e2e/vacation.spec.ts` run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary is introduced by this plan — it exercises the same Server Action / session / database boundaries Plan 07-01 and prior phases (03-vacation-pay) already threat-modeled. Recorded per-plan for traceability, not because new mitigation work is needed here.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-06 | Tampering | Vacation create/edit/delete Server Actions (incl. `checkOverlapVacations`) exercised through the real UI by an automated session | low | accept | Same already-mitigated, already-userId-scoped boundaries covered by Phase 3's unit tests (`src/app/actions/vacation.test.ts`) — this plan adds UI-level coverage, not a new boundary |

</threat_model>

<verification>
`npm run test:e2e -- e2e/vacation.spec.ts` passes end to end (all 4 tests). `npx playwright test --list` shows all 4 tests under the `authenticated` project.
</verification>

<success_criteria>
E2E-03 is fully automated: vacation create/edit/delete and overlap-rejection are each verified through the real UI, with the отпускные figure asserted against the real domain engine's output, not a hardcoded expectation.
</success_criteria>

<output>
Create `.planning/phases/07-e2e-test-suite/07-03-SUMMARY.md` when done
</output>
