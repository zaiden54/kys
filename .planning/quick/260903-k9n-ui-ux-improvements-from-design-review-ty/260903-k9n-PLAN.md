---
phase: quick/260903-k9n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/next-payment-card.tsx
  - src/components/annual-pie-chart.tsx
  - src/app/globals.css
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/register/page.tsx
  - src/app/(app)/vacations/page.tsx
  - src/app/(app)/page.tsx
  - src/app/(app)/error.tsx
  - src/app/(app)/settings/salary/page.tsx
  - src/app/(app)/onboarding/page.tsx
  - src/app/(app)/bonuses/page.tsx
  - src/components/pay-setup-forms.tsx
  - src/components/install-banner.tsx
  - src/components/ytd-estimate-banner.tsx
autonomous: true

must_haves:
  truths:
    - "Home screen's take-home figure visually dominates NextPaymentCard (42px vs. shared 28px display token) with clear separation from the breakdown block below it."
    - "Every page's <h1> (login, register, onboarding, settings/salary, vacations, bonuses, home-screen empty/error states) renders in the Georgia display typeface."
    - "AnnualPieChart's tax/net wedges resolve to the app's real --color-destructive / --color-accent tokens (light+dark mode correct), not hardcoded hex."
    - "NextPaymentCard is visually distinguishable from every other bordered card via an accent-tinted border."
    - "SalaryForm, ScheduleForm, and YtdForm each render as a distinct grouped card on both onboarding and settings/salary."
    - "At >=1100px the home page lays out NextPaymentCard and AnnualPieChart side-by-side inside a ~896px container instead of one narrow stacked column; unchanged below 1100px."
  artifacts:
    - src/components/next-payment-card.tsx
    - src/components/annual-pie-chart.tsx
    - src/app/globals.css (.next-payment-card rule)
    - src/components/pay-setup-forms.tsx
    - src/app/(app)/page.tsx
  key_links:
    - "next-payment-card.tsx's <section> className includes next-payment-card, whose globals.css rule sets border-color via color-mix(--color-accent)."
    - "annual-pie-chart.tsx's two <Cell> fill props reference var(--color-destructive) / var(--color-accent) directly, no TAX_COLOR/NET_COLOR constants remain."
    - "pay-setup-forms.tsx's three <form> elements carry the shared card className, consumed unchanged by both onboarding/page.tsx and settings/salary/page.tsx."
    - "page.tsx's configured-branch wrapper carries min-[1100px]:max-w-4xl and a min-[1100px]:grid grid-cols-2 inner wrapper around NextPaymentSection + AnnualSummarySection/fallback."
---

<objective>
Implement the design-review UI/UX pass approved by the user: typography hierarchy (hero
number scale + system-wide display-font H1s), correct chart token colors, visual emphasis
for the home screen's hero card, grouped setup forms, and a two-column desktop layout for
the home page. Pure presentational change — no tax/money calculation logic, Server Actions,
or database code is touched.

Goal-backward truths this plan must make true:
- The take-home amount on the home screen visually dominates the card as its thesis, not
  just another line of text.
- Every page's `<h1>` (login, register, onboarding, settings/salary, vacations, bonuses,
  home-screen empty/error states) renders in the Georgia display typeface, not the plain
  sans body font.
- The annual pie chart's tax/net wedge colors are driven by the app's real `--color-destructive`
  / `--color-accent` design tokens (light/dark-mode correct), not hardcoded hex values that
  never matched them.
- NextPaymentCard is visually distinguishable from every other bordered card in the app as
  the home screen's focal point.
- The three onboarding/settings forms (SalaryForm, ScheduleForm, YtdForm) read as three
  distinct grouped sections, not one undifferentiated wall of fields.
- At desktop widths (>=1100px, matching the existing sidebar-expansion breakpoint), the home
  page's NextPaymentCard and AnnualPieChart lay out side-by-side instead of leaving a large
  empty area next to the sidebar; mobile/tablet layout is unchanged.

Deferred (explicit, per task instructions): replacing the "Оклад и выплаты" nav icon
(SettingsIcon in src/components/app-navigation.tsx) is optional/low-priority and is NOT
included in this plan to stay within quick-task scope. Not a gap — instructed to skip if the
plan is already at capacity.

Purpose: Close the gap between the shipped v1.1 visual redesign and the design review's
concrete typography/color/emphasis/layout findings.
Output: Updated components/pages listed in `files_modified`, verified via automated
build/lint/test plus a scripted dev-server visual check.
</objective>

<context>
@/home/zaiden/code/kys/src/app/globals.css
@/home/zaiden/code/kys/src/components/next-payment-card.tsx
@/home/zaiden/code/kys/src/components/annual-pie-chart.tsx
@/home/zaiden/code/kys/src/components/pay-setup-forms.tsx
@/home/zaiden/code/kys/src/app/(app)/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero number scale, accent card identity, and real chart color tokens</name>
  <files>src/components/next-payment-card.tsx, src/components/annual-pie-chart.tsx, src/app/globals.css</files>
  <action>
    In next-payment-card.tsx, on the hero take-home `<p>` (the element carrying
    `font-[family-name:var(--font-family-display)] text-[length:var(--font-size-display)]
    font-[number:var(--font-weight-display)] leading-[var(--line-height-display)]
    text-[color:var(--color-accent)] tabular-nums`), replace only the
    `text-[length:var(--font-size-display)]` fragment with an explicit `text-[42px]`
    arbitrary value so the figure reads visibly larger than the shared 28px display token
    used everywhere else — keep the family/weight/line-height tokens unchanged so the
    typeface system itself is untouched, only the size scales up for this one element.
    Increase separation from the breakdown block below it: change `mt-6` to `mt-10` on all
    three conditional breakdown containers that follow the hero figure (the vacation-kind
    `<div>`, the `breakdown` `<dl>`, and the fallback `<dl>` — all three currently start
    `mt-6 flex`/`mt-6 grid`), so the hero reads as a distinct block above a secondary
    content group regardless of which forecast kind is rendered.
    Give NextPaymentCard a subtle, reusable accent identity via a new named CSS class
    rather than an inline color-mix arbitrary value, matching this file's existing
    convention of component-scoped classes in globals.css (`.brand-mark`, `.sign-out-button`,
    `.mobile-nav-link`, etc.) consumed via className in TSX: add a `.next-payment-card` rule
    to globals.css (place it near `.brand-mark`, which already uses the same
    `color-mix(in srgb, var(--color-accent) …%, …)` pattern this reuses) that sets
    `border-color` to `color-mix(in srgb, var(--color-accent) 35%, var(--color-tertiary-surface))`.
    Because `@import "tailwindcss";` is the first line of globals.css and this new rule is
    declared later in the same file, it naturally wins the cascade over the Tailwind
    utility layer at equal specificity — no `!important` needed. Add `next-payment-card` to
    the className list on next-payment-card.tsx's root `<section>`, keeping every existing
    Tailwind utility class (`rounded-[12px] border border-[color:var(--color-tertiary-surface)]
    bg-[color:var(--color-secondary)] p-6`) exactly as-is; the new class only overrides the
    border color, nothing else — one accessory, not a redesign.
    In annual-pie-chart.tsx, delete the hardcoded `TAX_COLOR` (`#dc2626`) and `NET_COLOR`
    (`#16a34a`) constants entirely and pass `fill="var(--color-destructive)"` on the "tax"
    `<Cell>` and `fill="var(--color-accent)"` on the "net" `<Cell>` directly. Recharts
    forwards `fill` straight to the SVG `fill` attribute, and since this `<PieChart>` renders
    as inline SVG in the live DOM (not an external `.svg` file), `fill="var(...)"` resolves
    against the real CSS custom properties at render time — the wedges now automatically
    follow the light/dark-mode color fork already defined in globals.css instead of staying
    pinned to Tailwind's `red-600`/`green-600` hex values that never matched the design
    tokens in either mode.
  </action>
  <verify>
    <automated>npx vitest run src/components/next-payment-card.render.test.tsx src/components/annual-pie-chart.render.test.tsx && ! grep -q "TAX_COLOR\|NET_COLOR" src/components/annual-pie-chart.tsx && grep -q "next-payment-card" src/app/globals.css</automated>
  </verify>
  <done>Hero take-home figure renders at 42px (up from the shared 28px display token) with mt-10 separation above the breakdown block; NextPaymentCard's root section carries a `next-payment-card` class whose globals.css rule tints its border toward `--color-accent` via `color-mix`; AnnualPieChart's two `<Cell>` elements use `fill="var(--color-destructive)"` / `fill="var(--color-accent)"` with no hardcoded hex color left in the file; both existing render test files still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Display typeface on every page H1 + card-grouped setup forms</name>
  <files>src/app/(auth)/login/page.tsx, src/app/(auth)/register/page.tsx, src/app/(app)/vacations/page.tsx, src/app/(app)/page.tsx, src/app/(app)/error.tsx, src/app/(app)/settings/salary/page.tsx, src/app/(app)/onboarding/page.tsx, src/app/(app)/bonuses/page.tsx, src/components/pay-setup-forms.tsx</files>
  <action>
    Add `font-[family-name:var(--font-family-display)]` into the className of every `<h1
    className="text-[length:var(--font-size-display)] font-[number:var(--font-weight-display)]
    text-[color:var(--color-text-primary)]">` occurrence across these files — the exact same
    arbitrary-value class fragment already applied to next-payment-card.tsx's hero number —
    so the Georgia display typeface actually functions as a page-level typographic system
    instead of a one-off on a single number. Sites (grep-confirmed, all following the
    identical className pattern above): login/page.tsx line 54, register/page.tsx line 52,
    vacations/page.tsx line 85, page.tsx lines 73 and 94 (both the "Не удалось загрузить
    данные" and the missing-salary/schedule empty-state headings), error.tsx line 23,
    settings/salary/page.tsx line 28, onboarding/page.tsx line 27, bonuses/page.tsx line 40.
    Do not touch any `<h2>`/`<h3>` in these files or in pay-setup-forms.tsx — only the 9
    `<h1>` elements matching this exact pattern.
    In pay-setup-forms.tsx, give each of the three exported forms' root `<form>` element the
    same card container treatment already used throughout the app (e.g. NextPaymentCard,
    AnnualPieChart): append `rounded-[12px] border border-[color:var(--color-tertiary-surface)]
    bg-[color:var(--color-secondary)] p-5` onto the existing `flex flex-col gap-3` className
    on SalaryForm's `<form>` (line 126), ScheduleForm's `<form>` (line 252), and YtdForm's
    `<form>` (line 377). Both onboarding/page.tsx and settings/salary/page.tsx render
    `<SalaryForm>`, `<ScheduleForm>`, `<YtdForm>` directly inside a shared `flex flex-col
    gap-8` wrapper with only an `<h2>` between them and no per-form container today —
    changing the shared component (not either call site) is what makes both pages read as
    three distinct grouped sections consistently, with zero edits needed to either page file.
  </action>
  <verify>
    <automated>test "$(grep -rn 'font-\[family-name:var(--font-family-display)\]' src/app | wc -l)" -eq 9 && test "$(grep -c 'rounded-\[12px\]' src/components/pay-setup-forms.tsx)" -eq 3 && npm run typecheck</automated>
  </verify>
  <done>All 9 `<h1>` sites across login, register, onboarding, settings/salary, vacations, bonuses, and the home page's empty/error states render in the Georgia display typeface; SalaryForm, ScheduleForm, and YtdForm each render inside their own rounded/bordered/secondary-background card in both onboarding and settings/salary; `npm run typecheck` passes with zero errors.</done>
</task>

<task type="auto">
  <name>Task 3: Two-column desktop grid for the home page</name>
  <files>src/app/(app)/page.tsx, src/components/next-payment-card.tsx, src/components/annual-pie-chart.tsx, src/components/install-banner.tsx, src/components/ytd-estimate-banner.tsx</files>
  <action>
    In page.tsx's "configured" success branch (the final `return` block, currently a `<div
    className="flex flex-1 flex-col items-center gap-4 px-6 py-16">` containing
    `InstallBanner`, the conditional `YtdEstimateBanner`, the `NextPaymentSection` Suspense,
    and the `annualResult.configured` ternary), add `min-[1100px]:max-w-4xl` to that outer
    wrapper's className so the whole content column widens to roughly 896px at desktop —
    matching the app's existing 1100px sidebar-expansion breakpoint already defined in
    globals.css's `@media (min-width: 1100px)` block, not Tailwind's default `lg:` (1024px).
    Wrap the two payment/chart sections — the `NextPaymentSection` `<Suspense>` block and the
    entire `annualResult.configured ? (...) : (...)` ternary (which renders either
    `AnnualSummarySection`'s `<Suspense>` or the "Сводка недоступна" fallback `<div>`) — in a
    new `<div className="flex w-full flex-col gap-4 min-[1100px]:grid min-[1100px]:grid-cols-2
    min-[1100px]:items-start">` so at >=1100px the two sections lay out side-by-side instead
    of stacked, while staying a single vertical column below that breakpoint exactly as
    today. `InstallBanner` and the conditional `YtdEstimateBanner` stay direct children of
    the outer wrapper, above this new grid `<div>`, so they remain full-width across the top
    unchanged.
    `NextPaymentCard`, `AnnualPieChart`, `InstallBanner`, and `YtdEstimateBanner` each
    hardcode `max-w-sm` on their own root element — without relaxing that cap, the new grid
    columns above would still visibly clip each card at 384px inside a much wider column.
    Add `min-[1100px]:max-w-none` immediately after each component's existing `max-w-sm` in
    its root className: next-payment-card.tsx line 25 (`<section>`), annual-pie-chart.tsx
    line 43 (`<section>`), install-banner.tsx line 75 (`<div>`), ytd-estimate-banner.tsx line
    20 (its root element) — so every one of these four components fills its container's
    width at >=1100px, and only the mobile/tablet `max-w-sm` cap remains below that
    breakpoint. Also add the same `min-[1100px]:max-w-none` to the "Сводка недоступна"
    defensive fallback `<div>` inside page.tsx (currently `w-full max-w-sm rounded-[12px]
    border …`) for consistency, even though that branch is unreachable by construction today
    per its own inline comment.
  </action>
  <verify>
    <automated>test "$(grep -c 'min-\[1100px\]:grid' "src/app/(app)/page.tsx")" -ge 1 && npm run build</automated>
  </verify>
  <done>At viewport widths >=1100px, NextPaymentCard and AnnualPieChart (or its "Сводка недоступна" fallback) render side-by-side inside a ~896px-wide container instead of stacked at max-w-sm each; below 1100px the layout is pixel-identical to today's single centered column; `npm run build` completes with no type or build errors.</done>
</task>

</tasks>

<threat_model>
No new trust boundary is introduced by this plan. Every change is a static Tailwind
className addition, a Recharts `fill` prop pointed at an existing CSS custom property, or a
new globals.css rule referencing existing design tokens — no new user input is accepted, no
Server Action, route handler, database query, or auth path is touched, and no new
network/file/package surface is added. STRIDE register intentionally omitted as
inapplicable; the constraint "do not touch tax/money calculation logic, Server Actions, or
database code" (see plan objective) is the controlling scope boundary here.
</threat_model>

<verification>
Run after all three tasks are committed, as the whole-plan regression sweep:

1. **Automated:** `npm run lint && npm run typecheck && npm run test && npm run build` — all
   four must pass with zero new errors/warnings introduced by this plan's edits.
2. **Scripted visual sanity check (dev server):** Start `npm run dev` (per
   `.claude/launch.json`, port 3000; requires a working `DATABASE_URL` in `.env.local` — this
   project's existing e2e suite already depends on the same local setup). Write a throwaway
   Node/Playwright script — not committed, delete it when done — that reuses this project's
   existing disposable-test-user pattern (`uniqueEmail`/`deleteUserByEmail` from
   `e2e/fixtures.ts`, following the same register -> fill SalaryForm/ScheduleForm -> skip
   YtdForm flow already scripted in `e2e/auth.setup.ts`) to: register a fresh disposable
   user; screenshot `/onboarding` at a 390x844 mobile viewport (confirming the three setup
   forms now render as separate bordered cards and the H1 uses the Georgia display font);
   navigate to `/` and screenshot it at 390x844 (mobile — hero number size/spacing, the
   accent-tinted card border, correct red/green chart colors) and at 1280x900 (desktop,
   >=1100px — the two-column NextPaymentCard/AnnualPieChart grid). Read each screenshot with
   the Read tool to visually confirm every change from Tasks 1-3 before considering this plan
   done. Delete the disposable user via `deleteUserByEmail` and stop the dev server
   afterward. If `.env.local`/`DATABASE_URL` is unavailable in this execution environment,
   skip the live screenshot pass and instead record in SUMMARY.md exactly which checks a
   human must run locally before the visual redesign is considered verified — do not
   silently skip without recording it (matches this project's established convention for
   environments without live browser tooling, see STATE.md decision history).
</verification>

<success_criteria>
- All 9 page `<h1>` sites render in `var(--font-family-display)` (Georgia).
- NextPaymentCard's hero take-home figure is visibly larger (42px vs. the shared 28px
  display token) with clearer separation from the breakdown block, and its card carries a
  distinct accent-tinted border via the new `.next-payment-card` class.
- AnnualPieChart's tax/net wedges use `var(--color-destructive)` / `var(--color-accent)`
  with zero hardcoded hex colors remaining in the file.
- SalaryForm, ScheduleForm, and YtdForm each render inside their own card container on both
  onboarding and settings/salary.
- The home page lays out NextPaymentCard and AnnualPieChart side-by-side at >=1100px inside
  a ~896px container, and is unchanged below that breakpoint.
- `npm run lint`, `npm run typecheck`, `npm run test`, and `npm run build` all pass.
- Item 7 (nav icon replacement) is explicitly deferred, not silently dropped — documented in
  this PLAN.md's objective and to be carried into SUMMARY.md as a known deferral, not a gap.
</success_criteria>

<output>
Create `.planning/quick/260903-k9n-ui-ux-improvements-from-design-review-ty/260903-k9n-SUMMARY.md` when done.
</output>
