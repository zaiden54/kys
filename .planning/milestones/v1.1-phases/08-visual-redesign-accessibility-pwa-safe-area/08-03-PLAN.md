---
phase: 08-visual-redesign-accessibility-pwa-safe-area
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/app/(app)/vacations/vacation-row.tsx
  - src/app/(app)/vacations/vacation-form.tsx
  - src/app/(app)/vacations/page.tsx
autonomous: true
requirements: [UI-01, UI-02, UI-05, UI-07]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "UI-01 (vacations screen, verbatim ROADMAP Phase 8 success criterion 1): The vacations screen shows a clear empty/loading/error state instead of a blank screen or a raw technical error. [Spec-less probe fallback note: UI-01 came back `unclassified`/`unresolved` from the deterministic edge-coverage engine — this truth is authored from ROADMAP.md's Phase 8 Success Criteria text plus 08-UI-SPEC.md's approved UI Considerations table.] `src/app/(app)/vacations/page.tsx`'s zero-row state renders the 08-UI-SPEC.md § Copywriting Contract empty-state heading \"Пока нет отпусков\" + body copy pointing at the \"Добавить отпуск\" CTA (replacing the current \"Нет отпусков\" heading — no e2e test asserts the old string, confirmed by grep of `e2e/*.spec.ts`). The list is wrapped in `<Suspense fallback={<SkeletonLoader count={3} variant=\"vacation-row\" />}>` instead of rendering with no loading affordance."
    - "UI-01 (vacations partial-state, per 08-UI-SPEC.md § UI Considerations row \"partial | vacation-list, no salary configured\"): `vacation-row.tsx`'s existing \"Укажите оклад, чтобы увидеть сумму\" fallback (rendered when `grossKopecks === null`) is retained and restyled to `--color-text-secondary`, never silently dropped or replaced with a fabricated ₽0 amount."
    - "UI-02 (vacations screen money/date formatting): Every money amount and date on the vacations screen (`vacation-row.tsx`'s display-mode start/end date, day count, and gross amount) carries the `.tabular-nums` utility class from `globals.css` on top of `formatKopecks()`/`formatIsoDateRu()`'s unchanged output — proven by `grep -c tabular-nums src/app/(app)/vacations/vacation-row.tsx` reporting at least 3 (two dates + amount)."
    - "UI-05/UI-07 (vacations screen visual system + accessibility): `vacation-row.tsx`'s \"Изменить отпуск\"/\"Удалить отпуск\" action links and `vacation-form.tsx`'s submit button carry `focus-visible:outline-2` per 08-UI-SPEC.md's Focus-Visible Outline Pattern; the destructive \"Удалить отпуск\" link uses `--color-destructive`. Every form input in `vacation-form.tsx`/`vacation-row.tsx`'s edit mode keeps a real `<label htmlFor>`/`id` pair."
  artifacts:
    - src/app/(app)/vacations/vacation-row.tsx
    - src/app/(app)/vacations/vacation-form.tsx
    - src/app/(app)/vacations/page.tsx
  key_links:
    - "vacation-row.tsx's native `window.confirm(...)` call inside `onDelete` → e2e/vacation.spec.ts's dialog-handling test — this plan must not touch the confirm() call, its message string, or the `onDelete` control flow, only surrounding className values"
    - "vacation-row.tsx's display-mode `div.grid > span` structure (5 spans in fixed order: startDate, endDate, dayCount, amount, actions) → e2e/vacation.spec.ts's `row.locator(\"div.grid > span\").nth(2)`/`.nth(3)` index-based selectors — the element order and the wrapping `div.grid` classname substring must survive this restyle unchanged, since the test indexes into it positionally, not by role or label"
    - "vacation-row.tsx's edit-mode `input[type=\"date\"]` pair → e2e/vacation.spec.ts's `row.locator('input[type=\"date\"]')` — input `type` attributes are load-bearing and must survive unchanged"
  prohibitions:
    - requirement_id: UI-03
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT modify vacation-row.tsx's window.confirm() delete-confirmation call, its message text, or its control flow — 08-CONTEXT.md explicitly locks this out of UI-03's restyle scope specifically because e2e/vacation.spec.ts's Playwright dialog handler is written against this exact native-dialog behavior."
    - requirement_id: UI-01
      category: safety
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT change the `div.grid > span` element count, order, or wrapping class name that e2e/vacation.spec.ts indexes into positionally (`.nth(2)` for day count, `.nth(3)` for amount) — only className styling values inside those exact five spans may change."
    - requirement_id: UI-05
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT change any value, formula, or branch in src/domain/** or src/lib/db/** or src/app/actions/vacation.ts while restyling — presentation-only pass, guarded by a git status --porcelain check in every task's acceptance criteria."
---

<objective>
Restyle the vacations screen (list-row component, add/edit form, and page composition) onto Plan 08-01's token system: consistent money/date formatting via `tabular-nums`, updated empty-state copy, a Suspense-driven loading skeleton for the vacation list, focus-visible accessibility, and destructive-color treatment for the delete action — while leaving the `window.confirm()` delete flow and, critically, the exact `div.grid > span` positional structure of the display-mode row unchanged, since `e2e/vacation.spec.ts` (Phase 7's regression safety net) indexes into that structure by position (`.nth(2)`/`.nth(3)`), not by role or label.

Purpose: Bring the vacations screen (one of the ROADMAP's five explicitly-named screens for UI-01) up to the new visual system without regressing the E2E coverage that exists specifically to protect this redesign from silent breakage.
Output: A restyled `vacation-row.tsx` (display + edit modes), `vacation-form.tsx`, and `vacations/page.tsx` with a Suspense-driven skeleton loading state and updated empty-state copy.
</objective>

<execution_context>
@/home/zaiden/.claude/gsd-core/workflows/execute-plan.md
@/home/zaiden/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-CONTEXT.md
@.planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-UI-SPEC.md
@.planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle vacation-row.tsx (display + edit modes) and vacation-form.tsx</name>
  <read_first>
    src/app/(app)/vacations/vacation-row.tsx (file being modified — current `zinc-*`/`red-700` display-mode `div.grid` with 5 direct `<span>` children in fixed order [startDate, endDate, dayCount, amount, actions], edit-mode form with two `input[type="date"]`, `window.confirm(...)` in `onDelete`, `editSessionRef` guard — do not touch any JS logic, only `className` strings, and never reorder/add/remove the 5 display-mode spans)
    src/app/(app)/vacations/vacation-form.tsx (file being modified — current `zinc-*`/`red-600`/`green-700` form, `startDate`/`endDate` date inputs, `dayCount` live-preview paragraph)
    .planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-PATTERNS.md § "Bonus/Vacation Row Components" (the pattern generalizes directly — apply the same token substitutions to vacation-row.tsx's 5-span grid), § "Form Fields", § "Focus-Visible Outline Pattern", § "Tabular Numerals for Money & Dates"
    .planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-UI-SPEC.md § Color, § Form Fields, § UI Considerations row "partial | vacation-list, no salary configured"
    e2e/vacation.spec.ts (confirms every selector this task must not break: `row.locator("div.grid > span").nth(2)` for day count, `.nth(3)` for amount, `getByRole("button", { name: "Изменить отпуск" })`/`"Удалить отпуск"`/`"Сохранить отпуск"`/`"Сохранить"`, `row.locator('input[type="date"]')`)
  </read_first>
  <files>src/app/(app)/vacations/vacation-row.tsx, src/app/(app)/vacations/vacation-form.tsx</files>
  <action>
    In `vacation-row.tsx`'s display mode: replace `border-zinc-200` with `border-[color:var(--color-tertiary-surface)]` on the `<li>`. Keep the `div.grid grid-cols-[5.5rem_1fr] ... sm:grid-cols-[6rem_6rem_4rem_7rem_auto]` wrapper class structure exactly (only append token-driven text-color/typography utility classes to it, never remove the `grid` class substring or change the column-template values). The 5 direct `<span>` children, in this exact order, get: (1) startDate span → `text-[color:var(--color-text-secondary)] tabular-nums font-[family-name:var(--font-family-caption)] text-[length:var(--font-size-caption)]`; (2) endDate span → same as (1); (3) dayCount span → `text-[color:var(--color-text-secondary)]`; (4) amount span → `font-semibold text-[color:var(--color-text-primary)]` wrapping, with its inner conditional: when `grossKopecks === null` the "Укажите оклад, чтобы увидеть сумму" fallback text gets `font-normal text-[color:var(--color-text-secondary)]` (retained, restyled, never replaced with a computed value), otherwise the `formatKopecks(grossKopecks)` output gets `tabular-nums`; (5) actions span unchanged structurally — "Изменить отпуск" gets `text-[color:var(--color-text-primary)] underline focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[color:var(--color-accent)]`, "Удалить отпуск" gets `text-[color:var(--color-destructive)] underline disabled:opacity-50 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[color:var(--color-destructive)]`. The inline error `<p>` gets `text-[color:var(--color-destructive)]`.

    In `vacation-row.tsx`'s edit mode: replace every `rounded border border-zinc-300 px-3 py-2` on the two `input[type="date"]` elements with `rounded-[8px] border border-[color:var(--color-tertiary-surface)] bg-[color:var(--color-dominant)] px-3 py-2 text-[color:var(--color-text-primary)] focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[color:var(--color-accent)]` — never change `type="date"`. Submit button (`bg-zinc-900`) → `bg-[color:var(--color-accent)] text-white font-semibold focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[color:var(--color-accent)]`; "Отмена" button → `border border-[color:var(--color-tertiary-surface)] text-[color:var(--color-text-primary)] focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[color:var(--color-accent)]`. Validation error `<p>` elements get `text-[color:var(--color-destructive)]`.

    In `vacation-form.tsx`: apply the same Form Fields pattern to both `startDate`/`endDate` inputs (label, input border/focus, error text) as `vacation-row.tsx`'s edit mode above — do not change `id="startDate"`/`id="endDate"`. The day-count live-preview `<p className="text-xs text-zinc-500">` becomes `text-[length:var(--font-size-caption)] text-[color:var(--color-text-secondary)]`. The success message (`text-green-700`) becomes `text-[color:var(--color-accent)]`. Submit button restyled identically to `vacation-row.tsx`'s edit-mode submit button.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run test:e2e -- e2e/vacation.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:e2e -- e2e/vacation.spec.ts` passes (exercises add, edit with day-count/amount recompute, overlap-validation error, and delete)
    - `grep -c "zinc-\|red-6\|red-7\|green-7" src/app/(app)/vacations/vacation-row.tsx src/app/(app)/vacations/vacation-form.tsx` reports 0 across both files
    - `grep -n 'className="grid grid-cols' src/app/(app)/vacations/vacation-row.tsx` or an equivalent grep confirms the `grid` class substring and column-template arbitrary values are still present unchanged in the display-mode wrapper
    - `grep -c "<span" src/app/(app)/vacations/vacation-row.tsx` reports the same count as before this task (5 in the display-mode block) — no span added or removed
    - `grep -n 'id="startDate"' src/app/(app)/vacations/vacation-form.tsx` and `grep -n 'id="endDate"'` both still match
    - `grep -n "window.confirm" src/app/(app)/vacations/vacation-row.tsx` still matches with the exact same message template
    - `git status --porcelain -- src/domain/ src/lib/db/ src/app/actions/vacation.ts` produces no output
    - `npx tsc --noEmit` reports no new errors
  </acceptance_criteria>
  <done>vacation-row.tsx (both modes) and vacation-form.tsx are fully token-driven with focus-visible accessibility and destructive-color delete styling; the display mode's 5-span grid structure, edit-mode date input types, and window.confirm() flow are byte-for-byte unchanged; e2e/vacation.spec.ts passes against a live dev server.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle vacations/page.tsx — updated empty-state copy + Suspense-driven loading skeleton</name>
  <read_first>
    src/app/(app)/vacations/page.tsx (file being modified — current `zinc-*` composition, "Нет отпусков" heading, `hasSalaryHistory` gate producing `null` grossKopecks per row, no Suspense/loading state)
    src/components/skeleton-loader.tsx (Plan 08-01's new component — `variant="vacation-row"` fallback)
    .planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-PATTERNS.md § "Bonus/Vacation List Pages with Loading Skeletons" (the bonuses-page pattern generalizes directly to vacations)
    .planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-UI-SPEC.md § Copywriting Contract (locked "Пока нет отпусков" pattern), § UI Considerations rows "empty | bonus-list, vacation-list" and "loading | bonus-list, vacation-list..."
    e2e/vacation.spec.ts, e2e/pie-chart.spec.ts (confirm form/row selectors are unaffected by this page-level restructure — those were handled in Task 1)
  </read_first>
  <files>src/app/(app)/vacations/page.tsx</files>
  <action>
    Restructure `vacations/page.tsx` per 08-PATTERNS.md's bonuses-page pattern, adapted for vacations' extra data dependencies (`listVacations`/`listSalaryHistory`/`listBonuses`/`calculateVacationPayGross`): extract the data-fetching + `rows` computation + list-rendering `<section>` into an internal `async function VacationListContent()`, wrapped in the page's default export with `<Suspense fallback={<SkeletonLoader count={3} variant="vacation-row" />}>`. Keep the outer `<h1>`/`<div id="vacation-form"><VacationForm /></div>` structure unchanged around it. Update the empty-state heading from `"Нет отпусков"` to `"Пока нет отпусков"` and restyle its container/CTA identically to Plan 08-02's bonuses-page treatment (`rounded-[12px] border border-[color:var(--color-tertiary-surface)] bg-[color:var(--color-secondary)] p-6`, `href="#vacation-form"` CTA with `bg-[color:var(--color-accent)]`). Restyle the row-grid header and page headings the same way as Plan 08-02's Task 2. The `hasSalaryHistory`/`grossKopecks: null` computation logic must not change — this task only restyles the render output.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build && npm run test:e2e -- e2e/vacation.spec.ts e2e/pie-chart.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `npm run test:e2e -- e2e/vacation.spec.ts e2e/pie-chart.spec.ts` passes (pie-chart.spec.ts also exercises vacation creation as a setup step, confirming the page restructure doesn't break that path)
    - `grep -n "Пока нет отпусков" src/app/(app)/vacations/page.tsx` matches; `grep -n "Нет отпусков" src/app/(app)/vacations/page.tsx` (the old string) does not match
    - `grep -n "Suspense" src/app/(app)/vacations/page.tsx` and `grep -n "SkeletonLoader" src/app/(app)/vacations/page.tsx` both match
    - `grep -c "zinc-" src/app/(app)/vacations/page.tsx` reports 0
    - `git status --porcelain -- src/domain/ src/lib/db/ src/app/actions/vacation.ts` produces no output
  </acceptance_criteria>
  <done>vacations/page.tsx shows a skeleton loading placeholder matching the row shape while data loads, an updated "Пока нет отпусков" empty state, and a fully token-driven page shell; e2e/vacation.spec.ts and e2e/pie-chart.spec.ts both pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `vacation-row.tsx`'s `window.confirm()` → browser's native dialog | Untouched by this plan; restyle must not accidentally wrap or intercept the native confirm call |
| Vacation form/row submit failures → rendered error text | Must stay the existing generic Russian message, never `error.message`/stack from a caught exception |
| `vacation-row.tsx`'s positional `div.grid > span` structure → e2e/vacation.spec.ts's index-based assertions | A structural contract this plan must not silently break while restyling |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-08-08 | Tampering | `vacation-row.tsx`'s `onDelete`/`window.confirm()` flow | high | mitigate | Task 1 explicitly forbids touching this function or its message string; acceptance criteria grep for the unchanged `window.confirm` call; `e2e/vacation.spec.ts`'s delete test is the final proof |
| T-08-09 | Tampering | `vacation-row.tsx`'s display-mode span structure | medium | mitigate | Task 1's acceptance criteria assert the span count is unchanged and the `div.grid` classname substring survives; `e2e/vacation.spec.ts`'s positional `.nth()` assertions are the final proof |
| T-08-10 | Tampering | `src/domain/**`, `src/app/actions/vacation.ts` | critical | mitigate | Both tasks scope their files list to presentation-only files; `git status --porcelain` structural guard in every task |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new package installs — pure Tailwind arbitrary-value restyle |
</threat_model>

<verification>
- `npx tsc --noEmit` — no new errors after either task
- `npm run test:e2e -- e2e/vacation.spec.ts e2e/pie-chart.spec.ts` — full vacation CRUD + overlap-validation + amount-after-edit + pie-chart-setup-via-vacation paths pass
- `npm run build` — production build succeeds
- `git status --porcelain -- src/domain/ src/lib/db/ src/app/actions/vacation.ts` — empty after both tasks
</verification>

<success_criteria>
- Vacation list/form/row screen fully reflects the new token-driven visual system with focus-visible accessibility and destructive-color delete styling
- Empty state updated to the locked "Пока нет отпусков" copy; loading state shows a shape-matched skeleton, never a spinner
- `window.confirm()` delete flow, the 5-span positional row structure, and edit-mode date input types are byte-for-byte unchanged — `e2e/vacation.spec.ts` passes without modification
</success_criteria>

## Artifacts this phase produces

- `src/app/(app)/vacations/vacation-row.tsx` — modified: token-driven restyle of display + edit modes, focus-visible accessibility, destructive-color delete action; zero logic or structural change
- `src/app/(app)/vacations/vacation-form.tsx` — modified: token-driven Form Fields pattern applied to both date inputs; ids unchanged
- `src/app/(app)/vacations/page.tsx` — modified: `VacationListContent` extracted and wrapped in `<Suspense>` with a `SkeletonLoader` fallback; empty-state copy updated to "Пока нет отпусков"

<output>
Create `.planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-03-SUMMARY.md` when done
</output>
</output>