---
phase: 08-visual-redesign-accessibility-pwa-safe-area
plan: 06
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03", "08-04", "08-05"]
files_modified: []
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05, UI-06, UI-07, PWA-01, PWA-02]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Whole-phase regression (all 9 requirement IDs): The full automated test suite (`npm test`), production build (`npm run build`), and full Playwright suite (`npm run test:e2e`) all pass after every Wave-1/Wave-2 plan's changes are combined on the phase branch — this is the final proof that no plan's restyle broke another plan's screen, and that `src/domain/**`/`src/lib/db/**` were never touched anywhere in the phase (verified by `git diff --stat main -- src/domain/ src/lib/db/` against the phase branch's merge-base, per PROJECT.md's frozen-calculation-engine constraint for the whole v1.1 milestone)."
    - statement: "UI Considerations backstop #1 (08-UI-SPEC.md § UI Considerations, 'empty | annual-summary-chart (media)'): a zero-income year (no salary/bonus/vacation configured for the current tax year) renders a distinct empty-state variant on the annual pie chart card, never a degenerate 0%/0% pie slice presented as if it were real data."
      verification: backstop
    - statement: "UI Considerations backstop #2 (08-UI-SPEC.md § UI Considerations, 'overflow | persistent nav header, app name/logo (nav)'): the persistent nav header's app-name link never wraps to two lines or overflows its 56px height at a 375px-wide viewport (iPhone SE, the narrowest common target)."
      verification: backstop
    - "PWA-01/PWA-02 (verbatim ROADMAP Phase 8 success criterion 5, live-device half): On an iPhone with a Dynamic Island, the header/nav never overlaps it or the home indicator. Plan 08-01 already proved `viewport-fit=cover` is configured and `env(safe-area-inset-*)` is applied at the single source-of-truth location (`(app)/layout.tsx`); this plan's human-check confirms the visual non-overlap outcome on a real or simulated notched device, which cannot be verified by any automated command in this environment."
    - "UI-06 (dark/light theme, whole-app visual check): Toggling the OS-level color-scheme preference (or a browser DevTools emulation of `prefers-color-scheme`) while viewing every screen touched in this phase (login, register, home, bonuses, vacations, settings/salary, onboarding) shows the dark-base/light-override token system switching correctly on every screen, with no screen left showing a stale raw Tailwind `zinc-*`/`amber-*` color that ignores the toggle — this is the final holistic proof that Plan 08-01's token system and Plans 08-02..08-05's per-screen migrations compose correctly across the whole app, not just individually."
    - "UI-07 (accessibility, whole-app keyboard pass): Tabbing through every screen with the keyboard only (no mouse) shows a visible 2px accent-colored focus ring with offset on every interactive element in sequence — nav header link, sign-out button, every form input, every submit/cancel/delete button, every list-row action link — with no element silently skipped or showing only a default/invisible browser focus outline."
  artifacts: []
  key_links:
    - "Every Wave-1/Wave-2 plan's individual verify commands → this plan's combined full-suite run — the final integration proof that per-plan verification, run in isolation, also holds true when every plan's changes are merged onto the same branch"
  prohibitions:
    - requirement_id: UI-05
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT close this plan (and therefore the phase) if git diff --stat against src/domain/ or src/lib/db/ shows any change — this is the whole-phase enforcement point for PROJECT.md's 'zero change to calculation logic' constraint; any drift found here blocks phase completion until reverted."
---

<objective>
Run the full-suite regression (unit tests, production build, full Playwright E2E suite) with every Wave-1/Wave-2 plan's changes merged onto the phase branch, then perform the end-of-phase human verification pass this plan set has been accumulating: the Dynamic Island/home-indicator safe-area check (PWA-01/02) that only a real or simulated notched device can prove, a whole-app dark/light OS-toggle visual check (UI-06), a keyboard-only focus-ring Tab-through pass (UI-07), and the two UI-SPEC backstop items (annual-summary-chart's zero-income empty state, nav-header overflow at 375px) that 08-UI-SPEC.md's checker explicitly flagged as needing a held-out visual check rather than an automated assertion.

Purpose: Close the loop on every truth this phase's five prior plans individually claimed, proving they compose correctly when combined, and capture the handful of genuinely device-dependent verifications per `workflow.human_verify_mode=end-of-phase`.
Output: A green full-suite regression run and a completed human-check record covering PWA-01/02, UI-06, UI-07, and both UI-SPEC backstop items.
</objective>

<execution_context>
@/home/zaiden/.claude/gsd-core/workflows/execute-plan.md
@/home/zaiden/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-CONTEXT.md
@.planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Whole-phase regression — unit suite, production build, full E2E suite, calculation-logic drift check</name>
  <precondition>All five of 08-01..08-05's plans have been executed and merged onto the phase branch, and a Neon CI/dev database is reachable for the Playwright suite exactly as Phase 7 configured it (DATABASE_URL set, e2e/ci-branch-setup.mjs or an equivalent local dev-server path available)</precondition>
  <read_first>
    package.json (`"test"`, `"build"`, `"test:e2e"` scripts — unchanged by this phase)
    .planning/phases/07-e2e-test-suite/07-05-SUMMARY.md (confirms the exact CI-isolation mechanism this task's `npm run test:e2e` run should match locally, or how to run the full suite against a local dev server per that phase's established pattern)
  </read_first>
  <files>(none — verification only; no source files modified)</files>
  <action>
    Run `npm test` (full Vitest unit/render suite — every domain, repository, and component render test in the project, not scoped to any single file) and confirm it exits 0. Run `npm run build` and confirm it exits 0 (production build, including the manifest/viewport/icon routes Plan 08-01 touched). Run the full `npm run test:e2e` suite (all five spec files: `auth.spec.ts`, `bonus.spec.ts`, `vacation.spec.ts`, `pie-chart.spec.ts`, `pwa.spec.ts`) against a live dev server and confirm every test passes — this is the first time in the phase all five specs run together against the fully-restyled app rather than individually per plan. Run `git diff --stat $(git merge-base HEAD main) -- src/domain/ src/lib/db/` (or, if the phase branch has already been merged, `git log --oneline -- src/domain/ src/lib/db/` since the phase's first commit) and confirm zero output — the whole-phase proof that no plan drifted into calculation or persistence logic.
  </action>
  <verify>
    <automated>npm test && npm run build && npm run test:e2e</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0 with zero failing tests across the whole suite
    - `npm run build` exits 0
    - `npm run test:e2e` exits 0 with all five spec files passing
    - `git diff --stat $(git merge-base HEAD main) -- src/domain/ src/lib/db/` (or equivalent since-phase-start diff) produces no output
    - `npx tsc --noEmit` reports zero errors
  </acceptance_criteria>
  <done>The entire phase's changes, combined, pass the full unit suite, production build, and full E2E suite with zero regression and zero calculation-logic drift.</done>
</task>

<task type="auto">
  <name>Task 2: End-of-phase human verification — safe-area on a notched device, dark/light toggle, keyboard focus pass, and both UI-SPEC backstop checks</name>
  <read_first>
    .planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-UI-SPEC.md § UI Considerations (the two 🧪 backstop rows), § Supplementary: Safe-Area / PWA
  </read_first>
  <files>(none — verification only; no source files modified)</files>
  <action>
    Perform the five numbered checks below on a real or simulated iPhone with a Dynamic Island and in a standard desktop browser with DevTools device/media-feature emulation, using the app built and deployed from this phase's combined branch (Task 1's green build). Record the outcome of each check (pass, or a specific description of what failed) in this plan's SUMMARY. If any check fails, do not silently note it — add an explicit entry to `.planning/STATE.md`'s Blockers/Concerns section describing exactly what regressed and on which screen, so the phase's verification pass surfaces it rather than losing it.
  </action>
  <verify>
    <human-check>
      <test>
        1. Safe-area (PWA-01/PWA-02): Open the app on a real iPhone with a Dynamic Island (or Safari's iPhone 15/16 Pro device-emulation mode with the notch/Dynamic Island simulated), installed to the home screen in standalone mode if possible. Check every `(app)` screen (home, bonuses, vacations, settings/salary) for the persistent nav header overlapping the Dynamic Island, and for any screen's bottom content sitting under the home indicator.
        2. Dark/light toggle (UI-06): Toggle the OS color-scheme preference (or DevTools' "Emulate CSS media feature prefers-color-scheme") while on each of: login, register, home (configured and not-configured states), bonuses (empty and populated), vacations (empty and populated), settings/salary, onboarding.
        3. Keyboard-only pass (UI-07): Starting from the login page, press Tab repeatedly through every interactive element on each screen (do not use the mouse) and observe the focus indicator at each stop.
        4. Backstop #1 — annual chart zero-income: Using a fresh test account with a salary configured but zero bonuses/vacations early in a tax year (or any account resolving to zero gross/tax/net for the current year), view the home screen's annual summary card.
        5. Backstop #2 — nav header overflow: View any `(app)` screen at a 375px-wide viewport (iPhone SE / DevTools responsive mode set to 375×667).
      </test>
      <expected>
        1. The header content and any bottom-anchored content never sit under the Dynamic Island or the home indicator on any of the four screens checked.
        2. Every screen re-renders with the correct dark-base or light-override color set immediately on toggle, with no element retaining a stale color from the other mode.
        3. Every interactive element (nav header link, sign-out button, every form input, every submit/cancel/delete/skip button, every bonus/vacation row action link) shows a clearly visible 2px accent-colored outline with a visible offset when focused via keyboard — no element is skipped in tab order and no element shows only the browser's default (often invisible-on-dark) focus ring.
        4. The annual summary card shows a distinct empty-state message/layout, never a 0%/0% (or otherwise degenerate) pie chart presented as if it were meaningful data.
        5. The "НаРуки" nav header link renders on one line, does not overflow the header's 56px height, and does not visually collide with the sign-out button, at 375px width.
      </expected>
      <why_human>Real notched-device safe-area rendering, OS-level color-scheme switching, keyboard focus-ring visibility, and the two explicitly-flagged UI-SPEC backstop states (annotated 🧪 in 08-UI-SPEC.md precisely because the ui-checker could not verify them without a held-out visual check) cannot be proven by any command available in this automated execution environment — Playwright can emulate viewport dimensions but this project's Phase 7 E2E suite was not scoped to iOS Dynamic Island simulation, OS dark-mode toggling, or keyboard-focus-visibility assertions, and 08-UI-SPEC.md's own checker explicitly deferred these to a human pass.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - All five numbered checks in the human-check test block above have been performed and their outcomes recorded in this plan's SUMMARY
    - Any failure found (safe-area overlap, a screen not responding to the dark/light toggle, a missing/invisible focus ring, a broken backstop state) is filed as a follow-up item in STATE.md's Blockers/Concerns section rather than silently passed over
  </acceptance_criteria>
  <done>PWA-01/02, UI-06, UI-07, and both UI-SPEC backstop items are confirmed working on a real/simulated device and recorded for the phase's UAT closure, or any gaps found are explicitly tracked as follow-up items.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Combined phase branch (all 5 prior plans merged) → production build/deploy | The final gate before this phase's changes are considered ready to ship |
| Human verifier → real/simulated notched device | The only boundary in this phase requiring a human, not automatable in this environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-08-17 | Tampering | Whole-phase `src/domain/**`/`src/lib/db/**` diff | critical | mitigate | Task 1's `git diff --stat` check is the final, whole-phase enforcement point for PROJECT.md's frozen-calculation-engine constraint — any drift blocks phase completion |
| T-08-18 | Denial of Service (accessibility) | Any screen where a focus ring is missing/invisible after all 5 plans compose | medium | mitigate | Task 2's keyboard-only pass is the final holistic proof across every screen, not just per-plan spot checks |
| T-08-19 | Information Disclosure | Combined E2E suite run surfaces any latent inter-plan regression (e.g. a shared component restyled inconsistently by two plans) | medium | mitigate | Task 1 runs all five E2E spec files together for the first time in the phase, against the fully-merged branch |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs nothing — verification-only |
</threat_model>

<verification>
- `npm test && npm run build && npm run test:e2e` — full suite green on the combined phase branch
- `git diff --stat $(git merge-base HEAD main) -- src/domain/ src/lib/db/` — empty, whole-phase
- Human-check: safe-area on notched device, dark/light toggle across all 7 screens, keyboard-only focus pass, both UI-SPEC backstop states
</verification>

<success_criteria>
- Every requirement ID (UI-01 through UI-07, PWA-01, PWA-02) is provably satisfied when all five prior plans' changes are combined, not just individually
- Zero regression in the full unit + E2E suite; zero calculation-logic drift across the whole phase
- Both UI-SPEC backstop items and the device-dependent PWA-01/02 outcome are confirmed or explicitly tracked as follow-ups, never silently assumed
</success_criteria>

## Artifacts this phase produces

This plan produces no new source files — it is a verification-only closure plan. Its output is a green full-suite regression run and a recorded human-verification pass (or explicitly tracked follow-up items) covering PWA-01/02, UI-06, UI-07, and both UI-SPEC backstop states.

<output>
Create `.planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-06-SUMMARY.md` when done
</output>
</output>
