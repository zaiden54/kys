---
phase: 08-visual-redesign-accessibility-pwa-safe-area
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/annual-pie-chart.tsx
  - src/components/annual-pie-chart.render.test.tsx
autonomous: true
gap_closure: true
requirements: [UI-01]

estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "UI-01 (annual chart zero-income empty state — closes 08-VERIFICATION.md's single BLOCKER gap / 08-UI-SPEC.md § UI Considerations backstop item #1): When `summary.grossKopecks === 0` for a configured user (salary effective-dated in the future, or zero avans/salary/bonus/vacation events accrued yet in the current tax year), `src/components/annual-pie-chart.tsx` renders a distinct empty-state message — heading + body copy, matching the established `bonus-row`/`vacation-row` list empty-state card pattern (`src/app/(app)/bonuses/page.tsx`'s `BonusListContent`) — instead of a degenerate two-slice 0%/0% Recharts `PieChart`. Proven by a render test asserting the empty-state heading is present and no `<svg>` (Recharts' root element) is rendered when `grossKopecks` is 0, and that the `<svg>` IS rendered (empty-state heading absent) when `grossKopecks` is nonzero."
  artifacts:
    - src/components/annual-pie-chart.tsx
    - src/components/annual-pie-chart.render.test.tsx
  key_links:
    - "annual-pie-chart.tsx's new `grossKopecks === 0` branch → src/app/(app)/page.tsx's `AnnualSummarySection` (Suspense-wrapped) caller: the caller keeps passing `annualResult.summary` straight through unmodified (it can already be `configured: true` with `grossKopecks: 0` — `computeAnnualSummary`'s only `configured: false` gates are missing salary/schedule, never zero accrued income). Per the verifier's remediation note ('annual-pie-chart.tsx or its caller'), the guard is implemented entirely inside the leaf component, so `page.tsx` needs zero changes."
---

<objective>
Close the single BLOCKER gap from `08-VERIFICATION.md`: `src/components/annual-pie-chart.tsx` has no `grossKopecks === 0` branch, so a configured user with zero accrued income in the current tax year (salary effective-dated in the future, or a fresh account before its first payment date) sees a meaningless 0%/0% pie slice instead of a clear empty-state message. This violates UI-01 and was flagged in `08-UI-SPEC.md § UI Considerations` as backstop item #1 ("🧪 backstop — held-out visual check required").

Purpose: Presentation-layer-only fix — detect `grossKopecks === 0` and branch to different markup, never touching how `grossKopecks` itself is computed. Zero risk to `src/domain/**`, `src/lib/db/**`, `src/app/actions/**` (frozen per PROJECT.md's "zero change to calculation logic" v1.1 milestone constraint).
Output: `annual-pie-chart.tsx` renders a distinct empty-state card for the zero-income case; a new render-test case proves the branch actually fires (not just a visual claim).
</objective>

<execution_context>
@/home/zaiden/.claude/gsd-core/workflows/execute-plan.md
@/home/zaiden/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-CONTEXT.md
@.planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-UI-SPEC.md
@.planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Zero-income empty-state branch in AnnualPieChart</name>
  <files>src/components/annual-pie-chart.tsx, src/components/annual-pie-chart.render.test.tsx</files>
  <read_first>
    src/components/annual-pie-chart.tsx (file being modified — current 87-line component: unconditionally builds `data = [{name: "Налог", value: taxKopecks}, {name: "На руки", value: netKopecks}]` and renders `<PieChart>` regardless of `grossKopecks`; no zero-gross branch exists anywhere in the file)
    src/app/(app)/page.tsx (caller, read-only — do NOT modify: `AnnualSummarySection` at lines 45-47 renders `<AnnualPieChart summary={summary} taxYear={taxYear} />` inside a `<Suspense>` boundary; `annualResult.configured` at line 115 only gates the missing-salary/missing-schedule case, never zero-gross — confirms the fix must live inside the leaf component, not the caller)
    src/app/actions/annual-summary.ts (read-only, understand the data contract — do NOT modify, this is calculation logic: `AnnualSummary` interface at lines 54-60 has `grossKopecks: Kopecks` which is `totalGrossKopecks` — a running sum starting from `baselineAmountKopecks` and adding every in-year event's gross; it legitimately reaches exactly 0 when the baseline is 0 and zero in-year events exist, e.g. a brand-new account or a salary effective-dated after `taxYear`'s end. `configured: true` in `AnnualSummaryResult` (lines 67-69) says nothing about whether `grossKopecks` is nonzero — only that salary+schedule exist)
    src/app/(app)/bonuses/page.tsx (read-only, established empty-state pattern to match — `BonusListContent`'s zero-rows branch at lines 16-25: `<div className="flex flex-col items-center gap-3 rounded-[12px] border border-[color:var(--color-tertiary-surface)] bg-[color:var(--color-secondary)] p-6 text-center">` containing an `<h3 className="font-semibold text-[color:var(--color-text-primary)]">` heading, a `<p className="text-sm text-[color:var(--color-text-secondary)]">` body, and an accent-button-styled CTA link)
  </read_first>
  <behavior>
    - Test 1: `AnnualPieChart` rendered with `summary.grossKopecks: 0, taxKopecks: 0, netKopecks: 0` and `taxYear={2026}` renders the text "Пока нет дохода в 2026 году" (empty-state heading) AND the rendered container has zero `<svg>` elements (Recharts' `PieChart` root never mounts).
    - Test 2: `AnnualPieChart` rendered with a nonzero `summary.grossKopecks` (existing `makeSummary()` default fixture) does NOT render "Пока нет дохода" anywhere, AND the rendered container has at least one `<svg>` element (the pie chart renders as before — this is a regression guard proving the new branch does not swallow the populated case).
  </behavior>
  <action>
    In `src/components/annual-pie-chart.tsx`: add `import Link from "next/link";` below the existing `import { formatKopecks } from "@/domain/money";` import. Inside the `AnnualPieChart` function body, after the existing `baselineIsEstimated` conditional note block (the `{baselineIsEstimated ? (...) : null}` JSX, currently lines 50-54) and before the `<div className="mt-4 flex justify-center">` PieChart wrapper (currently line 56), wrap everything from that PieChart `<div>` down through the closing `</dl>` (currently lines 56-83) in a ternary: `{grossKopecks === 0 ? ( <empty-state markup> ) : ( <>...existing PieChart div + gross <p> + breakdown <dl>...</> )}`. The empty-state markup is: `<div className="mt-4 flex flex-col items-center gap-2 rounded-[8px] border border-[color:var(--color-tertiary-surface)] bg-[color:var(--color-dominant)] p-6 text-center">` containing `<h3 className="font-semibold text-[color:var(--color-text-primary)]">Пока нет дохода в {taxYear} году</h3>`, then `<p className="text-sm text-[color:var(--color-text-secondary)]">Сводка появится, как только будет учтена первая выплата в этом году — аванс, зарплата, бонус или отпускные.</p>`, then `<Link href="/settings/salary" className="mt-2 rounded-[8px] bg-[color:var(--color-accent-button)] px-4 py-2 text-sm font-semibold text-white focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[color:var(--color-accent)]">Настроить оклад</Link>` — this exact CTA class string and `href="/settings/salary"` target already exist verbatim in `src/app/(app)/page.tsx`'s "Сводка недоступна" fallback card, reused here for visual consistency (no new class pattern introduced). The `data` array construction (currently lines 36-39, `[{name: "Налог", value: taxKopecks}, {name: "На руки", value: netKopecks}]`) stays exactly where it is (before the ternary) since it is only read inside the non-zero branch — leaving it unconditional is harmless (an unused `[{value:0},{value:0}]` array in the zero-gross case) and keeps the diff minimal. Do not touch `formatPercent`, `TAX_COLOR`, `NET_COLOR`, or the `percentFormatter` — those are only reached from the non-zero branch and are unchanged.

    In `src/components/annual-pie-chart.render.test.tsx`: add a new `describe("AnnualPieChart zero-income empty state", ...)` block (after the existing `describe("AnnualPieChart baseline-estimated note", ...)` block) with two `it(...)` cases matching the `<behavior>` above. Use `render(...)`'s returned `container` (destructure `{ container }` from `render(...)`, matching the existing `import { cleanup, render, screen } from "@testing-library/react"` already at the top of the file — no new import needed) and assert `container.querySelectorAll("svg").length` is `0` for the zero-income case and `> 0` for the nonzero case. Use the existing `makeSummary(overrides)` helper for both cases: `makeSummary({ grossKopecks: 0, taxKopecks: 0, netKopecks: 0 })` for Test 1, and bare `makeSummary()` for Test 2 (its existing defaults are already nonzero: `grossKopecks: 1_000_000_00`).

    RED first: write both test cases against the unmodified component, confirm Test 1 fails (no empty-state heading exists yet, `<svg>` count is not 0) via `npx vitest run src/components/annual-pie-chart.render.test.tsx`, THEN implement the component change above, THEN re-run to confirm both tests (all 4 total: 2 existing + 2 new) pass GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/components/annual-pie-chart.render.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/annual-pie-chart.render.test.tsx` reports 4 passed (2 pre-existing baseline-note tests + 2 new zero-income-empty-state tests), 0 failed
    - `grep -n "grossKopecks === 0" src/components/annual-pie-chart.tsx` matches exactly once — the new conditional branch exists
    - `grep -n "Пока нет дохода" src/components/annual-pie-chart.tsx` matches exactly once — the empty-state heading copy is present
    - `npx tsc --noEmit` reports no new errors (proves the `next/link` import and JSX ternary compile cleanly)
    - `git status --porcelain -- src/domain/ src/lib/db/ src/app/actions/` produces no output — zero risk to calculation logic, confirming this is a presentation-only change
    - `git status --porcelain -- src/app/(app)/page.tsx` produces no output — confirms the fix lives entirely in the leaf component, the caller is untouched
  </acceptance_criteria>
  <done>`AnnualPieChart` renders a distinct empty-state card (heading "Пока нет дохода в {taxYear} году" + body copy + "Настроить оклад" CTA to `/settings/salary`) instead of a degenerate 0%/0% pie whenever `summary.grossKopecks === 0`; the populated (nonzero) case is unchanged and covered by a regression test. Both new tests pass; `npx tsc --noEmit` and the full unit suite (`npm test`) are green; no file outside `src/components/annual-pie-chart.tsx` and `src/components/annual-pie-chart.render.test.tsx` is modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server-computed `AnnualSummary` prop → client component render (`annual-pie-chart.tsx`, `"use client"`) | Pre-existing boundary, unchanged by this gap-closure fix — `summary` values are already fully computed and trusted by the time they cross into this client component; no new data enters here |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-18 | Tampering | Calculation-logic drift (`src/domain/**`, `src/lib/db/**`, `src/app/actions/**` must stay byte-for-byte unchanged per PROJECT.md's frozen-calculation-logic v1.1 constraint) | high | mitigate | `git status --porcelain -- src/domain/ src/lib/db/ src/app/actions/` is a hard acceptance-criteria gate on Task 1; the fix only ever reads `grossKopecks` to branch presentation, never recomputes it |
| T-08-19 | Denial of Service (accessibility regression) | New `<Link href="/settings/salary">` CTA introduced in the empty-state branch | low | mitigate | CTA reuses the exact `focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[color:var(--color-accent)]` class string already verified elsewhere in Phase 8 (08-01's UI-07 audit), so no new unfocusable interactive element is introduced |
| T-08-20 | Information Disclosure | Empty-state copy | low | accept | Copy is a fixed, hardcoded Russian string with no interpolated data beyond `taxYear` (already a trusted server-derived integer, never user-controlled free text) — no exception message or internal state can leak through this branch |

</threat_model>

<verification>
Run `npx vitest run src/components/annual-pie-chart.render.test.tsx` — expect 4 passed. Run `npx tsc --noEmit` — expect zero errors. Run `git status --porcelain -- src/domain/ src/lib/db/ src/app/actions/ src/app/(app)/page.tsx` — expect empty output (confirms presentation-only, leaf-component-only fix). Optionally run the full suite `npm test` to confirm no other test regresses from the new `next/link` import or JSX restructure.
</verification>

<success_criteria>
- `src/components/annual-pie-chart.tsx` renders a distinct empty-state card (not a degenerate 0%/0% pie) whenever `summary.grossKopecks === 0`
- A real automated render test (not a visual-only claim) proves the branch fires: asserts the empty-state heading text is present and no `<svg>` renders in the zero-income case, and the inverse in the nonzero case
- 08-VERIFICATION.md's single BLOCKER gap (Truth #10, UI-01 / UI-SPEC backstop item #1) is closed
- Zero changes to `src/domain/**`, `src/lib/db/**`, `src/app/actions/**`, or `src/app/(app)/page.tsx`
</success_criteria>

## Artifacts this phase produces

- `src/components/annual-pie-chart.tsx` (modified) — adds the `grossKopecks === 0` empty-state branch
- `src/components/annual-pie-chart.render.test.tsx` (modified) — adds the 2-case zero-income-empty-state describe block proving the branch fires
- `.planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-07-SUMMARY.md` (new, written by the executor on completion)

<output>
Create `.planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-07-SUMMARY.md` when done
</output>
