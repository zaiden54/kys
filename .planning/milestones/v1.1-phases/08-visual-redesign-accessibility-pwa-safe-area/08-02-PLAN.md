---
phase: 08-visual-redesign-accessibility-pwa-safe-area
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/app/(app)/bonuses/bonus-row.tsx
  - src/app/(app)/bonuses/bonus-form.tsx
  - src/app/(app)/bonuses/page.tsx
autonomous: true
requirements: [UI-01, UI-02, UI-05, UI-07]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "UI-01 (bonuses screen, verbatim ROADMAP Phase 8 success criterion 1): The bonuses screen shows a clear empty/loading/error state instead of a blank screen or a raw technical error. [Spec-less probe fallback note: UI-01 came back `unclassified`/`unresolved` from the deterministic edge-coverage engine — this truth is authored from ROADMAP.md's Phase 8 Success Criteria text plus 08-UI-SPEC.md's approved UI Considerations table.] `src/app/(app)/bonuses/page.tsx`'s zero-row state renders the 08-UI-SPEC.md § Copywriting Contract empty-state heading \"Пока нет бонусов\" + body copy pointing at the \"Добавить бонус\" CTA (replacing the current \"Нет бонусов\" heading, per the UI-SPEC's locked `Пока нет {N}` pattern — no e2e test asserts the old string, confirmed by grep of `e2e/*.spec.ts`). The list is wrapped in `<Suspense fallback={<SkeletonLoader count={3} variant=\"bonus-row\" />}>` (Plan 08-01's new component) instead of rendering with no loading affordance. Submit/save failures in `bonus-form.tsx`/`bonus-row.tsx`'s edit mode keep their existing generic-Russian-message catch blocks (`\"Не удалось сохранить бонус. Попробуйте ещё раз.\"`), now styled in `--color-destructive`, never raw exception text."
    - "UI-02 (bonuses screen money/date formatting): Every money amount and date on the bonuses screen (`bonus-row.tsx`'s display-mode amount/date, `bonus-form.tsx` has no formatted output to wrap) carries the `.tabular-nums` utility class from `globals.css` on top of `formatKopecks()`/`formatIsoDateRu()`'s unchanged output — proven by `grep -c tabular-nums src/app/(app)/bonuses/bonus-row.tsx` reporting at least 2 (date span + amount span)."
    - "UI-05/UI-07 (bonuses screen visual system + accessibility): `bonus-row.tsx`'s \"Изменить бонус\"/\"Удалить бонус\" action links and `bonus-form.tsx`'s submit button carry `focus-visible:outline-2` per 08-UI-SPEC.md's Focus-Visible Outline Pattern; the destructive \"Удалить бонус\" link uses `--color-destructive` (not `--color-accent`), matching 08-UI-SPEC.md's explicit rule that accent never applies to destructive actions. Every form input in `bonus-form.tsx`/`bonus-row.tsx`'s edit mode keeps a real `<label>` (or the existing `aria-label=\"Тип выплаты\"` on the unlabeled edit-mode `<select>`, which 08-UI-SPEC.md § Form Fields explicitly confirms already satisfies UI-07 without conversion)."
  artifacts:
    - src/app/(app)/bonuses/bonus-row.tsx
    - src/app/(app)/bonuses/bonus-form.tsx
    - src/app/(app)/bonuses/page.tsx
  key_links:
    - "bonus-row.tsx's native `window.confirm(...)` call inside `onDelete` → e2e/bonus.spec.ts's dialog-handling test — this plan must not touch the confirm() call, its message string, or the `onDelete` control flow, only surrounding className values"
    - "bonus-form.tsx's `#amountRubles`/`#date`/`#note`/`#type` input ids → e2e/bonus.spec.ts's `page.locator(\"#amountRubles\")` etc. — these ids are load-bearing selectors and must survive this restyle unchanged"
    - "bonus-row.tsx's edit-mode `input[type=\"text\"]`/`input[type=\"number\"]`/`select[aria-label=\"Тип выплаты\"]` → e2e/bonus.spec.ts's `editingRow.locator('input[type=\"text\"]')` etc. — input `type` attributes and the select's `aria-label` are load-bearing and must survive unchanged"
  prohibitions:
    - requirement_id: UI-03
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT modify bonus-row.tsx's window.confirm() delete-confirmation call, its message text, or its control flow — 08-CONTEXT.md explicitly locks this out of UI-03's restyle scope specifically because e2e/bonus.spec.ts's Playwright dialog handler is written against this exact native-dialog behavior; changing it would silently break Phase 7's regression suite."
    - requirement_id: UI-05
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT change any value, formula, or branch in src/domain/** or src/lib/db/** or src/app/actions/bonus.ts while restyling — presentation-only pass, guarded by a git status --porcelain check in every task's acceptance criteria."
---

<objective>
Restyle the bonuses screen (list-row component, add/edit form, and page composition) onto Plan 08-01's token system: consistent money/date formatting via `tabular-nums`, the updated empty-state copy, a Suspense-driven loading skeleton for the bonus list, focus-visible accessibility on every interactive element, and destructive-color treatment for the delete action — all while leaving the `window.confirm()` delete flow, the form's input ids, and the edit-mode input types byte-for-byte unchanged, since `e2e/bonus.spec.ts` (Phase 7's regression safety net) is written directly against those exact selectors and behaviors.

Purpose: Bring the bonuses screen (one of the ROADMAP's five explicitly-named screens for UI-01) up to the new visual system without regressing the E2E coverage that exists specifically to protect this redesign from silent breakage.
Output: A restyled `bonus-row.tsx` (display + edit modes), `bonus-form.tsx`, and `bonuses/page.tsx` with a Suspense-driven skeleton loading state and updated empty-state copy.
</objective>

<execution_context>
@/home/zaiden/.claude/gsd-core/workflows/execute-plan.md
@/home/zaiden/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-CONTEXT.md
@.planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-UI-SPEC.md
@.planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle bonus-row.tsx (display + edit modes) and bonus-form.tsx</name>
  <read_first>
    src/app/(app)/bonuses/bonus-row.tsx (file being modified — current `zinc-*`/`red-700` display-mode grid, edit-mode form with `input[type="date"]`/`input[type="number"]`/`input[type="text"]`/`select[aria-label="Тип выплаты"]`, `window.confirm(...)` in `onDelete`, `editSessionRef` guard — do not touch any of the JS logic, only `className` strings)
    src/app/(app)/bonuses/bonus-form.tsx (file being modified — current `zinc-*`/`red-600`/`green-700` form, `#amountRubles`/`#date`/`#note`/`#type` ids)
    .planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-PATTERNS.md § "Bonus/Vacation Row Components" (display + edit mode revised patterns), § "Form Fields" (label/input/focus pattern), § "Focus-Visible Outline Pattern", § "Tabular Numerals for Money & Dates"
    .planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-UI-SPEC.md § Color (destructive vs. accent reservation rule), § Form Fields, § Copywriting Contract (Destructive confirmation row — confirms the locked, unchanged `window.confirm()` message text)
    e2e/bonus.spec.ts (confirms every selector this task must not break: `#amountRubles`/`#date`/`#note`/`#type`, `getByRole("button", { name: "Сохранить бонус" })`, `getByRole("button", { name: "Изменить бонус" })`/`"Удалить бонус"`, `editingRow.locator('input[type="text"]')`/`input[type="number"]`, `editingRow.getByRole("combobox", { name: "Тип выплаты" })`, `getByRole("button", { name: "Сохранить", exact: true })`)
  </read_first>
  <files>src/app/(app)/bonuses/bonus-row.tsx, src/app/(app)/bonuses/bonus-form.tsx</files>
  <action>
    In `bonus-row.tsx`'s display mode: replace `border-zinc-200` with `border-[color:var(--color-tertiary-surface)]`; the date span gets `text-[color:var(--color-text-secondary)] tabular-nums font-[family-name:var(--font-family-caption)] text-[length:var(--font-size-caption)]` (Caption/date rule); the amount span gets `font-semibold tabular-nums text-[color:var(--color-text-primary)]`; the note span gets `text-[color:var(--color-text-secondary)]` (keep `truncate` + `title={bonus.note ?? undefined}` unchanged — 08-UI-SPEC.md's UI Considerations "overflow | bonus note field" row confirms this exact pattern is already covered, restyle only). "Изменить бонус" gets `text-[color:var(--color-text-primary)] underline focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[color:var(--color-accent)]`; "Удалить бонус" gets `text-[color:var(--color-destructive)] underline disabled:opacity-50 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[color:var(--color-destructive)]`. The inline error `<p>` gets `text-[color:var(--color-destructive)]`.

    In `bonus-row.tsx`'s edit mode: replace every `rounded border border-zinc-300 px-3 py-2` input/select className with `rounded-[8px] border border-[color:var(--color-tertiary-surface)] bg-[color:var(--color-dominant)] px-3 py-2 text-[color:var(--color-text-primary)] focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[color:var(--color-accent)]` — do not change `type="date"`/`type="number"`/`type="text"` attributes or the `aria-label="Тип выплаты"` on the `<select>`. The submit button (`bg-zinc-900`) becomes `bg-[color:var(--color-accent)] text-white font-semibold focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[color:var(--color-accent)]`; the "Отмена" button (`border-zinc-300`) becomes `border border-[color:var(--color-tertiary-surface)] text-[color:var(--color-text-primary)] focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[color:var(--color-accent)]`. Validation error `<p>` elements get `text-[color:var(--color-destructive)]`.

    In `bonus-form.tsx`: apply the same Form Fields pattern to all four inputs (`amountRubles`/`date`/`note`/`type`) — label gets `text-[length:var(--font-size-label)] font-[number:var(--font-weight-label)] text-[color:var(--color-text-primary)]`, input/select gets the same `rounded-[8px] border border-[color:var(--color-tertiary-surface)] bg-[color:var(--color-dominant)] px-3 py-2 text-[color:var(--color-text-primary)] focus-visible:outline...` treatment as above, error `<p>` gets `text-[color:var(--color-destructive)]`, the helper `<p className="text-xs text-zinc-500">` (date field hint) becomes `text-[length:var(--font-size-caption)] text-[color:var(--color-text-secondary)]`. Do not change any `id="amountRubles"`/`id="date"`/`id="note"`/`id="type"` attribute. The submit button and the success message (`text-green-700` → keep a success-tone color; use `text-[color:var(--color-accent)]` since emerald is this app's success-signal color per 08-UI-SPEC.md § Color's "success indicators" usage row) get restyled the same way as `bonus-row.tsx`'s edit-mode submit button.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run test:e2e -- e2e/bonus.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:e2e -- e2e/bonus.spec.ts` passes (exercises add, edit, delete, and the amount-after-edit assertion against a live dev server + isolated Neon branch)
    - `grep -c "zinc-\|red-6\|red-7\|green-7" src/app/(app)/bonuses/bonus-row.tsx src/app/(app)/bonuses/bonus-form.tsx` reports 0 across both files
    - `grep -n 'id="amountRubles"' src/app/(app)/bonuses/bonus-form.tsx`, `grep -n 'id="date"'`, `grep -n 'id="note"'`, `grep -n 'id="type"'` all still match (ids unchanged)
    - `grep -n 'aria-label="Тип выплаты"' src/app/(app)/bonuses/bonus-row.tsx` still matches
    - `grep -n "window.confirm" src/app/(app)/bonuses/bonus-row.tsx` still matches with the exact same message template
    - `git status --porcelain -- src/domain/ src/lib/db/ src/app/actions/bonus.ts` produces no output
    - `npx tsc --noEmit` reports no new errors
  </acceptance_criteria>
  <done>bonus-row.tsx (both modes) and bonus-form.tsx are fully token-driven with focus-visible accessibility and destructive-color delete actions; all input ids/types/aria-labels and the window.confirm() flow are byte-for-byte unchanged; e2e/bonus.spec.ts passes against a live dev server.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle bonuses/page.tsx — updated empty-state copy + Suspense-driven loading skeleton</name>
  <read_first>
    src/app/(app)/bonuses/page.tsx (file being modified — current `zinc-*` composition, "Нет бонусов" heading, no Suspense/loading state)
    src/components/skeleton-loader.tsx (Plan 08-01's new component — `variant="bonus-row"` fallback)
    .planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-PATTERNS.md § "Bonus/Vacation List Pages with Loading Skeletons" (full revised structure: `BonusListContent` async sub-component wrapped in `<Suspense fallback={<SkeletonLoader count={3} variant="bonus-row" />}>`)
    .planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-UI-SPEC.md § Copywriting Contract (Empty state heading/body rows — locked "Пока нет бонусов" pattern), § UI Considerations rows "empty | bonus-list..." and "loading | bonus-list..."
    e2e/bonus.spec.ts (confirms `getByRole("button", { name: "Сохранить бонус" })`/list-row `<li>` selectors are unaffected by this page-level restructure — the form and rows themselves were restyled in Task 1, this task only touches the page shell and empty/loading states)
  </read_first>
  <files>src/app/(app)/bonuses/page.tsx</files>
  <action>
    Restructure `bonuses/page.tsx` per 08-PATTERNS.md's revised structure: extract the data-fetching + list-rendering portion (currently inline in the default export) into an internal `async function BonusListContent()` that calls `requireUserId()`/`listBonuses(userId)` and renders the `<section>` (heading "История бонусов", empty-state block, or the row-grid header + `<ul>` of `<BonusRow>`). Wrap `<BonusListContent />` in `<Suspense fallback={<SkeletonLoader count={3} variant="bonus-row" />}>` inside the page's default export, which keeps the outer `<h1>`/`<div id="bonus-form"><BonusForm /></div>` structure unchanged around it. Update the empty-state heading from `"Нет бонусов"` to `"Пока нет бонусов"` (08-UI-SPEC.md § Copywriting Contract's locked pattern) and restyle its container to `rounded-[12px] border border-[color:var(--color-tertiary-surface)] bg-[color:var(--color-secondary)] p-6`, heading `text-[color:var(--color-text-primary)]`, body `text-[color:var(--color-text-secondary)]`, CTA link (`href="#bonus-form"`) to `rounded-[8px] bg-[color:var(--color-accent)] px-4 py-2 text-sm font-semibold text-white focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[color:var(--color-accent)]`. Restyle the row-grid header (`border-zinc-200`/`text-zinc-500`) to `border-[color:var(--color-tertiary-surface)] text-[color:var(--color-text-secondary)]`, and the page `<h1>`/`<h2>` headings to `text-[length:var(--font-size-display)] font-[number:var(--font-weight-display)] text-[color:var(--color-text-primary)]` / `text-[length:var(--font-size-heading)] font-[number:var(--font-weight-heading)] text-[color:var(--color-text-primary)]` respectively.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build && npm run test:e2e -- e2e/bonus.spec.ts e2e/pie-chart.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `npm run test:e2e -- e2e/bonus.spec.ts e2e/pie-chart.spec.ts` passes (pie-chart.spec.ts also exercises bonus creation as a setup step, confirming the page restructure doesn't break that path)
    - `grep -n "Пока нет бонусов" src/app/(app)/bonuses/page.tsx` matches; `grep -n "Нет бонусов" src/app/(app)/bonuses/page.tsx` (the old string) does not match
    - `grep -n "Suspense" src/app/(app)/bonuses/page.tsx` and `grep -n "SkeletonLoader" src/app/(app)/bonuses/page.tsx` both match
    - `grep -c "zinc-" src/app/(app)/bonuses/page.tsx` reports 0
    - `git status --porcelain -- src/domain/ src/lib/db/` produces no output
  </acceptance_criteria>
  <done>bonuses/page.tsx shows a skeleton loading placeholder matching the row shape while data loads, an updated "Пока нет бонусов" empty state, and a fully token-driven page shell; e2e/bonus.spec.ts and e2e/pie-chart.spec.ts both pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `bonus-row.tsx`'s `window.confirm()` → browser's native dialog | Untouched by this plan; restyle must not accidentally wrap or intercept the native confirm call |
| Bonus form/row submit failures → rendered error text | Must stay the existing generic Russian message, never `error.message`/stack from a caught exception |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-08-05 | Tampering | `bonus-row.tsx`'s `onDelete`/`window.confirm()` flow | high | mitigate | Task 1 explicitly forbids touching this function or its message string; acceptance criteria grep for the unchanged `window.confirm` call; `e2e/bonus.spec.ts`'s delete test is the final proof |
| T-08-06 | Tampering | `src/domain/**`, `src/app/actions/bonus.ts` | critical | mitigate | Both tasks scope their files list to presentation-only files; `git status --porcelain` structural guard in every task |
| T-08-07 | Information Disclosure | Bonus save/delete failure messages | low | accept | Existing generic-message catch blocks are unchanged by this restyle — only their CSS color changes |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new package installs — pure Tailwind arbitrary-value restyle |
</threat_model>

<verification>
- `npx tsc --noEmit` — no new errors after either task
- `npm run test:e2e -- e2e/bonus.spec.ts e2e/pie-chart.spec.ts` — full bonus CRUD + amount-after-edit + pie-chart-setup-via-bonus paths pass
- `npm run build` — production build succeeds
- `git status --porcelain -- src/domain/ src/lib/db/ src/app/actions/bonus.ts` — empty after both tasks
</verification>

<success_criteria>
- Bonus list/form/row screen fully reflects the new token-driven visual system with focus-visible accessibility and destructive-color delete styling
- Empty state updated to the locked "Пока нет бонусов" copy; loading state shows a shape-matched skeleton, never a spinner
- `window.confirm()` delete flow, form input ids, and edit-mode input types are byte-for-byte unchanged — `e2e/bonus.spec.ts` passes without modification
</success_criteria>

## Artifacts this phase produces

- `src/app/(app)/bonuses/bonus-row.tsx` — modified: token-driven restyle of display + edit modes, focus-visible accessibility, destructive-color delete action; zero logic change
- `src/app/(app)/bonuses/bonus-form.tsx` — modified: token-driven Form Fields pattern applied to all four inputs; ids unchanged
- `src/app/(app)/bonuses/page.tsx` — modified: `BonusListContent` extracted and wrapped in `<Suspense>` with a `SkeletonLoader` fallback; empty-state copy updated to "Пока нет бонусов"

<output>
Create `.planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-02-SUMMARY.md` when done
</output>
</output>
