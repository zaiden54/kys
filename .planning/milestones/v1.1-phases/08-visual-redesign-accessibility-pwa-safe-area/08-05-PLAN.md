---
phase: 08-visual-redesign-accessibility-pwa-safe-area
plan: 05
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/app/(auth)/login/page.tsx
  - src/app/(auth)/register/page.tsx
autonomous: true
requirements: [UI-01, UI-02, UI-05, UI-07]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "UI-01 (login/register screens, verbatim ROADMAP Phase 8 success criterion 1): The login and register screens show a clear error state instead of a raw technical error. [Spec-less probe fallback note: UI-01 came back `unclassified`/`unresolved` from the deterministic edge-coverage engine — this truth is authored from ROADMAP.md's Phase 8 Success Criteria text plus 08-UI-SPEC.md's approved UI Considerations table.] `login/page.tsx`'s hardcoded SEC-02 generic error (`\"Неверный email или пароль\"`, locked by Phase 6) and its network-failure catch block are restyled to `--color-destructive` with zero change to the error text or the SEC-02 hardcoding logic that phase 6 built (verified by grep of the exact `setFormError` call sites). `register/page.tsx`'s existing duplicate-email error passthrough (`error.message ?? \"Не удалось зарегистрироваться\"`) is likewise restyled without changing its logic — 08-CONTEXT.md's decisions explicitly scope this phase to registration's visual layer only, never its error-message logic (which is a separate, already-shipped SEC-02-adjacent decision from Phase 6, out of scope for this phase to alter)."
    - "UI-01 (loading state, form): Both forms keep their existing isSubmitting-driven disabled+label-swap pattern (\"Входим…\"/\"Создаём аккаунт…\") restyled onto tokens — no new loading mechanism, per 08-UI-SPEC.md § UI Considerations row \"loading | auth-forms... (form) | covered\"."
    - "UI-05/UI-07 (auth screens visual system + accessibility): Both forms' email/password inputs keep their real `<label htmlFor>`/`id` pairs, gain 2px accent-color focus-visible outlines with visible offset, and 8px-radius tertiary-surface-bordered primary-surface backgrounds, matching 08-UI-SPEC.md § Form Fields exactly. The submit buttons use `--color-accent` fill; the standalone-mode info banner (login only) and the register/login cross-links use token-driven neutral/accent colors respectively."
  artifacts:
    - src/app/(auth)/login/page.tsx
    - src/app/(auth)/register/page.tsx
  key_links:
    - "login/page.tsx's setFormError(\"Неверный email или пароль\") (Phase 6's SEC-02 fix) → this task's restyle must only change the surrounding className, never the literal string or the fact that it never reads error.message/error.code — a regression here would silently reopen the SEC-02 account-enumeration vector Phase 6 closed"
    - "register/page.tsx's error.message ?? \"Не удалось зарегистрироваться\" passthrough → e2e/auth.spec.ts's registration flow — this exact behavior is out of this phase's locked scope (08-CONTEXT.md), restyle only"
  prohibitions:
    - requirement_id: UI-05
      category: safety
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT modify login/page.tsx's onSubmit error-handling branch beyond className changes — Phase 6's SEC-02 fix (the hardcoded 'Неверный email или пароль' string that never reads Better Auth's error payload) is a security-hardening decision this phase must not touch, weaken, or revert while restyling."
    - requirement_id: UI-05
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT change any value, formula, or branch in src/lib/auth.ts, src/lib/auth-client.ts, or src/lib/auth-allowed-hosts.ts while restyling — presentation-only pass, guarded by a git status --porcelain check."
---

<objective>
Restyle `login/page.tsx` and `register/page.tsx` onto Plan 08-01's token system — form fields, focus-visible accessibility, the standalone-mode info banner, and error-message presentation — while leaving Phase 6's SEC-02 hardened generic-error behavior on the login page completely untouched. This is the one place in the phase where a restyle task runs directly adjacent to a previously-shipped security fix, so the plan is explicit about the boundary between "may change" (CSS classes) and "must not change" (the `setFormError` call sites and their string literals).

Purpose: Bring the two auth screens (both explicitly named in ROADMAP Phase 8's UI-01 success criterion via "login") up to the new visual system without reopening the account-enumeration vector Phase 6 closed.
Output: Restyled `login/page.tsx` and `register/page.tsx`, token-driven throughout, with zero behavioral change to either page's auth-error handling.
</objective>

<execution_context>
@/home/zaiden/.claude/gsd-core/workflows/execute-plan.md
@/home/zaiden/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-CONTEXT.md
@.planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-UI-SPEC.md
@.planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-PATTERNS.md
@.planning/phases/06-auth-security-hardening/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle login/page.tsx</name>
  <read_first>
    src/app/(auth)/login/page.tsx (file being modified — current `zinc-*`/`red-600`/`bg-black`/`bg-blue-50 dark:bg-blue-900/30` composition; `onSubmit`'s `if (error) { setFormError("Неверный email или пароль"); return; }` — Phase 6's SEC-02 fix, do not touch this line beyond nothing at all; the `catch` block's `setFormError("Не удалось войти. Проверьте соединение и попробуйте снова.")`; `router.refresh()`/`router.push("/")` sequence)
    .planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-PATTERNS.md § "Auth Pages" (full revised login pattern)
    .planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-UI-SPEC.md § Form Fields, § Color
    .planning/phases/06-auth-security-hardening/06-01-SUMMARY.md (confirms exactly which line is the SEC-02 fix boundary this task must not cross)
    e2e/auth.spec.ts (confirms `getByLabel("Email")`, `getByLabel("Пароль")`, `getByRole("button", { name: "Зарегистрироваться" })` on the shared cross-link pattern — login's own render test `login/page.render.test.tsx` covers the SEC-02 assertions directly)
    src/app/(auth)/login/page.render.test.tsx (existing test — confirms the 3 SEC-02 render-test cases from Phase 6 that must keep passing unmodified: identical generic error text regardless of the mocked Better Auth error payload)
  </read_first>
  <files>src/app/(auth)/login/page.tsx</files>
  <action>
    Replace `border-zinc-300` on both `email`/`password` inputs with `rounded-[8px] border border-[color:var(--color-tertiary-surface)] bg-[color:var(--color-dominant)] px-3 py-2 text-[color:var(--color-text-primary)] focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[color:var(--color-accent)]` (do not change `id="email"`/`id="password"`, `autoComplete`, or `type` attributes). Labels get `text-[length:var(--font-size-label)] font-[number:var(--font-weight-label)] text-[color:var(--color-text-primary)]`. Both error `<p>` elements (`errors.email.message`/`errors.password.message`/`formError`) get `text-[color:var(--color-destructive)]` — do not touch the `if (error) { setFormError(...) }` block's logic, condition, or string literal; only the JSX className rendering `formError` may change. The standalone-mode info banner (`bg-blue-50 dark:bg-blue-900/30 text-blue-600 dark:text-blue-300`) becomes `rounded-[8px] bg-[color:var(--color-secondary)] border border-[color:var(--color-tertiary-surface)] p-3 text-[color:var(--color-text-primary)]`. Submit button (`bg-black`) becomes `rounded-[8px] bg-[color:var(--color-accent)] px-4 py-2 text-white font-semibold disabled:opacity-50 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[color:var(--color-accent)]`. The `<h1>` becomes `text-[length:var(--font-size-display)] font-[number:var(--font-weight-display)] text-[color:var(--color-text-primary)]`. The "Зарегистрироваться" cross-link becomes `text-[color:var(--color-accent)] underline focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-[color:var(--color-accent)]`; its surrounding "Нет аккаунта?" text stays `text-[color:var(--color-text-secondary)]`.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(auth)/login/page.render.test.tsx" && npx tsc --noEmit && npm run test:e2e -- e2e/auth.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run "src/app/(auth)/login/page.render.test.tsx"` passes unmodified — all 3 SEC-02 cases plus the router-refresh/push regression test still pass
    - `npm run test:e2e -- e2e/auth.spec.ts` passes
    - `grep -n 'setFormError("Неверный email или пароль")' src/app/(auth)/login/page.tsx` still matches byte-for-byte
    - `grep -c "zinc-\|red-6\|bg-black\|bg-blue" src/app/(auth)/login/page.tsx` reports 0
    - `git status --porcelain -- src/lib/auth.ts src/lib/auth-client.ts src/lib/auth-allowed-hosts.ts src/domain/ src/lib/db/` produces no output
    - `npx tsc --noEmit` reports no new errors
  </acceptance_criteria>
  <done>Login page is fully token-driven with accessible focus states; Phase 6's SEC-02 hardcoded generic-error fix is byte-for-byte unchanged; existing render test (including all 3 SEC-02 cases) and e2e/auth.spec.ts pass.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle register/page.tsx</name>
  <read_first>
    src/app/(auth)/register/page.tsx (file being modified — current `zinc-*`/`red-600`/`bg-black` composition; `error.message ?? "Не удалось зарегистрироваться"` passthrough, `router.refresh()`/`router.push("/onboarding")` sequence — mirror structure of login, no standalone-mode banner)
    src/app/(auth)/login/page.tsx (already restyled in Task 1 — use as the direct template for register's mirrored form-field/button/label treatment, minus the standalone banner)
    .planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-PATTERNS.md § "Auth Pages"
    .planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-CONTEXT.md `<decisions>` (confirms registration's error-message logic is out of this whole milestone's UI-03/SEC-02 scope — restyle only, matching Phase 6's explicit decision to leave registration's error passthrough untouched)
    src/app/(auth)/register/page.render.test.tsx (existing test — confirms which assertions must survive unmodified)
    e2e/auth.spec.ts (confirms `getByLabel("Email")`, `getByLabel("Пароль")`, `getByRole("button", { name: "Зарегистрироваться" })` on register are load-bearing for the registration step of the golden-path test)
  </read_first>
  <files>src/app/(auth)/register/page.tsx</files>
  <action>
    Apply the identical Form Fields/button/label token treatment from Task 1 to `register/page.tsx`'s `email`/`password` inputs, error text, submit button, and the "Войти" cross-link — do not change `id="email"`/`id="password"`, `autoComplete` values, or the `error.message ?? "Не удалось зарегистрироваться"` line's logic (only its rendering `className`). The `<h1>` "Регистрация" gets the same Display-role treatment as login's `<h1>`. No standalone-mode banner exists on this page (register has no `useIsStandalone()` call) — do not add one, since 08-CONTEXT.md's decisions never asked for one here.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(auth)/register/page.render.test.tsx" && npx tsc --noEmit && npm run test:e2e -- e2e/auth.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run "src/app/(auth)/register/page.render.test.tsx"` passes unmodified
    - `npm run test:e2e -- e2e/auth.spec.ts` passes
    - `grep -n 'error.message ?? "Не удалось зарегистрироваться"' src/app/(auth)/register/page.tsx` still matches byte-for-byte
    - `grep -c "zinc-\|red-6\|bg-black" src/app/(auth)/register/page.tsx` reports 0
    - `git status --porcelain -- src/lib/auth.ts src/lib/auth-client.ts src/domain/ src/lib/db/` produces no output
    - `npx tsc --noEmit` reports no new errors
  </acceptance_criteria>
  <done>Register page is fully token-driven, mirroring login's visual treatment; its existing error-message passthrough logic is byte-for-byte unchanged; existing render test and e2e/auth.spec.ts pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `login/page.tsx`'s `setFormError` call sites → SEC-02's account-enumeration protection | The single highest-risk boundary in this plan — a restyle mistake that reintroduces `error.message` reading would silently reopen a closed vulnerability |
| Auth form submission → Better Auth's `/api/auth/sign-in/email`/`/api/auth/sign-up/email` | Untouched by this plan; no request-shape or credential-handling change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-08-14 | Information Disclosure | `login/page.tsx`'s `onSubmit` error branch | critical | mitigate | Task 1 scopes the diff to className-only changes on the existing `setFormError` call; acceptance criteria grep for the exact unchanged string; `login/page.render.test.tsx`'s 3 SEC-02 cases are the final proof this restyle does not regress Phase 6's fix |
| T-08-15 | Spoofing / Tampering | `src/lib/auth.ts`, `src/lib/auth-client.ts`, `src/lib/auth-allowed-hosts.ts` | critical | mitigate | Neither task's files list includes these; `git status --porcelain` structural guard confirms it |
| T-08-16 | Information Disclosure | `register/page.tsx`'s `error.message` passthrough | low | accept | This is pre-existing, out-of-scope behavior per 08-CONTEXT.md's explicit lock — this plan changes only its presentation, and the underlying decision (leave registration's error text as Better Auth returns it) was already made and accepted in Phase 6 |
| T-08-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new package installs — pure Tailwind arbitrary-value restyle |
</threat_model>

<verification>
- `npx vitest run "src/app/(auth)/login/page.render.test.tsx" "src/app/(auth)/register/page.render.test.tsx"` — both pass unmodified, including Phase 6's 3 SEC-02 cases
- `npm run test:e2e -- e2e/auth.spec.ts` — full register → login → home golden path passes after both tasks
- `npx tsc --noEmit` — no new errors
- `git status --porcelain -- src/lib/auth.ts src/lib/auth-client.ts src/lib/auth-allowed-hosts.ts src/domain/ src/lib/db/` — empty after both tasks
</verification>

<success_criteria>
- Login and register screens fully reflect the new token-driven visual system with accessible focus states
- Phase 6's SEC-02 hardened generic-login-error behavior is provably unchanged (render test + e2e both pass without modification)
- Zero change to any auth/domain/db logic
</success_criteria>

## Artifacts this phase produces

- `src/app/(auth)/login/page.tsx` — modified: token-driven restyle; SEC-02 error-handling logic byte-for-byte unchanged
- `src/app/(auth)/register/page.tsx` — modified: token-driven restyle mirroring login; existing error passthrough unchanged

<output>
Create `.planning/phases/08-visual-redesign-accessibility-pwa-safe-area/08-05-SUMMARY.md` when done
</output>
</output>
