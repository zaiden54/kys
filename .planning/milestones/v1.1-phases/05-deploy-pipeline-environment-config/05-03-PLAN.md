---
phase: 05-deploy-pipeline-environment-config
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [DEPLOY-03, DEPLOY-05]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "DEPLOY-03 (verbatim ROADMAP Phase 5 success criterion 4): Every PR runs lint + typecheck + unit tests via GitHub Actions, and a failing check blocks merge. [Spec-less probe fallback note: unclassified/unresolved by the deterministic edge-coverage engine; authored from ROADMAP.md's Phase 5 Success Criteria text.] Delivered here: a real GitHub Actions workflow, triggered by an actual `gh pr create` against this repo, observed to run and pass via `gh pr checks`, then wired into a real GitHub branch-protection rule via `gh api` so a red check genuinely blocks merge — not just a workflow file that exists unexercised."
    - "DEPLOY-05 (verbatim ROADMAP Phase 5 success criterion 3): Opening a PR triggers exactly one deploy path per environment; GitHub Actions and Vercel auto-deploy never both deploy the same environment. [Spec-less probe fallback note: unclassified/unresolved; authored from ROADMAP.md's Phase 5 Success Criteria text.] Delivered here by construction: `.github/workflows/ci.yml` contains no deploy step at all — Vercel's existing native git integration (already active on this repo per `gh api repos/zaiden54/kys/deployments`, which shows `vercel[bot]` as the creator of every prior Production/Preview deployment) remains the sole deployer. This is a deliberate deviation from 05-RESEARCH.md's Pattern 3 (GitHub-Actions-triggered `vercel deploy --prod`, needing VERCEL_TOKEN/ORG_ID/PROJECT_ID secrets) — REQUIREMENTS.md's own Out-of-Scope rationale states plainly: \"GitHub Actions гейт + Vercel + ручной staging→prod промоушен достаточны\" (a GitHub Actions gate + Vercel + manual staging→prod promotion is sufficient), explicitly naming this exact split as sufficient and citing 'Enterprise-grade CI/CD' as out of scope. Fewer secrets to manage, fewer moving parts, and CONTEXT.md left the exact deploy-path mechanism to Claude's discretion."
  artifacts:
    - .github/workflows/ci.yml
  key_links:
    - "ci.yml's branch-protection required check name is discovered from a real completed check-run (gh api .../check-runs), not guessed or hardcoded — avoids the common failure mode where branch protection references a context string that never matches any real check"
  prohibitions:
    - requirement_id: DEPLOY-05
      category: safety
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT introduce a second automated deploy trigger for an environment already covered by Vercel's native git auto-deploy (e.g., a future GitHub Actions `vercel deploy`/`vercel --prod` step) without first removing the existing Vercel git-integration deploy for that same environment — the two deploy paths must never coexist, or the double-deploy race DEPLOY-05 exists to prevent recurs."
---

<objective>
Introduce `.github/workflows/ci.yml` (lint + typecheck + unit tests + build, against an ephemeral Postgres service container) as a real, exercised GitHub Actions check — proven by opening an actual PR from this phase's branch and watching the run go green — then wire it into a GitHub branch-protection rule on `main` so a failing check genuinely blocks merge.

Purpose: DEPLOY-03 requires the check to actually block merge, not merely exist; DEPLOY-05 requires this workflow to never also deploy, since Vercel's git integration is already the live deployer for this repo (confirmed via `gh api repos/zaiden54/kys/deployments` during planning — every prior deployment was created by `vercel[bot]`).
Output: `.github/workflows/ci.yml`, a real green CI run against a real PR, a GitHub branch-protection rule on `main` requiring that run's exact check-run name.
</objective>

<execution_context>
@/home/zaiden/.claude/gsd-core/workflows/execute-plan.md
@/home/zaiden/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-deploy-pipeline-environment-config/05-RESEARCH.md
@.planning/phases/05-deploy-pipeline-environment-config/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write CI workflow, open a real PR, watch it go green</name>
  <precondition>Plans 05-01 and 05-02 have landed on this branch — `npm run lint`, `npx tsc --noEmit`, and `npm run typecheck` all exit 0, and `src/lib/auth-allowed-hosts.test.ts` exists and passes (verify with a quick local run before writing the workflow; if any of these are red, the CI run this task depends on cannot go green and this task must halt rather than push a doomed workflow).</precondition>
  <read_first>
    package.json (scripts: `lint`, `typecheck` (added by Plan 05-02), `test`, `build`, `db:push` — the CI workflow calls each of these by name, not by re-deriving the underlying command)
    drizzle.config.ts (reads `process.env.DATABASE_URL` directly — the CI service-container Postgres URL must be exported before `npm run db:push` runs)
    src/lib/validation/auth-secret.ts (BETTER_AUTH_SECRET's real validation rules: minimum 32 chars, rejects a fixed list of placeholder markers including "openssl"/"test-secret", requires ≥8 distinct characters — a naive hardcoded CI secret like "test-secret-test-secret-test-secret" would FAIL this schema; use a freshly `openssl rand -base64 32`-generated value instead, which trivially satisfies all three rules)
    .planning/phases/05-deploy-pipeline-environment-config/05-RESEARCH.md ("Pattern 2: GitHub Actions CI Workflow" — use for step ordering/shape only; its `env:` handling for DATABASE_URL/BETTER_AUTH_SECRET is not addressed there and is specified fresh below, since RESEARCH.md's own Wave 0 gap table flagged this workflow as untested against a real Postgres-requiring test suite)
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
    Create .github/workflows/ci.yml: workflow name `CI`; `on: pull_request: branches: [main]` and `on: push: branches: [main, staging]`; one job `ci` running on `ubuntu-latest` with a `postgres:17` service container (matching CLAUDE.md's "Postgres 17-class" stack description) configured with `POSTGRES_PASSWORD: postgres`, `POSTGRES_DB: postgres`, port mapping `5432:5432`, and a health check (`--health-cmd pg_isready --health-interval 10s --health-timeout 5s --health-retries 5`). Steps, in order: `actions/checkout@v4`; `actions/setup-node@v4` with `node-version: 20.x` and `cache: 'npm'`; `npm ci`; a "Configure test environment" step that exports two job-scoped values via `$GITHUB_ENV` — `DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres` and `BETTER_AUTH_SECRET=$(openssl rand -base64 32)` (generated fresh each run; openssl is preinstalled on `ubuntu-latest`) — so every later step inherits them; `npm run lint`; `npm run typecheck`; `npm run db:push` (applies the Drizzle schema — both `src/lib/db/schema.ts` and `src/lib/db/auth-schema.ts` per drizzle.config.ts — to the fresh, empty service-container database before any DB-touching test runs); `npm test`; `npm run build`. Add a header comment at the top of the file: `# This workflow runs checks only — it never deploys. Vercel's native git integration (already active on this repo) is the sole deployer for every environment. See DEPLOYMENT.md and this plan's DEPLOY-05 prohibition before adding any deploy step here.`

    Commit this file on the current branch. Push the branch (`git push`). Open a real PR against `main` from this branch: `gh pr create --title "Phase 5: deploy pipeline & environment config" --body "..." --base main --head <current-branch>` (or `gh pr create --fill` if a PR does not already exist for this branch — check with `gh pr view` first to avoid a duplicate). Watch the triggered run to completion: `gh pr checks <pr-number> --watch` (or `gh run watch <run-id>` using the run ID from `gh run list --branch <current-branch> --limit 1`). If it fails, read the failure from `gh run view <run-id> --log-failed`, fix the root cause (do not weaken the workflow to force green), and re-push until it passes for real.
  </action>
  <verify>
    <automated>gh pr checks --watch 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists, is valid YAML, and contains no `vercel deploy`/`vercel --prod`/any `actions/*deploy*` step (`grep -ci 'vercel deploy\|vercel --prod' .github/workflows/ci.yml` == 0)
    - A real PR exists for this branch against `main` (`gh pr view --json number` returns a number)
    - `gh pr checks` reports the CI workflow's check(s) as passing (not pending, not failing) for the current commit
  </acceptance_criteria>
  <done>ci.yml is committed, pushed, and has been observed — via a real PR, not a local simulation — to run lint/typecheck/db:push/test/build against a real ephemeral Postgres and pass.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce the CI check via GitHub branch protection</name>
  <precondition>Task 1's PR has at least one completed (not pending) check run for the current commit on `main`'s target branch — required so this task discovers the check's real name instead of guessing it.</precondition>
  <read_first>
    .github/workflows/ci.yml (the workflow just created — job id is `ci` under a workflow named `CI`; the exact GitHub check-run display name for a job with no explicit `jobs.ci.name:` override defaults to the job id, but this must be confirmed against the real run, not assumed)
  </read_first>
  <files>N/A — this task changes GitHub repository configuration (branch protection) via `gh api`, no repository source files are modified</files>
  <action>
    Discover the exact check-run context name GitHub actually recorded for the head commit of Task 1's PR: `gh api repos/zaiden54/kys/commits/$(git rev-parse HEAD)/check-runs --jq '.check_runs[].name'`. Use that literal string (do not guess or hardcode "ci"/"CI" without confirming) as the required status check when creating the branch-protection rule: `gh api repos/zaiden54/kys/branches/main/protection --method PUT --input -` piping a JSON body with `required_status_checks: { strict: true, contexts: [<the discovered name>] }`, `enforce_admins: true`, `required_pull_request_reviews: null`, `restrictions: null` (matching GitHub's REST API v3 branch-protection schema — confirm the exact field names against `gh api repos/{owner}/{repo}/branches/{branch}/protection --method PUT --help`-equivalent docs if the PUT is rejected with a schema error). Verify the rule took effect by re-fetching it: `gh api repos/zaiden54/kys/branches/main/protection --jq '.required_status_checks.contexts'` must include the discovered check name.
  </action>
  <verify>
    <automated>gh api repos/zaiden54/kys/branches/main/protection --jq '.required_status_checks.contexts' 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - `gh api repos/zaiden54/kys/branches/main/protection` returns 200 (not 404 "Branch not protected", which was the state confirmed during planning before this task ran)
    - The returned `required_status_checks.contexts` array contains the exact check-run name discovered from Task 1's real completed run — not a guessed literal
  </acceptance_criteria>
  <done>main is genuinely protected: a PR whose CI check fails cannot be merged, confirmed by inspecting the live branch-protection API response, not by reading the workflow file alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| GitHub Actions runner ↔ npm registry / GitHub Marketplace actions | Supply-chain trust boundary for `actions/checkout@v4`, `actions/setup-node@v4`, `postgres:17` (Docker Hub) |
| GitHub Actions runner ↔ CI-only Postgres service container | Ephemeral, local to the runner, discarded after the run — never reachable from outside the job |
| `main` branch ↔ merge action | Branch-protection rule is the actual enforcement boundary; the workflow file alone is advisory until this rule exists |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|------------------|
| T-05-03 | Tampering | Deploy-path exclusivity (ci.yml) | medium | mitigate | ci.yml contains zero deploy invocation by construction; acceptance criteria greps for `vercel deploy`/`vercel --prod` and requires zero matches; Vercel's pre-existing native git integration remains sole deployer |
| T-05-05 | Tampering (supply chain) | `postgres:17` / `actions/checkout@v4` / `actions/setup-node@v4` | low | accept | All three are first-party (Docker Official Image / GitHub-authored actions), pinned to major version tags already in wide use across this project's ecosystem; not npm-registry installs, so the mandatory Package Legitimacy Gate (npm/pip/cargo only) does not apply — accepted at ASVS level 1 |
| T-05-04 | Elevation of Privilege | `main` branch merge path | medium | mitigate | `gh api ... branches/main/protection` PUT with `required_status_checks` referencing the real discovered check name + `enforce_admins: true`; verified by re-fetching the live protection state (not just trusting the PUT succeeded) |
</threat_model>

<verification>
- `.github/workflows/ci.yml` exists, contains no deploy step, and its check has been observed to pass on a real PR
- `gh api repos/zaiden54/kys/branches/main/protection` returns the rule, with the real discovered check name in `required_status_checks.contexts`
</verification>

<success_criteria>
- Every PR against `main` runs lint + typecheck + unit tests + build via GitHub Actions against a real, isolated Postgres
- A failing check genuinely blocks merge (branch protection confirmed live, not just configured in a file)
- Zero deploy invocation exists anywhere in this workflow — Vercel's existing native git integration remains the only deployer
</success_criteria>

<output>
Create `.planning/phases/05-deploy-pipeline-environment-config/05-03-SUMMARY.md` when done
</output>
