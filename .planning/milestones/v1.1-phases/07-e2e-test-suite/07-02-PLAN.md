---
phase: 07-e2e-test-suite
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - e2e/bonus.spec.ts
autonomous: true
requirements: [E2E-02]
user_setup: []

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A Playwright test creates a bonus through the real /bonuses form and confirms the home screen's next-payment forecast breakdown reflects the new bonus amount (E2E-02)"
    - "A Playwright test edits an existing bonus's amount/date/note/type through the real inline edit form and confirms both the bonus row and the forecast (if the bonus is still the relevant payment) reflect the new values (E2E-02)"
    - "A Playwright test deletes a future bonus (accepting the native `window.confirm` dialog Playwright must handle explicitly, per bonus-row.tsx's `window.confirm(...)` call) and confirms it disappears from the history list and no longer affects the forecast (E2E-02)"
  artifacts:
    - e2e/bonus.spec.ts
  key_links:
    - "e2e/bonus.spec.ts's `authenticated` project session (playwright/.auth/user.json from Plan 07-01) is the only auth path this file uses — it never re-implements login"

flagged_assumptions:
  - "Nyquist spec-less fallback: the deterministic edge-probe classified E2E-02 as 'unclassified/unresolved' (Russian-language E2E-testing requirements don't fit its shape rules). E2E-02's real acceptance criteria are captured in must_haves.truths above, sourced directly from ROADMAP.md's Phase 7 Success Criterion 2 and 07-CONTEXT.md, not from the probe."
---

<objective>
Cover E2E-02 end to end: add, edit, and delete a bonus through the real UI, and prove each mutation is reflected in the home screen's next-payment forecast — the actual regression this requirement exists to catch (a bonus silently not affecting the tax/forecast calculation, or a stale UI after a mutation).

Purpose: bonuses feed directly into the progressive-НДФЛ cumulative calculation and the отпускные average-earnings window (premium-type bonuses only); a UI-level regression here would silently corrupt a real user's forecast without any test noticing.
Output: `e2e/bonus.spec.ts`.
</objective>

<execution_context>
@/home/zaiden/.claude/gsd-core/workflows/execute-plan.md
@/home/zaiden/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-e2e-test-suite/07-CONTEXT.md
@.planning/phases/07-e2e-test-suite/07-PATTERNS.md
@src/app/(app)/bonuses/page.tsx
@src/app/(app)/bonuses/bonus-form.tsx
@src/app/(app)/bonuses/bonus-row.tsx
@src/app/actions/bonus.test.ts
@src/components/next-payment-card.tsx
@src/domain/money.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create-bonus flow and forecast impact</name>
  <files>e2e/bonus.spec.ts</files>
  <read_first>
    src/app/(app)/bonuses/bonus-form.tsx (field ids: `amountRubles`, `date`, `note`, `type` — `type` is a `<select id="type">` with two `<option>` values `premium`/`compensation`; success message is the literal text "Бонус сохранён."; form resets after success)
    src/app/(app)/bonuses/page.tsx (bonus rows render inside a `<ul>` under "История бонусов"; empty state shows "Нет бонусов" with an anchor `#bonus-form`)
    src/components/next-payment-card.tsx (when the next payment is a bonus, `KIND_LABELS.bonus === "Бонус или компенсация"`; when it composes with a salary/avans payment on the same date, `forecast.breakdown.bonusKopecks` renders under the "Бонус" `<dt>` inside the breakdown `<dl>`)
    src/app/actions/bonus.test.ts (exact server-side amount-in-kopecks math already unit-tested — this spec only needs to prove the UI wiring, not re-derive the tax math)
  </read_first>
  <action>
    Create `e2e/bonus.spec.ts` importing `test`/`expect` from `@playwright/test`. No `test.use({ storageState: ... })` override needed — this file is picked up by playwright.config.ts's `authenticated` project (added in Plan 07-01 Task 3), which already applies `playwright/.auth/user.json`.
    Test "creates a bonus and updates the forecast breakdown": `page.goto("/bonuses")` -> fill `amountRubles` with a value guaranteed to be visible in the forecast breakdown (pick a date equal to today's ISO date via the same `todayIsoInMoscow`-equivalent the app itself would compute, OR — simpler and less date-fragile — pick a date matching the authenticated fixture user's already-configured avans/salary schedule day if one exists; since `auth.setup.ts` only skipped/filled forms with arbitrary values, do not assume a specific schedule day is configured. Instead: set the bonus `date` field to TODAY (`new Date().toISOString().slice(0,10)`, computed in the test itself — a day-precision UTC/Moscow mismatch here is a non-issue since bonuses are matched by calendar date, not time-of-day) so it is guaranteed to be the earliest/next event in `generatePaymentEvents`'s ordering regardless of what the setup user's schedule looks like) -> fill `note` with a distinguishing string (e.g. `"e2e-bonus-" + Date.now()`) -> submit -> assert the success text "Бонус сохранён." appears -> assert the new row appears in the history list containing the entered amount (formatted via the same RUB-currency regex pattern from Plan 07-01's auth.spec.ts) and the note text -> `page.goto("/")` -> assert the NextPaymentCard shows kind label "Бонус или компенсация" (bonus-only next payment) OR, if it composed with a same-date scheduled payment, assert the breakdown `<dl>`'s "Бонус" row shows a non-zero ruble amount.
  </action>
  <verify>
    <automated>npm run test:e2e -- e2e/bonus.spec.ts -g "creates a bonus"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:e2e -- e2e/bonus.spec.ts -g "creates a bonus"` exits 0
    - The success text "Бонус сохранён." is asserted visible after form submission
    - The new bonus row in the history list contains both the entered amount (RUB-currency regex match) and the distinguishing note text
    - After navigating to `/`, the NextPaymentCard's kind label or breakdown `<dl>`'s "Бонус" row shows a non-zero, RUB-currency-formatted amount reflecting the new bonus
  </acceptance_criteria>
  <done>The create-bonus test passes against a real dev server and Neon database; the bonus appears both in the history list and in the home screen's forecast.</done>
</task>

<task type="auto">
  <name>Task 2: Edit and delete flows (window.confirm handling)</name>
  <files>e2e/bonus.spec.ts</files>
  <read_first>
    src/app/(app)/bonuses/bonus-row.tsx (edit mode toggled by "Изменить бонус" button; edit form has NO visible label on the type `<select>`, only `aria-label="Тип выплаты"` — use `getByRole("combobox", { name: "Тип выплаты" })` or `getByLabel("Тип выплаты")`, not `getByLabel` against visible text since there is none in edit mode; delete button text "Удалить бонус"; delete triggers a synchronous native `window.confirm(...)` dialog with message `` `Удалить бонус на сумму ${formatKopecks(...)} от ${formatIsoDateRu(...)}?` `` — Playwright does NOT auto-accept dialogs by default and the confirm() call will hang/reject the click's promise otherwise; register `page.on("dialog", (dialog) => dialog.accept())` BEFORE triggering the delete click, not after)
  </read_first>
  <action>
    Add a second test "edits a bonus and reflects the new amount": create a bonus first (reuse or duplicate the minimal creation steps from Task 1, with its own distinguishing note so the two tests don't collide on the same row), click "Изменить бонус" on that row, change the `amountRubles` field to a different value, submit, assert the success/updated amount now shows in the row (no separate "saved" toast on edit — bonus-row.tsx's `onEdit` just returns to display mode; assert on the row's rendered amount text itself, not a message).
    Add a third test "deletes a future bonus": create a bonus dated in the future (e.g. 30 days from today, so it is unambiguously deletable per the app's future-only delete rule proven in bonus.test.ts's "blocked-delete" case), register the `page.on("dialog", ...)` accept handler, click "Удалить бонус", assert the row is gone from the history list (or the empty-state "Нет бонусов" message appears if it was the only row) and that a fresh `page.goto("/")` no longer shows that bonus's amount anywhere in the forecast breakdown.
    Ensure all three tests in this file use distinct, self-generated data (unique note strings, dated far enough apart) so they can run in any order without interfering — this file's tests are NOT wrapped in their own account cleanup (the shared `authenticated` session's user persists for the whole suite run per Plan 07-01's design), so leftover bonus rows from a local re-run are an accepted, explicitly noted trade-off, not a bug to work around here.
  </action>
  <verify>
    <automated>npm run test:e2e -- e2e/bonus.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:e2e -- e2e/bonus.spec.ts` exits 0 (all 3 tests: create, edit, delete)
    - The edit test asserts the row's rendered amount text reflects the new value after submission, not a leftover value from before the edit
    - The delete test registers `page.on("dialog", (dialog) => dialog.accept())` before triggering the "Удалить бонус" click, and asserts the row is removed from the history list (or the "Нет бонусов" empty state appears)
    - After the delete test, a fresh `page.goto("/")` no longer shows the deleted bonus's amount anywhere in the forecast breakdown
    - All three tests use distinct note strings and non-colliding dates so they can run in any order
  </acceptance_criteria>
  <done>All three tests in e2e/bonus.spec.ts (create, edit, delete) pass in a single `npm run test:e2e -- e2e/bonus.spec.ts` run; the delete test correctly handles the native confirm dialog without hanging.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary is introduced by this plan — it exercises the same Server Action / session / database boundaries Plan 07-01 and prior phases (02-bonuses-one-off-payments) already threat-modeled. Recorded per-plan for traceability, not because new mitigation work is needed here.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-05 | Tampering | Bonus create/edit/delete Server Actions exercised through the real UI by an automated session | low | accept | These are the same `saveBonusAction`/`deleteBonusAction` boundaries already userId-scoped and unit-tested in Phase 2 (`src/app/actions/bonus.test.ts`) — this plan adds UI-level coverage of an already-mitigated boundary, not a new one |

</threat_model>

<verification>
`npm run test:e2e -- e2e/bonus.spec.ts` passes end to end (all 3 tests). `npx playwright test --list` shows all 3 tests under the `authenticated` project.
</verification>

<success_criteria>
E2E-02 is fully automated: bonus create/edit/delete are each verified through the real UI, and each mutation's effect on the home-screen forecast is asserted, not assumed.
</success_criteria>

## Artifacts this phase produces

- `e2e/bonus.spec.ts` — new file. Three Playwright tests covering create/edit/delete bonus flows and their effect on the home-screen forecast; runs under the `authenticated` project's storageState from Plan 07-01

<output>
Create `.planning/phases/07-e2e-test-suite/07-02-SUMMARY.md` when done
</output>
