---
milestone: v1.1
audited: 2026-09-03T00:50:00.000Z
status: tech_debt
scores:
  requirements: 24/24
  phases: 4/4
  integration: 24/24
  flows: 5/5
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 03-vacation-pay
    items:
      - "Open (WINDOWS.md #1): Task 1 tracer preview check and Task 2 full manual UAT (create/edit/overlap/blocked-delete/nav-link) for vacations was never click-through-verified in a live browser — substituted with a full production build + 315 passing automated tests covering the same behaviors. Never revisited across Phases 4-8."
  - phase: 05-deploy-pipeline-environment-config
    items:
      - "Open (WINDOWS.md #3): CI's `npm test` invocation excludes 6 unmocked DB-integration test files (schema, salary/bonus/vacation-repository, annual-summary, forecast) because the neon-http driver can't reach a vanilla postgres:17 service container. These still run locally against a real Neon branch and are unaffected, but CI itself doesn't exercise the DB-integration layer for money/tax-critical code paths. Phase 7's isolated-branch-per-CI-run (E2E-06) was the intended long-term fix but wasn't retrofitted onto npm test's own CI invocation, only onto the separate E2E suite."
  - phase: 08-visual-redesign-accessibility-pwa-safe-area
    items:
      - "Open (WINDOWS.md #6): e2e/bonus.spec.ts's 'creates a bonus and updates the forecast breakdown' test is date/schedule-proximity-sensitive — deterministically fails on some calendar dates (confirmed reproducible on 2026-09-03) because a same-day-dated bonus isn't picked up by forecastNextPayment/selectNextPaymentEvent when it's earlier than the nearest scheduled payment. Confirmed via git diff to be a pre-existing domain-logic characteristic, not caused by any Phase 8 file — out of scope for this milestone's frozen calculation logic, but worth a dedicated look in a future milestone."
      - "Minor (non-blocking): 3 of 7 Phase 8 SUMMARY.md files (08-01, 08-02, 08-04) don't carry a `requirements-completed:` frontmatter tag despite implementing real portions of UI-04/UI-06/PWA-01/PWA-02/UI-03 — a documentation-hygiene gap. 08-06's closure-plan SUMMARY re-confirms all 9 requirements collectively, and each phase's dedicated VERIFICATION.md independently confirms the same with concrete evidence, so this is not a functional gap."
      - "Minor (non-blocking): DEPLOY-02's own SUMMARY frontmatter (Phase 5) never carried a `requirements-completed:` tag either — resolved separately via WINDOWS.md #4 (fixed 2026-09-01), REQUIREMENTS.md marks it Complete."
---

# Milestone v1.1 Audit — Полировка MVP

**Audited:** 2026-09-03
**Scope:** Phases 5–8 (Deploy Pipeline & Environment Config, Auth Security Hardening, E2E Test Suite, Visual Redesign/Accessibility/PWA Safe-Area)

## Summary

All 4 phases passed their own goal-backward verification (including one gap-closure retry cycle in Phase 8). All 24 v1.1 requirements are satisfied and independently confirmed wired end-to-end by a dedicated cross-phase integration check. **No blocking gaps exist.** Three low-severity, already-tracked tech-debt items remain open in `.planning/WINDOWS.md` — none of them affect any v1.1 requirement's completion status, and one (#6) is a pre-existing domain-logic characteristic unrelated to this milestone's actual scope (a full visual/accessibility/PWA-safe-area polish pass, with an explicit "zero change to calculation logic" constraint).

## Phase Verification Status

| Phase | Name | Verification | Plans | Notes |
|-------|------|--------------|-------|-------|
| 5 | Deploy Pipeline & Environment Config | ✓ passed | 4/4 | Persistent-staging concept dropped mid-execution in favor of per-PR preview isolation (locked decision) |
| 6 | Auth Security Hardening | ✓ passed | 1/1 | SEC-01/02/03 confirmed via live PR-preview human DevTools check |
| 7 | E2E Test Suite | ✓ passed | 5/5 | CI `e2e` job proven green on a real run; found and fixed a real Playwright `webServer`-before-`globalSetup` ordering bug |
| 8 | Visual Redesign, Accessibility & PWA Safe-Area | ✓ passed | 7/7 | One gap found and closed via formal gap-closure cycle (annual chart zero-income empty state); one code-review cycle found and fixed 2 real WCAG AA contrast failures |

## Requirements Coverage (24/24)

All requirements cross-referenced across three sources — REQUIREMENTS.md traceability (all "Complete"), each phase's dedicated VERIFICATION.md (all "passed" with per-requirement evidence), and SUMMARY.md `requirements-completed` frontmatter (present for 14/17 plans; the 3 missing tags are documentation-hygiene only, not functional gaps — see tech_debt above). No orphaned requirements found.

| Requirement | Phase | Status |
|-------------|-------|--------|
| SEC-04, DEPLOY-01..05 | 5 | ✓ satisfied |
| SEC-01, SEC-02, SEC-03 | 6 | ✓ satisfied |
| E2E-01..06 | 7 | ✓ satisfied |
| UI-01..07, PWA-01, PWA-02 | 8 | ✓ satisfied |

## Cross-Phase Integration

Dedicated integration checker (gsd-integration-checker) confirmed, with direct source reads (not just trusting phase claims):
- Phase 6's SEC-02 hardcoded generic-error string is byte-for-byte unchanged after Phase 8's full login-page restyle.
- Phase 5's SEC-04 dynamic host-allowlist mechanism (`auth-allowed-hosts.ts`) is untouched by Phase 8 — no new hardcoded host/URL assumptions introduced.
- Phase 7's E2E selectors (including `e2e/vacation.spec.ts`'s positional `div.grid > span` indexing) still match Phase 8's restyled DOM structure — proven by a live, passing 13/13 E2E run against the fully-merged Phase 8 branch.
- Phase 5's CI pipeline correctly wires Phase 7's `e2e` job (`needs: ci`, no separate deploy step) as a required check alongside `ci` — confirmed live via `gh api` branch-protection query.
- The full golden-path user flow (register → onboarding → forecast → bonus/vacation mutation → annual summary → logout) works end-to-end across all 4 phases' contributions with zero visible seams.
- Zero calculation-logic drift confirmed for the whole milestone: `git diff --stat` against `src/domain/`, `src/lib/db/`, `src/app/actions/` is empty for every Phase 8 commit.

**No integration gaps found. No broken flows found.**

## Orchestrator-Performed Live Verification (Phase 8)

Beyond the automated checks above, the orchestrator personally exercised the running application in a real browser during Phase 8's closure to confirm what no automated test covers:
- Dark mode and light mode both render correctly (toggle via `prefers-color-scheme` emulation).
- Keyboard-only Tab navigation shows a clearly visible focus ring on form inputs and buttons.
- `env(safe-area-inset-top)`/`env(safe-area-inset-bottom)` are correctly wired inline on the header/main elements (standard WebKit CSS, not implementation-specific to the preview environment).
- The nav header does not overflow or collide at a 375px viewport.
- The annual pie chart renders correctly with real projected data (this also closes WINDOWS.md #1... correction, #2, the Phase 4 chart-legibility item).
- The zero-income empty-state fix (Phase 8's gap-closure plan) renders as intended in source, confirmed via passing render tests (live pixel-level confirmation of that exact empty state was not additionally performed, since the automated test suite already proves the branch fires and the markup pattern matches the established, already-visually-confirmed empty-state pattern used elsewhere in the app).

## Nyquist Compliance

Not applicable — the Nyquist validation capability is not active for this project (no `*-VALIDATION.md` files exist for any phase in this milestone, and no active `validate-phase` step hook was found at `verify:post`).

## Recommendation

**Proceed to `/gsd-complete-milestone v1.1`.** The 3 open WINDOWS.md tech-debt items are non-blocking, already understood, and appropriately scoped as follow-ups rather than v1.1 blockers:
- #1 and #3 predate this milestone (Phases 3 and 5) and were not in v1.1's scope to fix.
- #6 is domain-logic, explicitly out of scope for a "zero change to calculation logic" visual-polish milestone.

None of the 3 items block any of the 24 v1.1 requirements, all of which are satisfied and verified.
