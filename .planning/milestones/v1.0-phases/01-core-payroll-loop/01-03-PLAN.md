---
phase: 01-core-payroll-loop
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/domain/money.ts
  - src/domain/tax/ndfl-brackets.ts
  - src/domain/tax/calculate-ndfl.ts
  - src/domain/tax/calculate-ndfl.test.ts
  - src/domain/schedule/resolve-payment-date.ts
  - src/domain/schedule/resolve-payment-date.test.ts
  - src/domain/schedule/pay-gap.ts
  - src/domain/schedule/pay-gap.test.ts
autonomous: true
requirements: [TAX-01, TAX-02, SAL-01]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Tax on a single payment equals `taxOnCumulative(before + gross) - taxOnCumulative(before)`, so a payment straddling the 2,400,000 / 5,000,000 / 20,000,000 / 50,000,000 rub thresholds is split marginally rather than taxed at one flat rate (TAX-01)"
    - "Avans and salary are taxed by the identical code path — each is an independent income event evaluated against whatever cumulative income preceded it (TAX-02)"
    - "Take-home (net) amount is gross minus tax, where tax is the delta of ruble-rounded cumulative tax values (ст.52 НК РФ: round the cumulative tax, then take the difference between payments) — never rounded independently per payment, to prevent compounding rounding drift across the year."
    - "If avans and salary payment dates coincide on the same calendar date (e.g. after weekend/holiday shifting), avans is processed first (lower cumulative-income point), then salary — consistent with avans representing the earlier portion of the same pay period."
    - "Same-date avans/salary ordering is deterministic and stable: avans always sorts before salary when dates are equal (see adjacency resolution above) — never arbitrary/unstable order."
    - "If today's date is itself a valid payment date (post weekend/holiday-shift), the home screen shows today's date as the next payment — eligible dates are >= today, not strictly > today."
    - "A day-of-month that does not exist in the target month clamps to that month's last valid day (D-03): 31 becomes 30, 29, or 28 as appropriate"
    - "A payment date landing on a Saturday, Sunday, or RU public holiday shifts to the nearest earlier working day, chaining through consecutive non-working days (D-02)"
    - "Every module under `src/domain/` is pure: it imports nothing from `@/lib/db`, `next/*`, or any other I/O surface, so the whole tax and schedule surface is unit-testable without a database"
  prohibitions:
    - statement: "MUST NOT silently apply the 2025 НДФЛ bracket table to a payment dated in a tax year the table has not been verified for — an unregistered tax year must throw loudly rather than return a confidently wrong number."
      status: resolved
      verification: judgment
  artifacts:
    - path: "src/domain/money.ts"
      provides: "Kopeck money primitives and ru-RU formatting"
      exports: ["rublesToKopecks", "kopecksToRubles", "formatKopecks"]
    - path: "src/domain/tax/ndfl-brackets.ts"
      provides: "Versioned 2025 progressive НДФЛ scale keyed by effective year"
      exports: ["NDFL_SCALES", "bracketsForYear", "UnsupportedTaxYearError", "MAX_VERIFIED_TAX_YEAR"]
      contains: "2400000"
    - path: "src/domain/tax/calculate-ndfl.ts"
      provides: "Pure cumulative marginal НДФЛ calculation with ст.52 ruble rounding"
      exports: ["roundToRuble", "taxOnCumulative", "calculateNdfl"]
    - path: "src/domain/tax/calculate-ndfl.test.ts"
      provides: "Boundary-straddling, rounding, and avans/salary delta tests"
      min_lines: 80
    - path: "src/domain/schedule/resolve-payment-date.ts"
      provides: "Day-of-month clamping, weekend/holiday shifting, ordered payment-event generation"
      exports: ["resolvePaymentDate", "generatePaymentEvents", "nextPaymentOnOrAfter"]
    - path: "src/domain/schedule/resolve-payment-date.test.ts"
      provides: "Clamp, shift, ordering, and on-or-after-today tests"
      min_lines: 70
    - path: "src/domain/schedule/pay-gap.ts"
      provides: "ТК РФ 15-day avans/salary gap signal (D-04)"
      exports: ["MAX_PAY_GAP_DAYS", "payGapDays", "exceedsMaxPayGap"]
  key_links:
    - from: "src/domain/tax/calculate-ndfl.ts"
      to: "src/domain/tax/ndfl-brackets.ts"
      via: "bracketsForYear(taxYear) — the scale is looked up per year, never inlined as conditionals"
      pattern: "bracketsForYear"
    - from: "src/domain/schedule/resolve-payment-date.ts"
      to: "date-holidays"
      via: "RU public-holiday lookup, filtered to type 'public'"
      pattern: "date-holidays"
---

<objective>
Build the two pure domain engines the entire product's correctness rests on: the progressive 2025 НДФЛ calculation applied cumulatively from the start of the year (TAX-01, TAX-02), and the payment-date resolver that turns day-of-month schedule numbers into real calendar dates with D-03 clamping and D-02 weekend/holiday shifting (SAL-01), plus the D-04 fifteen-day gap signal.

Purpose: these are the highest-risk lines of code in НаРуки — get them wrong and every number on every screen is wrong. They are built as pure functions with zero DB, HTTP, or framework imports so they can be tested exhaustively at the boundaries that matter (2.4M / 5M / 20M / 50M rub thresholds, month-length edges, holiday chains) in milliseconds, then wired into the vertical slice by Plans 01-04 and 01-05.

Output: `src/domain/money.ts`, `src/domain/tax/*`, and `src/domain/schedule/*` with a passing Vitest suite.
</objective>

<execution_context>
@/home/zaiden/.claude/gsd-core/workflows/execute-plan.md
@/home/zaiden/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-core-payroll-loop/01-CONTEXT.md
@.planning/phases/01-core-payroll-loop/01-RESEARCH.md
@.planning/phases/01-core-payroll-loop/01-VALIDATION.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
</context>

## Artifacts this phase produces (Plan 03 scope)

| Symbol / path | Kind |
|---|---|
| `src/domain/money.ts` → `Kopecks` (type), `rublesToKopecks`, `kopecksToRubles`, `formatKopecks` | pure module |
| `src/domain/tax/ndfl-brackets.ts` → `NdflBracket` (type), `NDFL_SCALES`, `MAX_VERIFIED_TAX_YEAR`, `bracketsForYear`, `UnsupportedTaxYearError` | pure module |
| `src/domain/tax/calculate-ndfl.ts` → `roundToRuble`, `taxOnCumulative`, `calculateNdfl`, `NdflResult` (type) | pure module |
| `src/domain/schedule/resolve-payment-date.ts` → `PaymentKind`, `PaymentSchedule`, `PaymentEvent` (types), `resolvePaymentDate`, `generatePaymentEvents`, `nextPaymentOnOrAfter` | pure module |
| `src/domain/schedule/pay-gap.ts` → `MAX_PAY_GAP_DAYS`, `payGapDays`, `exceedsMaxPayGap` | pure module |
| `src/domain/tax/calculate-ndfl.test.ts`, `src/domain/schedule/resolve-payment-date.test.ts`, `src/domain/schedule/pay-gap.test.ts` | Vitest suites |

## Flagged assumptions (spec-less probe — unresolved row)

- **TAX-01 (unclassified):** the deterministic edge probe assigned no edge category to "система рассчитывает НДФЛ по прогрессивной шкале 2025 года нарастающим итогом", so no edge criterion constrains it beyond the explicitly-resolved TAX-02 and HOME-01 rows lifted into `must_haves.truths` above. This plan's tests cover the boundaries derived from `01-VALIDATION.md` instead; if a further TAX-01 edge exists, it is unplanned.

## Open item carried from research

`01-RESEARCH.md` Assumption A1 flags that the 2025 bracket thresholds and fixed-base amounts were cross-checked across three secondary sources (garant.ru, nalog-nalog.ru, an official ФНС regional page) but not read from the raw НК РФ ст.224 statute text. STATE.md carries the same blocker. Task 1's `human-check` closes it: the developer confirms the five thresholds and four fixed bases against primary statute text before these numbers reach a user. This is a read-and-confirm, not a rewrite.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure progressive НДФЛ engine with cumulative marginal calculation and ст.52 rounding</name>
  <files>src/domain/money.ts, src/domain/tax/ndfl-brackets.ts, src/domain/tax/calculate-ndfl.ts, src/domain/tax/calculate-ndfl.test.ts</files>
  <read_first>
    - `.planning/phases/01-core-payroll-loop/01-RESEARCH.md` § "НДФЛ 2025 Bracket Correctness" (the verified threshold/fixed-base table), § "Avans as Independent Taxable Event", and § "Code Examples" (the reference `taxOnCumulative` shape)
    - `.planning/research/PITFALLS.md` Pitfalls 1, 2, and 7 — marginal-vs-flat calculation, avans as an independent taxable event, and integer-kopeck money math with ст.52 rounding
    - `.planning/phases/01-core-payroll-loop/01-VALIDATION.md` § "Per-Task Verification Map" — the TAX-01 and TAX-02 rows this task's tests must satisfy
    - `vitest.config.ts` (from Plan 01-01) — the `@` alias and test include glob
  </read_first>
  <behavior>
    Written before the implementation. All amounts in kopecks (integers); rates as basis points.

    `roundToRuble(kopecks)` — ст.52 НК РФ:
    - 12_345 → 12_300 (45 kop dropped)
    - 12_350 → 12_400 (50 kop rounds up)
    - 12_399 → 12_400
    - 0 → 0

    `taxOnCumulative(cumulativeKopecks, 2025)` — the five-bracket scale, thresholds in rubles converted to kopecks:
    - 0 → 0
    - 100_000_00 (100k rub) → 13_000_00
    - 2_400_000_00 (exactly at the first threshold) → 312_000_00
    - 2_400_001_00 (one ruble over) → 312_000_15 rounded to whole rubles → 312_000_00 (15 kop dropped)
    - 5_000_000_00 → 702_000_00
    - 20_000_000_00 → 3_402_000_00
    - 50_000_000_00 → 9_402_000_00
    - 60_000_000_00 → 9_402_000_00 + 10_000_000_00 * 0.22 = 11_602_000_00

    `calculateNdfl(before, gross, 2025)`:
    - Straddling case: before = 2_350_000_00, gross = 100_000_00 → the first 50_000_00 taxed at 13%, the remaining 50_000_00 at 15%, so tax = 6_500_00 + 7_500_00 = 14_000_00; net = 86_000_00. A flat 13% would give 13_000_00 — the test must reject that value explicitly.
    - Independence: two sequential 100_000_00 payments from before = 0 produce cumulativeAfter 200_000_00 and total tax equal to `taxOnCumulative(200_000_00)` — folding avans then salary must not differ from a single 200_000_00 payment. This is the TAX-02 avans/salary-are-the-same-path assertion.
    - Rounding contract: for a payment sequence whose per-payment raw tax has fractional rubles, the sum of `calculateNdfl` taxes over the sequence equals `taxOnCumulative(final) - taxOnCumulative(start)` exactly — proving the delta-of-rounded-cumulatives rule and the absence of per-payment rounding drift.
    - Zero gross → tax 0, net 0, cumulativeAfter unchanged.

    `bracketsForYear(year)`:
    - 2025 and 2026 → the 176-ФЗ five-bracket scale
    - 2024 → throws `UnsupportedTaxYearError`
    - 2027 → throws `UnsupportedTaxYearError` (beyond `MAX_VERIFIED_TAX_YEAR`)
    - The thrown error message names the unsupported year.
  </behavior>
  <action>
    Create `src/domain/money.ts` exporting a `Kopecks` number type alias, `rublesToKopecks(rubles)` (multiply by 100 and round to an integer), `kopecksToRubles(kopecks)`, and `formatKopecks(kopecks)` producing a ru-RU currency string in rubles. This module has no imports beyond TypeScript types.

    Create `src/domain/tax/ndfl-brackets.ts`. Export an `NdflBracket` type with fields `fromKopecks`, `rateBasisPoints`, and `baseTaxKopecks`. Export `NDFL_SCALES` as a record keyed by the year a scale became effective, holding one entry — `2025` — with the five brackets from RESEARCH § "НДФЛ 2025 Bracket Correctness": thresholds 0 / 2,400,000 / 5,000,000 / 20,000,000 / 50,000,000 rubles, rates 1300 / 1500 / 1800 / 2000 / 2200 basis points, fixed bases 0 / 312,000 / 702,000 / 3,402,000 / 9,402,000 rubles — every value stored in kopecks. Export `MAX_VERIFIED_TAX_YEAR = 2026`. Export `bracketsForYear(year)` which selects the scale with the greatest effective year not exceeding `year`, and throws `UnsupportedTaxYearError` when `year` is below the earliest registered scale or above `MAX_VERIFIED_TAX_YEAR`. Export `UnsupportedTaxYearError` as an `Error` subclass whose message names the year. The registry plus the ceiling is deliberate: a forecast dated in a year nobody has verified the scale for must fail loudly rather than return a confidently wrong number, and raising `MAX_VERIFIED_TAX_YEAR` is the annual review checkpoint.

    Create `src/domain/tax/calculate-ndfl.ts` exporting three pure functions and one result type. `roundToRuble(kopecks)` implements ст.52 НК РФ with integer arithmetic only — add 50 kopecks, integer-divide by 100, multiply by 100 — so there is no floating-point tie-break ambiguity. `taxOnCumulative(cumulativeKopecks, taxYear)` looks the scale up via `bracketsForYear`, selects the highest bracket whose `fromKopecks` does not exceed the cumulative amount, computes `baseTaxKopecks + (cumulative - fromKopecks) * rateBasisPoints / 10000`, and returns that passed through `roundToRuble`. `calculateNdfl(cumulativeBeforeKopecks, paymentGrossKopecks, taxYear)` returns `{ taxKopecks, netKopecks, cumulativeAfterKopecks }` where `taxKopecks` is the difference of two `taxOnCumulative` results and `netKopecks` is gross minus that difference. Rounding happens inside `taxOnCumulative` on the cumulative figure only — never on the per-payment delta — because per-payment rounding compounds drift across a year (PITFALLS.md Pitfall 7).

    This module must import nothing from `@/lib`, `next`, or any I/O surface. Its only permitted imports are `./ndfl-brackets` and `../money`.

    Create `src/domain/tax/calculate-ndfl.test.ts` covering every case in the behavior block above, plus one property-style assertion: for a random sequence of 24 payments summing across at least two brackets, the sum of per-payment taxes equals `taxOnCumulative(total) - taxOnCumulative(0)`.
  </action>
  <verify>
    <automated>npx vitest run src/domain/tax &amp;&amp; npx tsc --noEmit &amp;&amp; node -e "const fs=require('fs');const t=fs.readFileSync('src/domain/tax/calculate-ndfl.ts','utf8');const b=fs.readFileSync('src/domain/tax/ndfl-brackets.ts','utf8');const strip=s=>s.split('\n').filter(l=>!/^\s*(\/\/|\*|\/\*)/.test(l)).join('\n');if(/from ['\"](next|@\/lib)/.test(strip(t)+strip(b)))throw new Error('domain/tax must not import next or @/lib');for(const v of ['2400000','5000000','20000000','50000000','312000','702000','3402000','9402000'])if(!strip(b).includes(v))throw new Error('bracket constant missing: '+v);if(!/UnsupportedTaxYearError/.test(b))throw new Error('UnsupportedTaxYearError must be exported');console.log('tax engine ok')"</automated>
    <human-check>
      Closes RESEARCH Assumption A1 and the matching STATE.md blocker. Open the primary statute text for НК РФ ст. 224 as amended by 176-ФЗ (12.07.2024) — pravo.gov.ru or consultant.ru's full-article view — and confirm against `src/domain/tax/ndfl-brackets.ts`:
      1. The five thresholds are 0, 2 400 000, 5 000 000, 20 000 000, and 50 000 000 rubles.
      2. The five rates are 13%, 15%, 18%, 20%, 22%.
      3. The four fixed bases are 312 000, 702 000, 3 402 000, and 9 402 000 rubles.
      4. The scale is still in force for the current tax year, unchanged since 01.01.2025 — this is what justifies `MAX_VERIFIED_TAX_YEAR = 2026`. If a later law changed it, say so rather than approving.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/domain/tax` passes with every case from the behavior block present as a named test
    - `src/domain/tax/ndfl-brackets.ts` contains all four threshold constants and all four fixed-base constants in kopeck form
    - `bracketsForYear(2024)` and `bracketsForYear(2027)` each throw `UnsupportedTaxYearError`; `bracketsForYear(2025)` and `bracketsForYear(2026)` return the five-bracket scale
    - `calculateNdfl(235_000_000, 10_000_000, 2025).taxKopecks` equals `1_400_000` and is asserted to differ from a flat-13% result
    - Two sequential 100 000 rub payments from a zero baseline produce the same total tax as one 200 000 rub payment
    - Neither `src/domain/tax/calculate-ndfl.ts` nor `src/domain/tax/ndfl-brackets.ts` imports from `next` or `@/lib`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>A pure, exhaustively-tested progressive НДФЛ engine exists: cumulative marginal calculation across all five 2025 brackets, ст.52 ruble rounding applied to the cumulative figure only, identical treatment of avans and salary, and a loud failure for any tax year whose scale has not been verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure payment-date resolver with day-of-month clamping, weekend/holiday shifting, and the ТК РФ gap signal</name>
  <files>src/domain/schedule/resolve-payment-date.ts, src/domain/schedule/resolve-payment-date.test.ts, src/domain/schedule/pay-gap.ts, src/domain/schedule/pay-gap.test.ts</files>
  <read_first>
    - `.planning/phases/01-core-payroll-loop/01-CONTEXT.md` — D-01 (day-of-month input), D-02 (shift earlier off weekends/holidays), D-03 (clamp to last valid day), D-04 (15-day gap warning)
    - `.planning/phases/01-core-payroll-loop/01-RESEARCH.md` § "Deriving 'Next Payment' (Not Stored)" (the reference `resolvePaymentDate` shape) and § "Common Pitfalls" (the `date-holidays` перенос data-freshness gap that must be documented, not solved, in Phase 1)
    - `.planning/phases/01-core-payroll-loop/01-VALIDATION.md` § "Per-Task Verification Map" — the two SAL-01 rows
    - `src/domain/money.ts` (Task 1) — for the shared kopeck type if any amount crosses this module
  </read_first>
  <behavior>
    Written before the implementation.

    `resolvePaymentDate(year, monthIndex, dayOfMonth)` returns a local-midnight `Date`:
    - (2026, 3 /* April */, 31) → 2026-04-30 — clamped to the 30-day month (D-03)
    - (2026, 1 /* February */, 31) → 2026-02-28
    - (2028, 1 /* February */, 31) → 2028-02-29 — leap year
    - (2026, 0 /* January */, 10) → shifts earlier past the New Year holiday block to the last working day of December 2025 (D-02 chains through consecutive non-working days)
    - A day landing on a Saturday shifts to the preceding Friday; a Sunday likewise shifts back to Friday
    - 2026-05-01 (Праздник Весны и Труда) shifts to the preceding working day
    - A day already on a working weekday is returned unchanged

    `generatePaymentEvents(schedule, fromDate, monthCount)` returns `PaymentEvent[]` sorted ascending:
    - For `{ avansDay: 20, salaryDay: 5 }` over 2 months from 2026-09-01, produces four events alternating salary (5th) and avans (20th), each already clamped and shifted
    - When an avans event and a salary event resolve to the same calendar date, the avans event sorts first, deterministically and stably, on every run
    - Sorting is by date ascending, then by kind rank with avans = 0 and salary = 1

    `nextPaymentOnOrAfter(schedule, today)`:
    - When `today` is itself a resolved payment date, that event is returned — eligibility is on-or-after, not strictly after
    - Otherwise returns the earliest event strictly later than today
    - Looks ahead at least two calendar months so a late-month `today` still finds next month's first payment
    - Returns `null` only when the lookahead window genuinely contains no event

    `payGapDays(avansDay, salaryDay)` / `exceedsMaxPayGap`:
    - (20, 5) → 15, does not exceed
    - (10, 25) → 15, does not exceed
    - (1, 20) → 19, exceeds
    - (5, 6) → 29, exceeds (a one-day gap on one side means a 29-day gap on the other)
    - `MAX_PAY_GAP_DAYS` is 15 and `exceedsMaxPayGap` is strictly greater-than
  </behavior>
  <action>
    Create `src/domain/schedule/resolve-payment-date.ts`. Export a `PaymentKind` union of the string literals for avans and salary, a `PaymentSchedule` type with `avansDay` and `salaryDay` numbers, and a `PaymentEvent` type with `date: Date` and `kind: PaymentKind`.

    `resolvePaymentDate(year, monthIndex, dayOfMonth)` first clamps the day to the target month's length using `lastDayOfMonth` from `date-fns` (D-03), builds a local-midnight date, then walks one day earlier at a time while the date is a weekend or an RU public holiday (D-02). Instantiate `date-holidays` once at module scope for country `RU` and filter its result to entries whose `type` is `public` — the library also reports observances and optional days, which are working days and must not trigger a shift. Add a file-header note recording the accepted v1 limitation from RESEARCH § Common Pitfalls: the library's bundled RU rule data covers fixed federal holidays but its yearly government weekend-transfer overrides are stale past 2022, so a one-off decreed non-working day may not shift.

    `generatePaymentEvents(schedule, fromDate, monthCount)` produces, for each of `monthCount` consecutive months starting at `fromDate`'s month, one avans event and one salary event through `resolvePaymentDate`, then sorts the combined array by timestamp ascending with a tie-break on kind rank — avans ranked 0, salary ranked 1 — so a same-date collision resolves to avans-then-salary deterministically. Avans sorting first reflects that it is the earlier portion of the same pay period and therefore sits at the lower cumulative-income point.

    `nextPaymentOnOrAfter(schedule, today)` calls `generatePaymentEvents` with a lookahead of three months starting one month before `today`'s month (so a clamped-and-shifted date belonging to the prior month is not missed), filters to events whose date is greater than or equal to `startOfDay(today)`, and returns the first, or `null` when the window is empty.

    Create `src/domain/schedule/pay-gap.ts` exporting `MAX_PAY_GAP_DAYS` set to 15, `payGapDays(avansDay, salaryDay)`, and `exceedsMaxPayGap(avansDay, salaryDay)`. `payGapDays` computes both circular distances between the two days on a 30-day reference cycle and returns the larger — the longest interval the employee actually waits. Document the 30-day reference cycle in a file-header comment as the deliberate simplification it is. `exceedsMaxPayGap` returns true when `payGapDays` is strictly greater than `MAX_PAY_GAP_DAYS`. These functions only report; nothing here blocks a save (D-04 is a non-blocking signal).

    Both modules are pure: permitted imports are `date-fns`, `date-holidays`, and sibling domain modules only. Nothing from `@/lib`, `next`, or React.

    Create `src/domain/schedule/resolve-payment-date.test.ts` and `src/domain/schedule/pay-gap.test.ts` covering every case in the behavior block. Pin `today` explicitly in each test rather than reading the system clock, so the suite cannot go green or red depending on the day it runs.
  </action>
  <verify>
    <automated>npx vitest run src/domain/schedule &amp;&amp; npx tsc --noEmit &amp;&amp; node -e "const fs=require('fs');const strip=s=>s.split('\n').filter(l=>!/^\s*(\/\/|\*|\/\*)/.test(l)).join('\n');const r=strip(fs.readFileSync('src/domain/schedule/resolve-payment-date.ts','utf8'));const g=strip(fs.readFileSync('src/domain/schedule/pay-gap.ts','utf8'));if(/from ['\"](next|react|@\/lib)/.test(r+g))throw new Error('domain/schedule must not import next, react, or @/lib');if(!/date-holidays/.test(r))throw new Error('holiday lookup missing');if(!/public/.test(r))throw new Error('holiday result must be filtered to public-type entries');for(const f of ['resolvePaymentDate','generatePaymentEvents','nextPaymentOnOrAfter'])if(!new RegExp('export (function|const) '+f).test(r))throw new Error('missing export '+f);if(!/MAX_PAY_GAP_DAYS/.test(g))throw new Error('MAX_PAY_GAP_DAYS missing');console.log('schedule engine ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/domain/schedule` passes with every case from the behavior block present as a named test
    - `resolvePaymentDate(2026, 3, 31)` returns 2026-04-30 and `resolvePaymentDate(2028, 1, 31)` returns 2028-02-29
    - A date falling on a Saturday, a Sunday, and on 2026-05-01 each resolve to an earlier working day
    - `generatePaymentEvents` output is sorted ascending, and a same-date avans/salary pair always yields avans at the lower index across repeated runs
    - `nextPaymentOnOrAfter(schedule, d)` returns the event dated `d` when `d` is itself a resolved payment date
    - `payGapDays(20, 5)` is 15 and does not exceed; `payGapDays(1, 20)` is 19 and exceeds
    - The holiday lookup filters `date-holidays` results to `public`-type entries
    - Neither schedule module imports from `next`, `react`, or `@/lib`
    - `resolve-payment-date.ts` carries a header note recording the `date-holidays` weekend-transfer data-freshness limitation
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Day-of-month schedule numbers resolve to real calendar dates with month-length clamping and earlier-shifting off weekends and RU public holidays; payment events generate in a stable avans-before-salary order; the next payment on or after a given day is derivable; and the ТК РФ 15-day gap is reported without blocking anything.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action / RSC → domain engine | Validated but user-originated amounts and day numbers enter pure functions here |
| domain engine → rendered output | Every money figure a user sees originates from these two modules |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02 | Tampering | `calculateNdfl` execution site | high | mitigate | The tax modules are pure and framework-free, and are imported only by server-side callers (Plans 01-04 and 01-05 enforce this at their own boundaries). Nothing here accepts or returns a client-asserted tax figure — the client can only supply gross amounts, which are re-taxed server-side on every read |
| T-01-10 | Tampering | Tax-year selection reaching an unverified bracket scale | high | mitigate | `bracketsForYear` throws `UnsupportedTaxYearError` outside the registered, human-verified range rather than falling back to the nearest scale; `MAX_VERIFIED_TAX_YEAR` makes the annual review explicit |
| T-01-11 | Information Disclosure | Money values in thrown error messages | low | accept | `UnsupportedTaxYearError` names only the year, never an amount. Amounts are not interpolated into any error string in these modules |
| T-01-12 | Denial of Service | Unbounded loop in the weekend/holiday backward walk | low | mitigate | The shift loop is bounded — the longest RU non-working run is the ~8-day New Year block — and the tests exercise that chain directly; a runaway walk would fail the January test rather than hang in production |
</threat_model>

<verification>
1. `npx vitest run src/domain` passes.
2. `npx tsc --noEmit` exits 0.
3. The purity assertions in both tasks' verify commands pass — no `next`, `react`, or `@/lib` import reaches `src/domain/`.
4. The bracket-constant assertion passes for all eight threshold and fixed-base values.
5. The `human-check` on the 2025 scale is performed and recorded, closing RESEARCH Assumption A1 and the matching STATE.md blocker.
</verification>

<success_criteria>
- `calculateNdfl` computes tax as the difference of ruble-rounded cumulative tax values, splitting a bracket-straddling payment marginally across the 13/15/18/20/22% scale.
- Avans and salary run through one identical code path; folding two payments equals taxing their sum.
- An unregistered or unverified tax year throws rather than returning a number.
- Day-of-month values clamp to the month's last valid day and shift earlier off weekends and RU public holidays, chaining through consecutive non-working days.
- Same-date avans and salary events order deterministically avans-first, and the next payment on or after today includes today.
- The 15-day avans/salary gap is reported as a signal that blocks nothing.
- Every module under `src/domain/` is pure and unit-testable with no database.
</success_criteria>

<output>
Create `.planning/phases/01-core-payroll-loop/01-03-SUMMARY.md` when done
</output>
