---
phase: 04-annual-overview-pwa-installability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/pay/payment-accrual.ts
  - src/lib/db/salary-repository.ts
  - src/app/actions/annual-summary.ts
  - src/app/actions/annual-summary.test.ts
  - src/components/annual-pie-chart.tsx
  - src/components/annual-pie-chart.render.test.tsx
  - src/app/(app)/page.tsx
  - src/app/(app)/error.tsx
  - src/app/(app)/error.render.test.tsx
  - package.json
autonomous: true
requirements: [HOME-02]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A signed-in user with configured salary+schedule sees a Recharts donut chart on the home screen below NextPaymentCard, with Налог and На руки slices that sum to Грязными, plus a 3-row summary (Грязными 100%, Налог X%, На руки Y%) each shown as a ruble amount and a percentage."
    - "computeAnnualSummary(userId, taxYear)'s grossKopecks/taxKopecks/netKopecks reconcile exactly (zero-kopeck tolerance) against an independent per-event getCumulativeIncomeBeforeDate + calculateNdfl walk over the same year's salary, bonus, and vacation events (HOME-02 success criterion #2)."
    - "The applicable YTD baseline amount is added into grossKopecks exactly once, as a dateless opening amount, and is never itself passed through calculateNdfl as a taxed event."
    - "A user with no configured salary/schedule sees no annual chart at all — the existing MISSING_COPY early return in page.tsx already fully covers this since computeAnnualSummary shares the identical `!schedule || salaryHistoryRows.length===0` gate as forecastNextPayment; the two can never diverge for the same taxYear."
    - "When the applicable YTD baseline is estimated (not user-confirmed), a distinct inline note reading 'Примечание: начальное значение дохода — это ваша оценка.' appears with the chart; when confirmed, no such note appears (UI-SPEC 'estimated-baseline'/'confirmed-baseline' rows)."
    - "computeAnnualSummary never mixes one user's salary/bonus/vacation rows into another user's summary (cross-user isolation test)."
    - { statement: "npm run build succeeds after the Recharts + annual-summary wiring is added to page.tsx (proves the new client component and Next.js metadata typing compile cleanly).", verification: backstop }
  artifacts:
    - src/app/actions/annual-summary.ts
    - src/app/actions/annual-summary.test.ts
    - src/components/annual-pie-chart.tsx
    - src/components/annual-pie-chart.render.test.tsx
    - src/app/(app)/error.tsx
    - src/app/(app)/error.render.test.tsx
  key_links:
    - "src/app/(app)/page.tsx -> computeAnnualSummary(userId, taxYear) -> AnnualPieChart({summary, taxYear})"
    - "computeAnnualSummary -> resolveBaselineWindow (new, shared with computeCumulativeIncome) -> guarantees the reconciliation truth above by construction, not by parallel reimplementation"
    - "computeAnnualSummary -> selectEffectiveEntry (newly exported from payment-accrual.ts) -> per-event salary resolution for every avans/salary occurrence in the year"
  flagged_assumptions:
    - "HOME-02 edge-probe: the deterministic edge-probe returned `unclassified`/unresolved for HOME-02 (could not auto-classify edges from requirement text alone). This plan substitutes the explicit truths above (reconciliation exactness, baseline-dateless-inclusion, zero-gross percentage guard, cross-user isolation, unconfigured-gate equivalence) as HOME-02's edge-coverage authority for this phase — not a silent drop."
    - "UI-SPEC 'loading' state for the annual chart section has NO dedicated automated test: an async Server Component's Suspense boundary cannot be unit-tested under this project's Vitest+@testing-library/react setup (Server Components require the Next.js RSC runtime, not plain ReactDOM). Per existing project precedent (NextPaymentCard/HOME-01 has never had a loading skeleton since Phase 1), this plan does NOT add a new Suspense boundary — the 'loading' state is satisfied by the browser's native full-page navigation affordance, same as every other page in this app. Flagged, not silently dropped."
    - "UI-SPEC 'unconfigured' state for the annual chart section (the 'Сводка недоступна' empty-state card) is implemented in page.tsx as a defensive branch that is structurally unreachable today (computeAnnualSummary's not-configured gate is byte-for-byte identical to forecastNextPayment's, and page.tsx already early-returns on the latter) — kept as cheap forward-compatible insurance rather than omitted, but has no dedicated automated test for the same reason page.tsx's existing MISSING_COPY branch has never had one in Phase 1-3."
  prohibitions:
    - "The annual summary must not be presented as an official/confirmed tax figure — it must carry the same forecast/disclaimer framing already established for NextPaymentCard ('Это плановый расчёт для планирования бюджета — не официальная и не гарантированная сумма'), since it aggregates the same simplified vacation-pay assumptions (VAC-03) and forecast-not-payslip framing."
---

**As a** signed-in user with salary, bonuses, and vacation pay already tracked in НаРуки, **I want to** see my full calendar-year gross/tax/net breakdown as a pie chart on my home screen, **so that** I can review my annual income at a glance without adding up every individual payment myself.

*(Note to reviewer: ROADMAP.md's Phase 4 Goal line is not itself in `As a/I want to/so that` format — this framing is a direct, non-invented paraphrase of that Goal line plus 04-CONTEXT.md's phase boundary. Run `/gsd mvp-phase 4` if a fully SPIDR-split story is wanted.)*

## Artifacts this phase produces (this plan)

- `computeAnnualSummary(userId: string, taxYear: number): Promise<AnnualSummaryResult>` — new server-only action (`src/app/actions/annual-summary.ts`)
- `AnnualSummary` interface — `{ grossKopecks, taxKopecks, netKopecks, baselineIsEstimated }`
- `AnnualSummaryResult` type — `{ configured: true; summary: AnnualSummary } | { configured: false; missing: "salary" | "schedule" }`
- `resolveBaselineWindow(baseline: YtdBaselineRow, isoDate: string)` — new exported function in `src/lib/db/salary-repository.ts`, returning `{ baselineApplies, baselineAmountKopecks, windowBoundIso }`
- `selectEffectiveEntry` — newly exported (was module-private) from `src/domain/pay/payment-accrual.ts`
- `AnnualPieChart({ summary, taxYear })` — new client component (`src/components/annual-pie-chart.tsx`)
- `AppError({ error, reset })` — new Next.js `error.tsx` boundary (`src/app/(app)/error.tsx`)
- `recharts@3.10.1` — new npm dependency (already vetted OK/Approved in 04-RESEARCH.md's Package Legitimacy Audit)

<objective>
Deliver HOME-02: a signed-in user sees a full calendar-year gross/tax/net breakdown on the home screen, combining salary, bonuses, and vacation pay, reconciling exactly (to the ruble) with the sum of individual payment breakdowns for the year.

Purpose: Extends the proven Phase 1-3 tax/vacation-pay engines (never reimplemented, only orchestrated) into a whole-year walk, matching `forecastNextPayment`'s existing single-event pattern.
Output: `computeAnnualSummary` server action, `AnnualPieChart` component wired onto the home screen, a reconciliation test proving exactness, and a generic app-level error boundary.
</objective>

<execution_context>
@/home/zaiden/.claude/gsd-core/workflows/execute-plan.md
@/home/zaiden/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-annual-overview-pwa-installability/04-CONTEXT.md
@.planning/phases/04-annual-overview-pwa-installability/04-UI-SPEC.md
@src/app/actions/forecast.ts
@src/lib/db/salary-repository.ts
@src/domain/pay/payment-accrual.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Annual summary engine + Recharts chart, wired end-to-end onto the home screen</name>
  <files>src/domain/pay/payment-accrual.ts, src/lib/db/salary-repository.ts, src/app/actions/annual-summary.ts, src/app/actions/annual-summary.test.ts, src/components/annual-pie-chart.tsx, src/app/(app)/page.tsx, package.json</files>
  <read_first>src/app/actions/forecast.ts, src/lib/db/salary-repository.ts, src/domain/pay/payment-accrual.ts, src/domain/schedule/resolve-payment-date.ts, src/domain/vacation/calculate-average-daily-earnings.ts, src/domain/tax/calculate-ndfl.ts, src/domain/money.ts, src/components/next-payment-card.tsx, src/app/(app)/page.tsx, src/app/actions/forecast.test.ts</read_first>
  <behavior>
    - computeAnnualSummary returns `{configured:false, missing:"salary"}` when the user has no salary_history rows, and `{configured:false, missing:"schedule"}` when there is no payment_schedule row (identical gate to forecastNextPayment: `!schedule || salaryHistoryRows.length===0`).
    - For a user with salary+schedule+bonuses+vacations spanning one calendar year, computeAnnualSummary's summary.grossKopecks and summary.taxKopecks equal the SUM, across every avans/salary/bonus/vacation event that year, of an independently-derived per-event `getCumulativeIncomeBeforeDate(userId, event.dateIso, kind)` + `calculateNdfl(cumulativeBefore, eventGross, taxYear)` oracle (kind="avans" for bonus/vacation events, matching forecastNextPayment's own existing convention; kind=event.kind for avans/salary events). Exact match, zero-kopeck tolerance.
    - A confirmed (non-estimated) YTD baseline whose asOfDate falls within taxYear is added ONCE into summary.grossKopecks as a dateless opening amount and seeds the cumulative tracker, but is never itself an argument to calculateNdfl.
    - summary.baselineIsEstimated is true whenever the baseline does not apply to taxYear (wrong year) OR the applicable baseline's own isEstimated flag is true; false only when an applicable, confirmed baseline exists.
    - Two different users' computeAnnualSummary results never include each other's salary/bonus/vacation rows (cross-user isolation).
  </behavior>
  <action>
    In src/domain/pay/payment-accrual.ts, change the module-private `function selectEffectiveEntry(salaryHistory, eventDateIso)` to `export function selectEffectiveEntry(...)` — no behavior change, only visibility, so annual-summary.ts can reuse the exact same "latest effectiveFrom not exceeding this event's own date" resolution `accruedGrossBetween` already uses internally, instead of re-deriving it.

    In src/lib/db/salary-repository.ts, extract computeCumulativeIncome's existing 6-line inline baseline-applicability block (the `paymentYear`/`baselineApplies`/`baselineAmountKopecks`/`windowBoundIso` computation) into a new exported pure function `resolveBaselineWindow(baseline: YtdBaselineRow, isoDate: string): { baselineApplies: boolean; baselineAmountKopecks: number; windowBoundIso: string }`, using the exact same formula (`baseline.asOfDate.slice(0,4) === isoDate.slice(0,4) && baseline.asOfDate <= isoDate`; windowBoundIso = baseline.asOfDate when applicable, else `${targetYear - 1}-12-31`). Refactor computeCumulativeIncome to call `resolveBaselineWindow(baseline, isoDate)` and destructure its three fields instead of computing them inline — pure refactor, zero behavior change (verify by running the FULL existing test suite, not just this plan's new tests). This single shared function is what guarantees computeAnnualSummary's baseline-window logic can never drift from the already-shipped single-event forecast logic.

    Create src/app/actions/annual-summary.ts as a server-only module (identical `typeof window !== "undefined"` throw-guard pattern as src/app/actions/forecast.ts, same doc-comment convention, no `"use server"` directive since this is called directly from a Server Component render, never as a form action). Export `interface AnnualSummary { grossKopecks: Kopecks; taxKopecks: Kopecks; netKopecks: Kopecks; baselineIsEstimated: boolean }` and `type AnnualSummaryResult = { configured: true; summary: AnnualSummary } | { configured: false; missing: "salary" | "schedule" }`. Implement `async function computeAnnualSummary(userId: string, taxYear: number): Promise<AnnualSummaryResult>`:
      1. Fetch in parallel via Promise.all: getSchedule(userId), listSalaryHistory(userId), listBonuses(userId), listVacations(userId), getYtdBaseline(userId).
      2. Not-configured gate identical to forecastNextPayment's own.
      3. Call `resolveBaselineWindow(ytdBaseline, \`${taxYear}-12-31\`)` — passing Dec-31-of-taxYear as the isoDate stand-in makes the SAME isoDate-relative formula answer "does this baseline apply anywhere in taxYear," since any in-year date is automatically `<=` Dec 31 of that year while the year-match check still restricts it correctly.
      4. Build a flat list of `{dateIso, grossKopecks}` events: (a) avans/salary — call `generatePaymentEvents(schedule, new Date(taxYear - 1, 11, 1), 14)` (14-month span with a full year's safety margin on both sides, mirroring accruedGrossBetween's own symmetric-margin discipline, so a January-nominal event that D-02 shifts backward into December is never missed), map each to its own `format(date,"yyyy-MM-dd")`, keep only events where `dateIso > windowBoundIso && dateIso.slice(0,4) === String(taxYear)`, resolve each one's own gross via `selectEffectiveEntry(salaryHistoryEntries, dateIso)` + `halfSplitGross(entry.grossAmountKopecks, event.kind)` (skip an event with no applicable entry, exactly like accruedGrossBetween's own rule); (b) each bonus row individually (not grouped with same-date schedule events — this is safe because summed marginal tax over any chronological sequence between two cumulative endpoints telescopes to the same total regardless of same-date grouping, so per-row bonus events are simpler and equally correct for an AGGREGATE total), keeping only rows with `date > windowBoundIso && date.slice(0,4) === String(taxYear)`; (c) each vacation row via `resolveVacationPaymentDate(vacation.startDate)` as its dateIso and `calculateVacationPayGross(startDate, endDate, salaryHistoryEntries, toPremiumBonusEntries(bonusRows))` for its gross — note the premium-bonus entries passed to the vacation-pay average MUST be the full unfiltered `bonusRows` (its own 12-month lookback window can reach into the prior year), never year-filtered — keeping only vacations whose resolved paymentDateIso satisfies the same `> windowBoundIso && year===taxYear` filter.
      5. Sort the flat event list ascending by dateIso (any stable tie-break is correct per the telescoping argument above).
      6. Walk chronologically: `cumulativeYtdKopecks` and `totalGrossKopecks` both start at `baselineAmountKopecks`; for each event call `calculateNdfl(cumulativeYtdKopecks, event.grossKopecks, taxYear)`, add `event.grossKopecks` to `totalGrossKopecks`, add the returned `taxKopecks` to `totalTaxKopecks`, then add `event.grossKopecks` to `cumulativeYtdKopecks`.
      7. Return `{configured:true, summary:{grossKopecks: totalGrossKopecks, taxKopecks: totalTaxKopecks, netKopecks: totalGrossKopecks - totalTaxKopecks, baselineIsEstimated: !baselineApplies || ytdBaseline.isEstimated}}`.

    Create src/app/actions/annual-summary.test.ts following the exact throwaway-user/beforeEach/afterEach isolation pattern from src/app/actions/forecast.test.ts (no frozen clock needed anywhere in this file — computeAnnualSummary takes taxYear as an explicit parameter and never calls nowInMoscow/todayIsoInMoscow, unlike forecastNextPayment). Write the 5 behaviors listed above as separate `it(...)` cases; the reconciliation case should mix at least one salary+schedule pair, one same-date-as-schedule bonus, one different-date bonus, and one vacation, all within a single named taxYear, to exercise every event kind in one pass.

    Run `npm install recharts@3.10.1` (already vetted OK/Approved in 04-RESEARCH.md's Package Legitimacy Audit — no blocking checkpoint required).

    Create src/components/annual-pie-chart.tsx as a `"use client"` component (Recharts internally needs the client runtime; this is why it cannot be a server component like NextPaymentCard) exporting `AnnualPieChart({ summary, taxYear }: { summary: AnnualSummary; taxYear: number })`. Render EXACTLY two Recharts `<Pie>` data slices — Налог (tax, fill #dc2626, Tailwind red-600) and На руки (net, fill #16a34a, Tailwind green-600) — which mathematically partition Грязными (gross) into 360°; do NOT add a third "Грязными" wedge (04-RESEARCH.md's own illustrative pie-chart example computed `gross - tax` and mislabeled it "Грязными" when that is actually net again — a bug, not a spec requirement — and a genuine third gross wedge would make displayed percentages sum to 200%, which is mathematically wrong). Guard `taxPercent`/`netPercent` against division by zero when `summary.grossKopecks === 0` (render 0% rather than NaN). Above/beside the donut render a bolded total line showing `formatKopecks(summary.grossKopecks)` labeled "Грязными", plus a plain 3-row summary list (not a Recharts Legend, since Recharts' data-driven Legend only has 2 entries) showing all three rows — Грязными (100%), Налог, На руки — each with `formatKopecks(...)` and a ru-RU-locale one-decimal percentage (`Intl.NumberFormat("ru-RU", {minimumFractionDigits:1, maximumFractionDigits:1}).format(percent)` + "%"). Section heading "Годовая сводка", chart title "Доход и налоги в {taxYear} году" (per UI-SPEC Copywriting Contract). Card styling: `rounded border border-zinc-300 p-4` matching Phase 1-2 card convention (see next-payment-card.tsx's `rounded-2xl border border-zinc-200 p-6` as the closest analog — use the plainer `rounded border border-zinc-300 p-4` from UI-SPEC's own "Pie-chart container" rule instead, since UI-SPEC is the more specific source for this new component).

    Wire it into src/app/(app)/page.tsx: import `computeAnnualSummary` and `AnnualPieChart`; compute `const currentYear = Number(todayIsoInMoscow().slice(0, 4));` (already imported from @/domain/time in this file's existing forecast-date logic — reuse it, do not hand-roll `new Date().getFullYear()`, matching 04-RESEARCH.md Pitfall 4); replace the single `forecastNextPayment(userId)` await with `const [result, annualResult] = await Promise.all([forecastNextPayment(userId), computeAnnualSummary(userId, currentYear)]);`; in the existing `if (!result.configured)` early-return branch, leave it untouched (this already fully satisfies CONTEXT.md's "chart hidden when not configured" rule, since annualResult shares the identical gate); in the configured return, after `<NextPaymentCard forecast={result.forecast} />`, add `{annualResult.configured ? <AnnualPieChart summary={annualResult.summary} taxYear={currentYear} /> : null}`.
  </action>
  <verify>
    <automated>npm run test -- src/app/actions/annual-summary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- src/app/actions/annual-summary.test.ts` passes with all 5 behaviors green.
    - `npm run test` (full suite) still passes after the computeCumulativeIncome/selectEffectiveEntry refactors — zero regressions in existing forecast.test.ts / payment-accrual.test.ts / salary-repository.test.ts.
    - `npm run build` succeeds (proves the new client component, recharts types, and page.tsx wiring compile).
    - `grep -c "grossKopecks: value" src/components/annual-pie-chart.tsx | grep -qv '^[3-9]'` is not the check — instead: `grep -c "Cell key" src/components/annual-pie-chart.tsx` returns exactly 2 (proves exactly 2 pie slices, not 3).
  </acceptance_criteria>
  <done>computeAnnualSummary is implemented, tested, and reconciles exactly with the per-event oracle; AnnualPieChart renders a 2-slice donut plus a 3-row summary list on the home screen below NextPaymentCard for a configured user.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Baseline-estimated note, defensive empty-state fallback, app-level error boundary, edge-case tests</name>
  <files>src/app/(app)/page.tsx, src/components/annual-pie-chart.tsx, src/components/annual-pie-chart.render.test.tsx, src/app/(app)/error.tsx, src/app/(app)/error.render.test.tsx, src/app/actions/annual-summary.test.ts</files>
  <read_first>src/app/(app)/page.tsx, src/components/annual-pie-chart.tsx, src/app/actions/annual-summary.ts, src/app/(app)/bonuses/bonus-row.render.test.tsx, .planning/phases/04-annual-overview-pwa-installability/04-UI-SPEC.md</read_first>
  <behavior>
    - AnnualPieChart renders the note "Примечание: начальное значение дохода — это ваша оценка." (text-xs text-zinc-500, per UI-SPEC's D-V12-disclaimer-matching styling) when `summary.baselineIsEstimated` is true, and renders no such note when false.
    - AppError (error.tsx) renders heading "Ошибка при загрузке сводки", body "Попробуйте ещё раз", and a retry button; clicking the retry button calls the `reset` prop exactly once.
    - computeAnnualSummary: a YTD baseline dated exactly `${taxYear}-12-31` (confirmed, not estimated) excludes every dated event from the year's walk (since every possible in-year event date is `<= windowBoundIso`), so summary.grossKopecks equals exactly the baseline amount and summary.taxKopecks is exactly 0.
    - computeAnnualSummary: two throwaway users with disjoint salary/bonus/vacation data never see each other's rows reflected in their own summary totals.
  </behavior>
  <action>
    In src/components/annual-pie-chart.tsx, add the conditional baseline-estimated note directly below the chart title, rendered only when `summary.baselineIsEstimated` is true, using the exact copy above and `text-xs text-zinc-500` classes (per UI-SPEC's "estimated-baseline" row). Do NOT render the UI-SPEC's separately-listed "confirmed" banner copy — UI-SPEC's own Coverage Summary table explicitly resolves the confirmed case as "no banner is displayed," which supersedes its own Copywriting Contract's vestigial confirmed-banner text entry.

    In src/app/(app)/page.tsx, add a defensive fallback for the (currently unreachable-by-construction, but forward-compatible) case where `result.configured` is true yet `annualResult.configured` is false: render a small card with heading "Сводка недоступна", body "Заполните оклад и график выплат, чтобы увидеть годовую сводку.", and a CTA link to `/settings/salary` labeled "Настроить оклад" (UI-SPEC's own exact copy for this state), in place of the `null` currently returned by the ternary added in Task 1.

    Create src/app/(app)/error.tsx as a `"use client"` Next.js error-boundary file (the file-convention name Next.js requires for this route segment's error UI) exporting `default function AppError({ error, reset }: { error: Error & { digest?: string }; reset: () => void })`. Render heading "Ошибка при загрузке сводки", body "Попробуйте ещё раз", and a button that calls `reset()` on click. This is a general safety net for the whole `(app)` route segment (home, bonuses, vacations, settings, onboarding) catching any unexpected render-time throw — not scoped narrowly to the annual chart, matching Next.js's own idiomatic per-segment error-boundary convention.

    Extend src/components/annual-pie-chart.render.test.tsx (new file) with the `@vitest-environment jsdom` pragma (matching src/app/(app)/bonuses/bonus-row.render.test.tsx's exact convention) covering both baseline-note behaviors above via `@testing-library/react`'s `render`/`screen`.

    Create src/app/(app)/error.render.test.tsx (jsdom, same pragma) rendering `<AppError error={new Error("boom")} reset={resetMock} />`, asserting the heading/body text and that clicking the retry button invokes `resetMock` exactly once.

    Extend src/app/actions/annual-summary.test.ts with the two additional `it(...)` cases described in this task's behavior list (baseline-excludes-all-events; cross-user isolation).
  </action>
  <verify>
    <automated>npm run test -- src/components/annual-pie-chart.render.test.tsx src/app/(app)/error.render.test.tsx src/app/actions/annual-summary.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test -- src/components/annual-pie-chart.render.test.tsx src/app/(app)/error.render.test.tsx src/app/actions/annual-summary.test.ts` passes.
    - `npm run test` (full suite) still green.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>Estimated-baseline note, defensive empty-state fallback, and an app-level error boundary are implemented and tested; annual-summary.test.ts covers the baseline-excludes-all-events edge case and cross-user isolation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Neon DB -> annual-summary.ts | server-only aggregation reads salary_history/bonuses/vacations/ytd_baseline, always scoped by `userId` sourced from `requireUserId()` (session-derived), never client input |
| annual-summary.ts -> page.tsx (Server Component) -> AnnualPieChart (Client Component) | only pre-aggregated, non-raw totals (grossKopecks/taxKopecks/netKopecks/baselineIsEstimated) cross the server/client boundary — no individual salary/bonus/vacation rows ever reach the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | computeAnnualSummary | high | mitigate | userId always sourced from `requireUserId()`; every query already ownership-scoped via the existing listSalaryHistory/listBonuses/listVacations/getSchedule/getYtdBaseline repository functions (all filter by `eq(table.userId, userId)`); Task 2's cross-user isolation test proves userB's data never appears in userA's summary. |
| T-04-02 | Denial of Service | AnnualPieChart rendering an unbounded/malformed summary | low | accept | `grossKopecks===0` is explicitly guarded to avoid NaN percentages; Recharts renders a fixed 2-slice dataset regardless of the magnitude of the underlying kopeck values, so no user-controlled data volume affects render cost. |
| T-04-SC | Tampering | npm install (recharts) | high | mitigate | 04-RESEARCH.md's Package Legitimacy Audit already lists `recharts` as OK/Approved (5-year history, ~2.5M weekly downloads, github.com/recharts/recharts) — no `[ASSUMED]`/`[SUS]` disposition, so no blocking human-verify checkpoint is required per the gate rules; pin the exact audited version (3.10.1) at install time. |

**Canon-referral breadcrumb (dropped, not minted):** "computeAnnualSummary's server-rendered output could be cached and served across different authenticated users via Next.js's Full Route Cache" was considered and dropped — `requireUserId()`'s use of `headers()` already forces this route into dynamic (per-request) rendering under Next.js's automatic dynamic-detection rules, an existing architectural guarantee this phase does not need to newly establish.
</threat_model>

<verification>
- `npm run test -- src/app/actions/annual-summary.test.ts src/components/annual-pie-chart.render.test.tsx src/app/(app)/error.render.test.tsx` — all new/edge-case behaviors green.
- `npm run test` — full suite green (zero regressions from the computeCumulativeIncome/selectEffectiveEntry refactors).
- `npm run build` — compiles cleanly with recharts and the new client component.
</verification>

<success_criteria>
- HOME-02 satisfied: home screen shows an annual gross/tax/net breakdown combining salary, bonuses, and vacation pay, for a configured user, reconciling exactly with the sum of individual payment breakdowns (proven by the reconciliation test).
- No chart shown for an unconfigured user (existing gate reused, not duplicated).
- Estimated-baseline caveat surfaced distinctly on the chart itself, never silently treated as confirmed.
</success_criteria>

<output>
Create `.planning/phases/04-annual-overview-pwa-installability/04-01-SUMMARY.md` when done
</output>
