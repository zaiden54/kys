DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=
# DEPRECATED: no longer required — SEC-04's dynamic allowedHosts in
# src/lib/auth.ts / src/lib/auth-allowed-hosts.ts replaces it.
# BETTER_AUTH_URL=http://localhost:3000
